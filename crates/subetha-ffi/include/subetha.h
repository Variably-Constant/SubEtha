/*
 * SubEtha C ABI.
 *
 * Generated by cbindgen from crates/subetha-ffi. Do not edit by hand:
 * tests/header.rs regenerates this file and fails when it has drifted.
 *
 * Every function returns int32_t: SUBETHA_OK, or a code that
 * subetha_strerror() names. A failing call leaves its specifics in a
 * thread-local buffer that subetha_last_error_detail() copies out.
 */

#ifndef SUBETHA_H
#define SUBETHA_H

/* Generated with cbindgen:0.28.0 */

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

/**
 * The most bytes one interned value holds: a reference carries the
 * length in 24 bits.
 */
#define SUBETHA_ARENA_VALUE_MAX 16777215

/**
 * The largest capacity an arena is created with: a reference carries the
 * offset in 40 bits.
 */
#define SUBETHA_ARENA_CAPACITY_MAX 1099511627775

/**
 * No ordering constraint beyond the operation's own atomicity.
 */
#define SUBETHA_ORDER_RELAXED 0

/**
 * Reads that follow this one see everything the releasing write saw.
 */
#define SUBETHA_ORDER_ACQUIRE 1

/**
 * Writes made before this one are visible to an acquiring reader.
 */
#define SUBETHA_ORDER_RELEASE 2

/**
 * Acquire and release together, for a read-modify-write.
 */
#define SUBETHA_ORDER_ACQ_REL 3

/**
 * Acquire and release, and a single total order over every such
 * operation. What the plain calls use.
 */
#define SUBETHA_ORDER_SEQ_CST 4

/**
 * The atomic holds a `uint32_t`.
 */
#define SUBETHA_ATOMIC_U32 0

/**
 * The atomic holds a `uint64_t`.
 */
#define SUBETHA_ATOMIC_U64 1

/**
 * The atomic holds a `bool`.
 */
#define SUBETHA_ATOMIC_BOOL 2

/**
 * Bytes one broadcast slot carries; a push zero-fills past its payload
 * and a receive yields the whole slot.
 */
#define SUBETHA_BROADCAST_PAYLOAD_BYTES 52

/**
 * Consumers a broadcast ring can have registered at once.
 */
#define SUBETHA_BROADCAST_MAX_CONSUMERS 16

/**
 * Keys one node holds. A tree of `capacity` nodes holds at most
 * `capacity * SUBETHA_BTREE_KEYS_PER_NODE` entries, and fewer in
 * practice, since a node below the root is only guaranteed half full.
 */
#define SUBETHA_BTREE_KEYS_PER_NODE 15

/**
 * The least keys a node below the root carries.
 */
#define SUBETHA_BTREE_MIN_KEYS_PER_NODE 7

/**
 * The most bytes one cell holds.
 */
#define SUBETHA_CELL_PAYLOAD_BYTES 52

/**
 * The epoch a version carries while it is still current: no pin can be
 * at or above it, so `subetha_pin_sees` answers true for it.
 */
#define SUBETHA_EPOCH_LIVE UINT64_MAX

/**
 * The smallest block a region carries: one must hold the free-list link
 * that threads it onto the free list.
 */
#define SUBETHA_FRAME_BLOCK_MIN 8

/**
 * The index that names no block.
 */
#define SUBETHA_FRAME_NO_BLOCK 4294967295

/**
 * In a compound morph, keep the current value of that axis.
 */
#define SUBETHA_KEEP UINT32_MAX

/**
 * Morph target locale: keep the current one.
 */
#define SUBETHA_TARGET_KEEP 0

/**
 * Morph target locale: anonymous memory.
 */
#define SUBETHA_TARGET_ANON 1

/**
 * Morph target locale: files under the base path in `target_name`.
 */
#define SUBETHA_TARGET_FILE 2

/**
 * Morph target locale: shared memory under the prefix in `target_name`.
 */
#define SUBETHA_TARGET_SHM 3

/**
 * The call succeeded.
 */
#define SUBETHA_OK 0

/**
 * No call may precede `subetha_init`.
 */
#define SUBETHA_E_NOT_INITIALIZED 1

/**
 * `subetha_init` was called again with a different default mode.
 */
#define SUBETHA_E_INIT_CONFLICT 2

/**
 * An argument was null, zero where a count is required, or out of range.
 */
#define SUBETHA_E_INVALID_ARGUMENT 3

/**
 * The handle names no live object: never issued, already destroyed, or
 * issued by an earlier occupant of the same table slot.
 */
#define SUBETHA_E_INVALID_HANDLE 4

/**
 * A panic inside an earlier call on this handle left the object
 * unusable. Destroy it and create another.
 */
#define SUBETHA_E_HANDLE_POISONED 5

/**
 * This call panicked. The handle it ran on, if any, is now poisoned;
 * `subetha_last_panic_message` has the text.
 */
#define SUBETHA_E_PANIC 6

/**
 * The handle is live but names an object of another kind.
 */
#define SUBETHA_E_WRONG_KIND 7

/**
 * An allocation failed.
 */
#define SUBETHA_E_OUT_OF_MEMORY 8

/**
 * A caller-provided buffer is too small; the detail names the size needed.
 */
#define SUBETHA_E_BUFFER_TOO_SMALL 9

/**
 * A string argument is not valid UTF-8.
 */
#define SUBETHA_E_INVALID_UTF8 10

/**
 * The timeout elapsed before the condition held.
 */
#define SUBETHA_E_TIMEOUT 11

/**
 * The library has been shut down; `subetha_init` again before any other call.
 */
#define SUBETHA_E_SHUT_DOWN 12

/**
 * `subetha_shutdown` found live handles and closed them; the detail says how many.
 */
#define SUBETHA_E_HANDLES_WERE_LIVE 13

/**
 * The requested behavior exists in the library but not on this platform
 * or in this mode.
 */
#define SUBETHA_E_NOT_SUPPORTED 14

/**
 * The object was destroyed while this call was waiting on it.
 */
#define SUBETHA_E_DESTROYED 15

/**
 * The ring is full.
 */
#define SUBETHA_E_RING_FULL 100

/**
 * The ring is empty.
 */
#define SUBETHA_E_RING_EMPTY 101

/**
 * A backing exists but its magic or capacity does not match the request.
 */
#define SUBETHA_E_RING_LAYOUT_MISMATCH 102

/**
 * The payload exceeds what one slot carries.
 */
#define SUBETHA_E_RING_PAYLOAD_TOO_LARGE 103

/**
 * The operation needs ordering stamps the ring was not built with.
 */
#define SUBETHA_E_RING_NOT_STAMPED 104

/**
 * Another consumer holds the drainer lease; back off and retry.
 */
#define SUBETHA_E_RING_NOT_DRAINER 105

/**
 * A morph was asked for while the previous shape still holds a backlog.
 */
#define SUBETHA_E_RING_STALE_BACKLOG 106

/**
 * The OS refused an open, map, or remove; the detail names the kind.
 */
#define SUBETHA_E_RING_IO 107

/**
 * Producer registration refused by a declared contract or the slot ceiling.
 */
#define SUBETHA_E_RING_TOO_MANY_PRODUCERS 108

/**
 * Consumer registration refused by a declared contract or the slot ceiling.
 */
#define SUBETHA_E_RING_TOO_MANY_CONSUMERS 109

/**
 * Registration claimed a slot but the grown backing could not be created.
 */
#define SUBETHA_E_RING_GROWTH_FAILED 110

/**
 * Every waiter slot is in use; poll with the try variant instead.
 */
#define SUBETHA_E_RING_WAKER_FULL 111

/**
 * A waker region exists but does not match the ring's request.
 */
#define SUBETHA_E_RING_WAKER_LAYOUT 112

/**
 * Every consumer slot of the broadcast ring is taken.
 */
#define SUBETHA_E_BROADCAST_NO_CONSUMER_SLOT 113

/**
 * The consumer index is out of range or not registered.
 */
#define SUBETHA_E_BROADCAST_INVALID_CONSUMER 114

/**
 * The position has not been published yet.
 */
#define SUBETHA_E_PUBSUB_PENDING 115

/**
 * The position was overwritten before it was read; the subscriber's
 * position has moved to the ring's head.
 */
#define SUBETHA_E_PUBSUB_LOST 116

/**
 * The deque handle is a thief's; push and pop belong to the owner.
 */
#define SUBETHA_E_DEQUE_NOT_OWNER 117

/**
 * The map has no free slot for the key.
 */
#define SUBETHA_E_MAP_FULL 118

/**
 * The key has no entry in the map.
 */
#define SUBETHA_E_MAP_KEY_ABSENT 119

/**
 * The arena has no room for the whole value.
 */
#define SUBETHA_E_ARENA_FULL 120

/**
 * The reference reaches past what the arena holds.
 */
#define SUBETHA_E_ARENA_INVALID_REF 121

/**
 * The handle was opened read-only and the call writes.
 */
#define SUBETHA_E_READ_ONLY 122

/**
 * The index is at or past the length or the capacity.
 */
#define SUBETHA_E_OUT_OF_BOUNDS 123

/**
 * Every pin slot in the epoch table is held by a live process.
 */
#define SUBETHA_E_EPOCHS_PINS_EXHAUSTED 124

/**
 * Every ticket slot in the epoch table is held by a live process.
 */
#define SUBETHA_E_EPOCHS_TICKETS_EXHAUSTED 125

/**
 * Someone else holds the lock, or a writer is waiting for it.
 */
#define SUBETHA_E_WOULD_BLOCK 126

/**
 * The call needs the lease and this process does not hold it.
 */
#define SUBETHA_E_NOT_OWNER 127

/**
 * Every slot in the fence clock is registered.
 */
#define SUBETHA_E_FENCE_CLOCK_FULL 128

/**
 * The barrier has no live peer to wait for, so a wait would never be
 * released by anyone.
 */
#define SUBETHA_E_BARRIER_NO_LIVE_PEERS 129

/**
 * The epoch waited on is behind the one the barrier has reached, so the
 * caller is asking to join a rendezvous that is already over.
 */
#define SUBETHA_E_BARRIER_EPOCH_PASSED 130

/**
 * Every holder slot on the shared value is held by a live process.
 */
#define SUBETHA_E_ARC_HOLDERS_EXHAUSTED 131

/**
 * The claim on a lazy value belongs to a process that is gone, so no
 * one will publish it. `subetha_lazy_reclaim` frees it for the next
 * caller to claim.
 */
#define SUBETHA_E_LAZY_CLAIMANT_GONE 132

/**
 * The lazy value is claimed by a live process and not yet published.
 */
#define SUBETHA_E_LAZY_NOT_CLAIMED 133

/**
 * The handle names an adaptive ring.
 */
#define SUBETHA_KIND_RING 1

/**
 * The handle names a blocking single-producer single-consumer ring.
 */
#define SUBETHA_KIND_SPSC 2

/**
 * The handle names one producer of a blocking MPSC pool.
 */
#define SUBETHA_KIND_MPSC_PRODUCER 3

/**
 * The handle names the consumer of a blocking MPSC pool.
 */
#define SUBETHA_KIND_MPSC_CONSUMER 4

/**
 * The handle names one producer of a blocking MPMC grid.
 */
#define SUBETHA_KIND_MPMC_PRODUCER 5

/**
 * The handle names one consumer of a blocking MPMC grid.
 */
#define SUBETHA_KIND_MPMC_CONSUMER 6

/**
 * The handle names a Vyukov MPMC ring.
 */
#define SUBETHA_KIND_VYUKOV 7

/**
 * The handle names the producer side of a Lamport SPSC pair.
 */
#define SUBETHA_KIND_LAMPORT_PRODUCER 8

/**
 * The handle names the consumer side of a Lamport SPSC pair.
 */
#define SUBETHA_KIND_LAMPORT_CONSUMER 9

/**
 * The handle names a broadcast ring.
 */
#define SUBETHA_KIND_BROADCAST 10

/**
 * The handle names a pub/sub ring.
 */
#define SUBETHA_KIND_PUBSUB 11

/**
 * The handle names a subscriber of a pub/sub ring.
 */
#define SUBETHA_KIND_SUBSCRIBER 12

/**
 * The handle names a capacity-adaptive ring.
 */
#define SUBETHA_KIND_CAPACITY_RING 13

/**
 * The handle names a locale-adaptive ring.
 */
#define SUBETHA_KIND_LOCALE_RING 14

/**
 * The handle names a capacity-adaptive broadcast ring.
 */
#define SUBETHA_KIND_CAPACITY_BROADCAST 15

/**
 * The handle names a capacity-adaptive pub/sub ring.
 */
#define SUBETHA_KIND_CAPACITY_PUBSUB 16

/**
 * The handle names a subscriber of a capacity-adaptive pub/sub ring.
 */
#define SUBETHA_KIND_CAPACITY_SUBSCRIBER 17

/**
 * The handle names an exact-delivery receiver on a stamped ring.
 */
#define SUBETHA_KIND_ORDERED_RECEIVER 18

/**
 * The handle names a shared Treiber stack.
 */
#define SUBETHA_KIND_STACK 19

/**
 * The handle names a work-stealing deque, as its owner or as a thief.
 */
#define SUBETHA_KIND_DEQUE 20

/**
 * The handle names a pollable notifier attached to a ring.
 */
#define SUBETHA_KIND_NOTIFIER 21

/**
 * The handle names a shared hash map.
 */
#define SUBETHA_KIND_HASHMAP 22

/**
 * The handle names a shared string arena.
 */
#define SUBETHA_KIND_ARENA 23

/**
 * The handle names a shared vec.
 */
#define SUBETHA_KIND_VEC 24

/**
 * The handle names a shared slab.
 */
#define SUBETHA_KIND_SLAB 25

/**
 * The handle names a shared region.
 */
#define SUBETHA_KIND_REGION 26

/**
 * The handle names a shared atomic of one of the three widths.
 */
#define SUBETHA_KIND_ATOMIC 27

/**
 * The handle names a shared doubly-linked list.
 */
#define SUBETHA_KIND_LIST 28

/**
 * The handle names a shared B-tree map.
 */
#define SUBETHA_KIND_BTREE 29

/**
 * The handle names a shared cell.
 */
#define SUBETHA_KIND_CELL 30

/**
 * The handle names a frame region of fixed-size blocks.
 */
#define SUBETHA_KIND_FRAME_REGION 31

/**
 * The handle names an epoch table.
 */
#define SUBETHA_KIND_EPOCHS 32

/**
 * The handle names a pin held against an epoch table.
 * Retired: a pin is a token now, not a handle. The number is not
 * reused, so an old pin handle is refused rather than resolved.
 */
#define SUBETHA_KIND_PIN 33

/**
 * The handle names an open ticket on an epoch table.
 * Retired, like `SUBETHA_KIND_PIN`: a ticket is a token now. The number
 * is not reused.
 */
#define SUBETHA_KIND_TICKET 34

/**
 * The handle names a reader-writer lock.
 */
#define SUBETHA_KIND_RWLOCK 35

/**
 * Retired. A hold on a reader-writer lock is a token now, not a handle,
 * so no object has this kind. The number is not reused: a caller that
 * still passes an old hold handle should be refused rather than
 * resolved to whatever took the number.
 */
#define SUBETHA_KIND_LOCK_HOLD 36

/**
 * The handle names a counting semaphore.
 */
#define SUBETHA_KIND_SEMAPHORE 37

/**
 * Retired, like `SUBETHA_KIND_LOCK_HOLD`: a permit is a token now, not
 * a handle. The number is not reused, so an old permit handle is
 * refused rather than resolved to whatever took the number.
 */
#define SUBETHA_KIND_PERMIT 38

/**
 * The handle names a condition variable.
 */
#define SUBETHA_KIND_CONDVAR 39

/**
 * The handle names an owner lease.
 */
#define SUBETHA_KIND_OWNER_LEASE 40

/**
 * The handle names a leader election.
 */
#define SUBETHA_KIND_LEADER 41

/**
 * The handle names a heartbeat table.
 */
#define SUBETHA_KIND_HEARTBEAT 42

/**
 * The handle names a holder table.
 */
#define SUBETHA_KIND_HOLDERS 43

/**
 * The handle names a shared fence clock.
 */
#define SUBETHA_KIND_FENCE_CLOCK 44

/**
 * The handle names an epoch barrier.
 */
#define SUBETHA_KIND_EPOCH_BARRIER 45

/**
 * The handle names a shared value kept alive by its holders.
 */
#define SUBETHA_KIND_SHARED_ARC 46

/**
 * The handle names a value produced once across every process.
 */
#define SUBETHA_KIND_LAZY_VALUE 47

/**
 * The handle names a cross-process waker.
 */
#define SUBETHA_KIND_WAKER 48

/**
 * The handle names one half of a TCP bridge.
 */
#define SUBETHA_KIND_TCP_BRIDGE 49

/**
 * The handle names one half of a QUIC bridge.
 */
#define SUBETHA_KIND_QUIC_BRIDGE 50

/**
 * The handle names one half of a Sens-O-Matic link.
 */
#define SUBETHA_KIND_SENS 51

/**
 * The handle names a shared Bloom filter.
 */
#define SUBETHA_KIND_BLOOM 52

/**
 * The handle names a shared count-min sketch.
 */
#define SUBETHA_KIND_CMS 53

/**
 * The handle names a shared histogram.
 */
#define SUBETHA_KIND_HISTOGRAM 54

/**
 * The handle names a shared rate limiter.
 */
#define SUBETHA_KIND_RATE_LIMITER 55

/**
 * The handle names a shared blocked Bloom filter.
 */
#define SUBETHA_KIND_BLOCKED_BLOOM 56

/**
 * The handle names a shared reservoir sampler.
 */
#define SUBETHA_KIND_RESERVOIR 57

/**
 * The handle names a shared topology map.
 */
#define SUBETHA_KIND_TOPOLOGY 58

/**
 * The handle names a shared versioned chain.
 */
#define SUBETHA_KIND_VERSIONED_CHAIN 59

/**
 * The handle names a shared versioned slab.
 */
#define SUBETHA_KIND_VERSIONED_SLAB 60

/**
 * The handle names a shared versioned map.
 */
#define SUBETHA_KIND_VERSIONED_MAP 61

/**
 * The handle names a shared laned versioned map.
 */
#define SUBETHA_KIND_LANED_MAP 62

/**
 * The handle names a virtual endpoint registry.
 */
#define SUBETHA_KIND_ENDPOINT_REGISTRY 63

/**
 * The handle names a runtime-mutable quality-of-service policy.
 */
#define SUBETHA_KIND_QOS_POLICY 64

/**
 * The handle names a shared bit vector.
 */
#define SUBETHA_KIND_BIT_VEC 65

/**
 * The handle names a shared HyperLogLog.
 */
#define SUBETHA_KIND_HLL 66

/**
 * The handle names a least-recently-used cache.
 */
#define SUBETHA_KIND_LRU_CACHE 67

/**
 * The handle names a directed graph.
 */
#define SUBETHA_KIND_GRAPH 68

/**
 * The handle names a versioned time-point tile.
 */
#define SUBETHA_KIND_TIME_POINT 69

/**
 * The handle names a strategy-switching set.
 */
#define SUBETHA_KIND_UNIVERSAL 70

/**
 * The handle names a cascade tower.
 */
#define SUBETHA_KIND_K_TOWER 71

/**
 * The handle names a blocking bridge: a blocking SPSC ring carried
 * across a socket connection.
 */
#define SUBETHA_KIND_BLOCKING_TCP_BRIDGE 72

/**
 * The process id that leads nothing; every real process id differs.
 */
#define SUBETHA_LEADER_NONE 0

/**
 * The grace window to pass when a caller has no reason to pick another.
 */
#define SUBETHA_LEADER_DEFAULT_GRACE 3

/**
 * The sentinel head's index. A walk that reaches it has run out of
 * nodes, and no node of the caller's ever carries it.
 */
#define SUBETHA_LIST_HEAD_INDEX 0

/**
 * The index that names no node.
 */
#define SUBETHA_LIST_NIL_INDEX 4294967295

/**
 * Locale: anonymous memory, this process only.
 */
#define SUBETHA_LOCALE_ANON 0

/**
 * Locale: files, page-cached and persistent.
 */
#define SUBETHA_LOCALE_FILE 1

/**
 * Locale: named shared memory, RAM-resident.
 */
#define SUBETHA_LOCALE_SHM 2

/**
 * The smallest precision the sketch accepts.
 */
#define SUBETHA_HLL_MIN_PRECISION 4

/**
 * The largest. Registers, and so the file, grow as two to this power.
 */
#define SUBETHA_HLL_MAX_PRECISION 16

/**
 * The index that means no node or no edge. A walk ends when
 * `subetha_graph_next_edge` answers this.
 */
#define SUBETHA_GRAPH_NIL UINT32_MAX

/**
 * The word holds a double.
 */
#define SUBETHA_NAN_F64 0

/**
 * The word holds nil.
 */
#define SUBETHA_NAN_NIL 1

/**
 * The word holds a signed 32-bit integer.
 */
#define SUBETHA_NAN_I32 2

/**
 * The word holds an unsigned 32-bit integer.
 */
#define SUBETHA_NAN_U32 3

/**
 * The word holds a boolean.
 */
#define SUBETHA_NAN_BOOL 4

/**
 * The word holds an offset pointer.
 */
#define SUBETHA_NAN_OFFSET_PTR 5

/**
 * The word holds a tag this library does not name. A caller reading one
 * is talking to a newer writer and should leave the value alone rather
 * than guess at it.
 */
#define SUBETHA_NAN_RESERVED 6

/**
 * The widest tag the two-level form accepts. Thirty two would leave no
 * index bits.
 */
#define SUBETHA_NAN_MAX_TAG_BITS 31

/**
 * Lanes in a tile.
 */
#define SUBETHA_TILE_LANES 16

/**
 * The most a lane's payload can carry.
 */
#define SUBETHA_TILE_MAX_PAYLOAD 56

/**
 * Bytes a packed pointer occupies.
 */
#define SUBETHA_UMBRA_BYTES 16

/**
 * Payload bytes a caller may carry beside the tag.
 */
#define SUBETHA_UMBRA_EXT_BYTES 7

/**
 * The tag meaning no caller extension is present.
 */
#define SUBETHA_UMBRA_EXT_NONE 0

/**
 * The target index meaning the pointer aims at nothing.
 */
#define SUBETHA_UMBRA_NIL UINT32_MAX

/**
 * Elements in a vector, scanned linearly. Faster for a small set.
 */
#define SUBETHA_UNIVERSAL_VEC 0

/**
 * Elements in a hash map. Faster for a large one.
 */
#define SUBETHA_UNIVERSAL_MAP 1

/**
 * The index that means "nothing here" at any level of a path.
 */
#define SUBETHA_TOWER_NIL UINT32_MAX

/**
 * In-process memory; the bytes go when the process does.
 */
#define SUBETHA_QOS_VOLATILE 0

/**
 * Named shared memory; outlives the producer while a holder keeps it.
 */
#define SUBETHA_QOS_TRANSIENT 1

/**
 * File-backed; the bytes reach the page cache and can be flushed.
 */
#define SUBETHA_QOS_PERSISTENT 2

/**
 * Loss is acceptable.
 */
#define SUBETHA_QOS_BEST_EFFORT 0

/**
 * Loss is not.
 */
#define SUBETHA_QOS_RELIABLE 1

/**
 * Keep the last `history_depth` items.
 */
#define SUBETHA_QOS_KEEP_LAST 0

/**
 * Keep everything, so `history_depth` says nothing.
 */
#define SUBETHA_QOS_KEEP_ALL 1

/**
 * Each producer's items stay in its own order; producers do not order
 * against each other.
 */
#define SUBETHA_QOS_PER_PRODUCER 0

/**
 * One order across every producer.
 */
#define SUBETHA_QOS_GLOBAL_FIFO 1

/**
 * The streaming preset: volatile, best-effort, last 1024, 100 ms.
 */
#define SUBETHA_QOS_PRESET_STREAMING 0

/**
 * The reliable publish-subscribe preset.
 */
#define SUBETHA_QOS_PRESET_RELIABLE_PUBSUB 1

/**
 * The persistent log preset.
 */
#define SUBETHA_QOS_PRESET_PERSISTENT_LOG 2

/**
 * The id names no binding.
 */
#define SUBETHA_ENDPOINT_NONE 0

/**
 * The id names a ring in this process.
 */
#define SUBETHA_ENDPOINT_LOCAL 1

/**
 * The id names a remote host.
 */
#define SUBETHA_ENDPOINT_REMOTE 2

/**
 * The handle was created by `subetha_deque_create`: it pushes, pops and
 * steals.
 */
#define SUBETHA_DEQUE_OWNER 0

/**
 * The handle was opened by `subetha_deque_open_thief`: it steals.
 */
#define SUBETHA_DEQUE_THIEF 1

/**
 * The insert placed a new entry.
 */
#define SUBETHA_MAP_INSERTED 0

/**
 * The insert overwrote an existing entry's value.
 */
#define SUBETHA_MAP_UPDATED 1

/**
 * The most bytes a key and a value take together in one entry.
 */
#define SUBETHA_HASHMAP_PAYLOAD_BYTES 48

/**
 * The process id in a slot nobody holds.
 */
#define SUBETHA_HEARTBEAT_EMPTY_PID 0

/**
 * Work units one slot's bitmap tracks, so a bit index runs from zero to
 * one below this.
 */
#define SUBETHA_HEARTBEAT_IN_FLIGHT 64

/**
 * The grace window a barrier uses when the caller passes zero: how many
 * epochs a heartbeat slot may be stale before it stops counting as a
 * peer worth waiting for.
 *
 * Spelled as a literal because cbindgen copies the right-hand side into
 * the header verbatim, and a name from another crate does not exist
 * there. `the_default_grace_matches_the_primitive` holds it to the
 * value it mirrors.
 */
#define SUBETHA_BARRIER_GRACE_DEFAULT 3

/**
 * Bytes a lazy value can hold.
 */
#define SUBETHA_LAZY_MAX_BYTES ONCE_PAYLOAD_BYTES

/**
 * Nobody has claimed the value.
 */
#define SUBETHA_LAZY_UNCLAIMED (uint32_t)STATE_EMPTY

/**
 * A caller holds the claim and has not published yet.
 */
#define SUBETHA_LAZY_CLAIMED (uint32_t)STATE_INITIALIZING

/**
 * The value is published.
 */
#define SUBETHA_LAZY_PUBLISHED (uint32_t)STATE_INITIALIZED

/**
 * Remove the backing when the last holder releases.
 */
#define SUBETHA_ARC_UNLINK 0

/**
 * Leave the backing for a process that attaches later.
 */
#define SUBETHA_ARC_KEEP 1

/**
 * The half that ships items to a peer.
 */
#define SUBETHA_SENS_SENDER 0

/**
 * The half that binds and delivers what arrives.
 */
#define SUBETHA_SENS_RECEIVER 1

/**
 * The sliding-window random linear code, for low to moderate loss.
 */
#define SUBETHA_SENS_CODE_RLC 0

/**
 * The block Reed-Solomon code, for high sustained loss.
 */
#define SUBETHA_SENS_CODE_RS 1

/**
 * The accompanying count is a real number.
 */
#define SUBETHA_DROPS_EXACT 0

/**
 * It happened and this host will not say how many. The count is zero and
 * means nothing.
 */
#define SUBETHA_DROPS_OCCURRED 1

/**
 * This host reports nothing either way. The count is zero and means
 * nothing.
 */
#define SUBETHA_DROPS_UNKNOWN 2

/**
 * The half that ships from a local producer ring.
 */
#define SUBETHA_BRIDGE_CLIENT 0

/**
 * The half that binds, accepts one connection and receives.
 */
#define SUBETHA_BRIDGE_SERVER 1

/**
 * One producer to one consumer.
 */
#define SUBETHA_TOPOLOGY_POINT_TO_POINT 0

/**
 * One producer to many consumers.
 */
#define SUBETHA_TOPOLOGY_BROADCAST_TREE 1

/**
 * Peers routing to one another.
 */
#define SUBETHA_TOPOLOGY_ALL_TO_ALL_MESH 2

/**
 * The fan-out at or above which the traffic reads as a broadcast tree.
 */
#define SUBETHA_TOPOLOGY_FAN_OUT_DEFAULT 3

/**
 * The fan-in at or above which the traffic reads as an all-to-all mesh.
 */
#define SUBETHA_TOPOLOGY_FAN_IN_DEFAULT 3

/**
 * The state of a slot nobody holds.
 */
#define SUBETHA_HOLDER_FREE 0

/**
 * The state of a slot claimed whose payload is not decided yet. A
 * caller's payload may not be this value.
 */
#define SUBETHA_HOLDER_RESERVED UINT64_MAX

/**
 * Strategy: the merge by stamp with a consumer-side re-ordering window.
 */
#define SUBETHA_ORDERED_REORDER 0

/**
 * Strategy: the strict merge; the pop is exact, nothing is buffered.
 */
#define SUBETHA_ORDERED_STRICT 1

/**
 * Strategy: direct delivery; the stamps need no correction.
 */
#define SUBETHA_ORDERED_DIRECT 2

/**
 * The largest payload a lease carries.
 */
#define SUBETHA_LEASE_PAYLOAD_MAX 48

/**
 * The process id that holds no lease; every real process id differs.
 */
#define SUBETHA_LEASE_NO_OWNER 0

/**
 * Bytes one pub/sub slot carries; a publish zero-fills past its payload
 * and a read yields the whole slot.
 */
#define SUBETHA_PUBSUB_PAYLOAD_BYTES 56

/**
 * The index that names no slot; every other index below the capacity
 * names one.
 */
#define SUBETHA_REGION_NIL_INDEX 4294967295

/**
 * The bytes one sampled record takes.
 */
#define SUBETHA_RESERVOIR_ITEM_BYTES 56

/**
 * Bytes one slot holds. A pop buffer must be at least this large.
 */
#define SUBETHA_RING_SLOT_BYTES 64

/**
 * The largest payload every shape accepts. A push between this and
 * `SUBETHA_RING_SLOT_BYTES` succeeds only while the ring is in its
 * single-producer single-consumer shape, and is otherwise refused with
 * `SUBETHA_E_RING_PAYLOAD_TOO_LARGE`.
 */
#define SUBETHA_RING_PAYLOAD_MAX 56

/**
 * Ring shape: one producer, one consumer.
 */
#define SUBETHA_RING_SHAPE_SPSC 0

/**
 * Ring shape: several producers, one consumer.
 */
#define SUBETHA_RING_SHAPE_MPSC 1

/**
 * Ring shape: several producers, several consumers.
 */
#define SUBETHA_RING_SHAPE_MPMC 2

/**
 * Ring shape: the Vyukov queue, global FIFO across producers.
 */
#define SUBETHA_RING_SHAPE_VYUKOV 3

/**
 * Shared-memory names resolve per session (`Local\` on Windows).
 */
#define SUBETHA_SHM_SESSION 0

/**
 * Shared-memory names resolve machine-wide (`Global\` on Windows).
 */
#define SUBETHA_SHM_MACHINE 1

/**
 * The shape sidecar's scan cadence when a managed-mode caller names
 * none, in microseconds.
 *
 * The cadence is the added round-trip latency of managed mode, so this
 * is a latency budget rather than a tuned optimum: measured on Linux
 * and FreeBSD from 250 to 10000 microseconds, a request/response round
 * trip costs exactly one interval and a streaming caller costs nothing,
 * with no knee anywhere in that range. A quarter of a millisecond is
 * small against a network hop and against most process-to-process round
 * trips, and costs a few percent of the scanning CPU that a ten-times
 * shorter cadence would.
 */
#define SUBETHA_SCAN_INTERVAL_DEFAULT_US 250

/**
 * Wait without a deadline.
 */
#define SUBETHA_WAIT_FOREVER -1

/**
 * The largest frame that rides inside a slot; a larger one spills to the
 * ring's payload region, which exists from the first spill on.
 */
#define SUBETHA_RING_FRAME_INLINE_BUDGET 51

/**
 * Bytes per block of the payload region the ring creates on its first
 * spilled frame. A frame larger than this is refused with
 * `SUBETHA_E_RING_PAYLOAD_TOO_LARGE`.
 */
#define SUBETHA_RING_FRAME_DEFAULT_BLOCK 8192

/**
 * The frame rode inside the slot.
 */
#define SUBETHA_FRAME_INLINE 0

/**
 * The frame's bytes live in the payload region; the slot carried the offset.
 */
#define SUBETHA_FRAME_OFFSET 1

/**
 * Inline when the frame fits the budget, else the region.
 */
#define SUBETHA_LAYOUT_AUTO 0

/**
 * Inline only; a frame past the budget is refused.
 */
#define SUBETHA_LAYOUT_FORCE_INLINE 1

/**
 * The region even for a frame that would fit inline.
 */
#define SUBETHA_LAYOUT_FORCE_OFFSET 2

/**
 * No ordering stamps: the ring's payload is the whole slot and its
 * ordering is per-producer FIFO.
 */
#define SUBETHA_STAMPS_NONE 0

/**
 * Stamps from the host's default source: the TSC where it is invariant,
 * the shared counter on x86 without one, the monotonic clock elsewhere.
 * On an open, whatever the creator chose.
 */
#define SUBETHA_STAMPS_DEFAULT 1

/**
 * Stamps from the invariant TSC.
 */
#define SUBETHA_STAMPS_TSC 2

/**
 * Stamps from one shared counter: a total order at the price of one
 * contended increment per push.
 */
#define SUBETHA_STAMPS_SHARED_COUNTER 3

/**
 * Stamps from the system's monotonic clock, in nanoseconds.
 */
#define SUBETHA_STAMPS_MONOTONIC 4

/**
 * Ordering mode: the partition pop, per-producer FIFO; stamps only feed
 * the inversion counter.
 */
#define SUBETHA_ORDERING_UNORDERED 0

/**
 * Ordering mode: a k-way merge by stamp over the ring heads, global FIFO
 * within the stamp source's skew window, under a single drainer.
 */
#define SUBETHA_ORDERING_MERGE_BY_STAMP 1

/**
 * Ordering mode: the merge plus the per-producer watermark gate, exact
 * global FIFO at the cost of waiting on the slowest producer.
 */
#define SUBETHA_ORDERING_MERGE_STRICT 2

/**
 * Contract ordering: no guarantee.
 */
#define SUBETHA_CONTRACT_UNORDERED 0

/**
 * Contract ordering: each producer's items in push order.
 */
#define SUBETHA_CONTRACT_FIFO_PER_PRODUCER 1

/**
 * Contract ordering: one global order across every producer; the
 * per-producer-lane shape is then refused.
 */
#define SUBETHA_CONTRACT_FIFO 2

/**
 * Contract ordering: an item lands at most `k_out_of_order` positions
 * from its global arrival order.
 */
#define SUBETHA_CONTRACT_K_OUT_OF_ORDER 3

/**
 * `last_holder`: the backings outlive the last handle that held them,
 * which is what a ring with no holder table does.
 */
#define SUBETHA_LAST_HOLDER_KEEP 0

/**
 * `last_holder`: the last handle to release removes every backing the
 * ring's prefix names, including the holder table itself.
 */
#define SUBETHA_LAST_HOLDER_UNLINK 1

/**
 * ABI version, major part. The ABI is unstable until the major reaches
 * 1, which it does once tiers 0 through 3 have shipped and a consumer
 * outside this repository has used them. Both halves are load-bearing:
 * the first real user is what finds the awkward signature, and the
 * freeze is what makes an awkward signature permanent.
 */
#define SUBETHA_ABI_VERSION_MAJOR 0

/**
 * ABI version, minor part: the highest tier of `C_ABI_TIERS.md` that
 * has shipped, plus one. A consumer reads it to learn which families it
 * is linked against without probing for symbols, which is the whole
 * reason `subetha_abi_version` exists.
 *
 * | minor | tier | what it adds |
 * |---|---|---|
 * | 1 | 0 | runtime, handle table, error model, the adaptive ring |
 * | 2 | 1 | the remaining rings and channels, stack, deque, notifier |
 * | 3 | 2 | ten shared-state families |
 * | 4 | 3 | eight coordination families |
 * | 5 | 4 | the transports: Sens-O-Matic and the TCP and QUIC bridges |
 * | 6 | 5 | the probabilistic and specialist structures |
 *
 * `abi_version_matches_the_tiers_document` in `tests/header.rs` holds
 * this against the document, so the two cannot drift.
 */
#define SUBETHA_ABI_VERSION_MINOR 6

/**
 * ABI version, patch part. Moves for a fix inside a tier that changes
 * no signature.
 */
#define SUBETHA_ABI_VERSION_PATCH 0

/**
 * Strict mode: no thread starts unless a call says so, data-path calls
 * write into caller buffers, and allocation is documented per function.
 */
#define SUBETHA_MODE_STRICT 0

/**
 * Managed mode: the object runs the background work the Rust API runs.
 */
#define SUBETHA_MODE_MANAGED 1

/**
 * Take the process default chosen at `subetha_init`.
 */
#define SUBETHA_MODE_DEFAULT 2

/**
 * A hold taken for reading: others may read beside it, none may write.
 */
#define SUBETHA_LOCK_READ 0

/**
 * A hold taken for writing: nobody else may read or write.
 */
#define SUBETHA_LOCK_WRITE 1

/**
 * The bytes one version's value takes.
 */
#define SUBETHA_VERSIONED_CHAIN_ITEM_BYTES 48

/**
 * A key size for identifiers and small integers.
 */
#define SUBETHA_VERSIONED_MAP_KEY_BYTES_SMALL 8

/**
 * The key size `subetha_versioned_map_create_default` uses.
 */
#define SUBETHA_VERSIONED_MAP_KEY_BYTES_DEFAULT 16

/**
 * A key size for compound and textual keys.
 */
#define SUBETHA_VERSIONED_MAP_KEY_BYTES_LARGE 32

/**
 * The value size that pairs with the small key.
 */
#define SUBETHA_VERSIONED_MAP_VALUE_BYTES_SMALL 16

/**
 * The value size `subetha_versioned_map_create_default` uses.
 */
#define SUBETHA_VERSIONED_MAP_VALUE_BYTES_DEFAULT 64

/**
 * The value size that pairs with the large key.
 */
#define SUBETHA_VERSIONED_MAP_VALUE_BYTES_LARGE 256

/**
 * A value size for keys, counters and small records.
 */
#define SUBETHA_VERSIONED_VALUE_BYTES_SMALL 16

/**
 * The value size `subetha_versioned_slab_create_default` uses.
 */
#define SUBETHA_VERSIONED_VALUE_BYTES_DEFAULT 64

/**
 * A value size for records that carry a payload of their own.
 */
#define SUBETHA_VERSIONED_VALUE_BYTES_LARGE 256

/**
 * The depth that pairs with the small value size.
 */
#define SUBETHA_VERSIONED_DEPTH_SMALL 2

/**
 * The depth `subetha_versioned_slab_create_default` uses.
 */
#define SUBETHA_VERSIONED_DEPTH_DEFAULT 4

/**
 * The depth that pairs with the large value size.
 */
#define SUBETHA_VERSIONED_DEPTH_LARGE 8

/**
 * A handle: zero is never issued.
 */
typedef uint64_t subetha_handle;

/**
 * A snapshot of a shared string arena.
 */
typedef struct subetha_arena_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Whether this handle may intern and clear; false for one opened
     * read-only.
     */
    bool writable;
    /**
     * Bytes of room past the header.
     */
    uint64_t capacity_bytes;
    /**
     * Bytes interned so far.
     */
    uint64_t used_bytes;
    /**
     * `capacity_bytes - used_bytes`.
     */
    uint64_t remaining_bytes;
} subetha_arena_stats;

/**
 * What an unlink found.
 */
typedef struct subetha_unlink_report {
    /**
     * Files removed.
     */
    uint64_t removed;
    /**
     * Files the prefix names that were not present.
     */
    uint64_t missing;
    /**
     * Files whose removal the OS refused; the first is named in the detail.
     */
    uint64_t failed;
} subetha_unlink_report;

/**
 * A snapshot of a shared atomic.
 */
typedef struct subetha_atomic_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of the `SUBETHA_ATOMIC_` constants.
     */
    uint32_t width;
    /**
     * Bytes the value occupies: 4, 8, or 1 for the boolean.
     */
    uint32_t bytes;
} subetha_atomic_stats;

/**
 * A snapshot of a blocking bridge.
 */
typedef struct subetha_blocking_tcp_bridge_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of `SUBETHA_BRIDGE_CLIENT` or `SUBETHA_BRIDGE_SERVER`.
     */
    uint32_t role;
    /**
     * Items the last finished run carried. A run that ended in an error
     * reports zero, since the primitive counts no partial transfer.
     */
    uint64_t items;
    /**
     * Whether a run is under way right now.
     */
    bool running;
    /**
     * Whether the last run finished.
     */
    bool finished;
    /**
     * The code the last run ended with, or `SUBETHA_OK`.
     */
    int32_t last_code;
} subetha_blocking_tcp_bridge_stats;

/**
 * A declared contract for a ring: peer ceilings, an ordering envelope,
 * a capacity ceiling. A zero everywhere is the unbounded contract, which
 * is what a ring has when none is declared: registration then grows the
 * ring and never fails.
 */
typedef struct subetha_ring_contract {
    /**
     * Producers that may be registered at once; zero is unbounded.
     */
    uint8_t max_producers;
    /**
     * Consumers that may be registered at once; zero is unbounded.
     */
    uint8_t max_consumers;
    /**
     * One of the `SUBETHA_CONTRACT_` constants.
     */
    uint32_t ordering;
    /**
     * The `k` of `SUBETHA_CONTRACT_K_OUT_OF_ORDER`; ignored otherwise.
     */
    uint32_t k_out_of_order;
    /**
     * A ceiling on the capacity; zero is none.
     */
    uint32_t capacity_bound;
} subetha_ring_contract;

/**
 * Options every ring constructor takes. A C initializer that names the
 * first three fields and leaves the rest zero asks for no stamps, no
 * contract, the lazily created frame region and no security descriptor.
 */
typedef struct subetha_ring_options {
    /**
     * `SUBETHA_MODE_STRICT`, `SUBETHA_MODE_MANAGED`, or `SUBETHA_MODE_DEFAULT`.
     */
    uint32_t mode;
    /**
     * Waiter slots in each of the ring's two wakers; zero takes the
     * library's default of 32.
     */
    uint32_t max_waiters;
    /**
     * Managed mode only: how often the shape sidecar scans, in
     * microseconds. Zero takes `SUBETHA_SCAN_INTERVAL_DEFAULT_US`.
     * Ignored in strict mode.
     *
     * This value is the added round-trip latency of managed mode: a
     * request and its response each wait for a scan, so a caller doing
     * request/response pays one interval per round trip. Streaming
     * callers pay nothing. Measured on Linux and FreeBSD across 250 to
     * 10000 microseconds, the extra time per round trip equals the
     * interval at every point.
     */
    uint64_t scan_interval_us;
    /**
     * One of the `SUBETHA_STAMPS_` constants. A stamped ring carries an
     * ordering region beside its backings, pays 8 bytes of every slot
     * for the stamp, never takes the Vyukov shape, and refuses frames.
     */
    uint32_t stamps;
    /**
     * The ring's declared contract; all zero declares none.
     */
    struct subetha_ring_contract contract;
    /**
     * Bytes per block of the adaptive ring's frame payload region, which
     * is created at construction when this and `frame_blocks` are both
     * non-zero; a frame up to this size then travels past the slot. Zero
     * leaves the region to be created at
     * `SUBETHA_RING_FRAME_DEFAULT_BLOCK` bytes on the first spilled
     * frame. Set with `frame_blocks` or not at all.
     */
    uint64_t frame_block;
    /**
     * Blocks in the frame payload region; see `frame_block`.
     */
    uint32_t frame_blocks;
    /**
     * Holder slots in the ring's holder table, which is the number of
     * handles that may hold the backings open at once. Zero takes no
     * hold at all, which is the behavior of a ring that names no
     * holder table: the backings outlive every handle. A file-backed
     * ring only; see `last_holder`.
     */
    uint32_t max_holders;
    /**
     * `SUBETHA_LAST_HOLDER_KEEP` or `SUBETHA_LAST_HOLDER_UNLINK`: what
     * becomes of the backings when the last holder releases. Unlinking
     * needs `max_holders`, and the library names no default for it, so
     * asking to unlink with `max_holders` zero is refused rather than
     * given a number nobody chose. Only a file-backed ring has files to
     * remove; anonymous and shared-memory rings refuse a hold.
     */
    uint32_t last_holder;
    /**
     * The security descriptor, as an SDDL string, for every shared-memory
     * region this object creates; null applies the platform's default,
     * which on Windows admits the creating account and administrators.
     * A region that must be mapped from another Windows session needs
     * one. Ignored where the object is not in shared memory and on
     * platforms without descriptors.
     */
    const char *shm_sddl;
} subetha_ring_options;

/**
 * A snapshot of a broadcast ring.
 */
typedef struct subetha_broadcast_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Consumers currently registered.
     */
    uint32_t active_consumers;
    /**
     * Slots in the ring.
     */
    uint64_t capacity;
    /**
     * Items pushed since creation.
     */
    uint64_t producer_position;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
    /**
     * Whether every registered consumer has read everything pushed.
     */
    bool fully_drained;
} subetha_broadcast_stats;

/**
 * A snapshot of a shared blocked Bloom filter.
 */
typedef struct subetha_blocked_bloom_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Hash functions each item is put through, all within one block.
     */
    uint32_t n_hashes;
    /**
     * Blocks of 512 bits, one cache line each.
     */
    uint64_t n_blocks;
} subetha_blocked_bloom_stats;

/**
 * A snapshot of a shared Bloom filter.
 */
typedef struct subetha_bloom_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Hash functions each item is put through.
     */
    uint32_t n_hashes;
    /**
     * Bits in the filter.
     */
    uint64_t n_bits;
    /**
     * Items the set bits imply, which is an estimate: the filter stores
     * no count, and re-inserting a member adds nothing to it.
     */
    uint64_t estimated_items;
    /**
     * The false-positive rate the bits now set imply, from zero to one.
     */
    double false_positive_rate;
} subetha_bloom_stats;

/**
 * A snapshot of a shared B-tree map.
 */
typedef struct subetha_btree_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Nodes the map can hold.
     */
    uint64_t capacity;
    /**
     * Entries in the map.
     */
    uint64_t len;
    /**
     * Nodes handed out, the free list included.
     */
    uint64_t node_count;
    /**
     * Bytes per key.
     */
    uint64_t key_size;
    /**
     * Bytes per value.
     */
    uint64_t value_size;
    /**
     * The layout tag the map was created with.
     */
    uint64_t tag;
    /**
     * Bytes from one node to the next in the region.
     */
    uint64_t node_stride;
} subetha_btree_stats;

/**
 * A snapshot of a shared cell.
 */
typedef struct subetha_cell_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Bytes the value takes.
     */
    uint64_t value_size;
    /**
     * Twice the writes the cell has taken, and odd while one is in
     * flight.
     */
    uint64_t version;
} subetha_cell_stats;

/**
 * A snapshot of a shared count-min sketch.
 */
typedef struct subetha_cms_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Rows: one hash function each, and the estimate is their minimum.
     */
    uint32_t d;
    /**
     * Counters per row.
     */
    uint32_t w;
    /**
     * Everything counted in, summed over every insert.
     */
    uint64_t total_inserts;
} subetha_cms_stats;

/**
 * A snapshot of a condition variable.
 */
typedef struct subetha_condvar_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Waiter slots the backing was built with.
     */
    uint32_t max_waiters;
    /**
     * The generation, which every notify advances by one.
     */
    uint64_t generation;
    /**
     * Waits through this handle that gave up when their timeout
     * elapsed.
     */
    uint64_t timeouts;
    /**
     * Waits through this handle refused because every waiter slot was
     * parked.
     */
    uint64_t waker_full;
    /**
     * Waiters woken by notifies through this handle.
     */
    uint64_t woken;
} subetha_condvar_stats;

/**
 * A snapshot of an epoch table.
 */
typedef struct subetha_epochs_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Pins the table holds, which is also the ticket count.
     */
    uint64_t capacity;
    /**
     * The published epoch: what a pin takes right now.
     */
    uint64_t now;
    /**
     * Epochs at or below this have no reader.
     */
    uint64_t reclaim_horizon;
    /**
     * Pins outstanding across every process.
     */
    uint64_t live_pins;
    /**
     * Tickets open across every process.
     */
    uint64_t open_tickets;
    /**
     * Bytes the table takes on disk.
     */
    uint64_t file_size;
} subetha_epochs_stats;

/**
 * A snapshot of a frame region.
 */
typedef struct subetha_frame_region_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Bytes one block holds.
     */
    uint64_t block_size;
    /**
     * Blocks the region carries.
     */
    uint64_t block_count;
    /**
     * Bytes the whole region takes on disk.
     */
    uint64_t file_size;
} subetha_frame_region_stats;

/**
 * A snapshot of a capacity-adaptive ring.
 */
typedef struct subetha_capacity_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * The active backing's shape, one of the `SUBETHA_RING_SHAPE_` constants.
     */
    uint32_t shape;
    /**
     * The live ordering mode, one of the `SUBETHA_ORDERING_` constants.
     */
    uint32_t ordering_mode;
    /**
     * Capacity of the backing the producers write to.
     */
    uint64_t current_capacity;
    /**
     * Bumped by every morph; a pin taken at an older value is stale.
     */
    uint64_t pin_generation;
    /**
     * Items in the active backing, read without a lock.
     */
    uint64_t approx_len;
    /**
     * Capacity of the backing held in the warm cache; zero when none.
     */
    uint64_t warm_capacity;
    /**
     * Morphs that consumed a warm backing instead of building one.
     */
    uint64_t warm_hits;
    /**
     * Items the consumer took from stale backings since creation.
     */
    uint64_t stale_pops;
    /**
     * Cross-producer inversions observed; zero on an unstamped ring.
     */
    uint64_t inversions;
    /**
     * Morphs the managed-mode sidecar issued; zero in strict mode.
     */
    uint64_t sidecar_morphs;
    /**
     * Prewarms the managed-mode sidecar issued; zero in strict mode.
     */
    uint64_t sidecar_prewarms;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
    /**
     * Whether the backings carry ordering stamps.
     */
    bool stamped;
} subetha_capacity_stats;

/**
 * A snapshot of a capacity-adaptive broadcast ring.
 */
typedef struct subetha_capacity_broadcast_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Consumers registered on the active backing.
     */
    uint32_t active_consumers;
    /**
     * Capacity of the backing the producer writes to.
     */
    uint64_t current_capacity;
    /**
     * Bumped by every morph.
     */
    uint64_t pin_generation;
    /**
     * Items pushed into the active backing since it was built.
     */
    uint64_t producer_position;
    /**
     * Capacity of the backing held in the warm cache; zero when none.
     */
    uint64_t warm_capacity;
    /**
     * Morphs that consumed a warm backing instead of building one.
     */
    uint64_t warm_hits;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_capacity_broadcast_stats;

/**
 * A snapshot of a capacity-adaptive pub/sub ring.
 */
typedef struct subetha_capacity_pubsub_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Backings in the chain, the active one included.
     */
    uint32_t chain_len;
    /**
     * Capacity of the backing the publisher writes to.
     */
    uint64_t current_capacity;
    /**
     * Bumped by every morph.
     */
    uint64_t pin_generation;
    /**
     * Slots across every backing in the chain.
     */
    uint64_t chain_total_capacity;
    /**
     * Capacity of the backing held in the warm cache; zero when none.
     */
    uint64_t warm_capacity;
    /**
     * Morphs that consumed a warm backing instead of building one.
     */
    uint64_t warm_hits;
} subetha_capacity_pubsub_stats;

/**
 * A snapshot of a Lamport pair's producer side.
 */
typedef struct subetha_lamport_producer_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in the ring.
     */
    uint64_t capacity;
    /**
     * Items pushed so far.
     */
    uint64_t head;
} subetha_lamport_producer_stats;

/**
 * A snapshot of a Lamport pair's consumer side.
 */
typedef struct subetha_lamport_consumer_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in the ring.
     */
    uint64_t capacity;
    /**
     * Items popped so far.
     */
    uint64_t tail;
} subetha_lamport_consumer_stats;

/**
 * A snapshot of a shared laned versioned map.
 */
typedef struct subetha_laned_map_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Lanes the map holds.
     */
    uint32_t lanes;
    /**
     * Lanes a statement is holding right now.
     */
    uint32_t held_lanes;
    /**
     * Entries across every lane, tombstones not yet reclaimed included.
     */
    uint64_t len;
    /**
     * Bytes a key takes.
     */
    uint64_t key_size;
    /**
     * Bytes a value takes.
     */
    uint64_t value_size;
    /**
     * The epoch the shared table stands at now.
     */
    uint64_t epoch;
} subetha_laned_map_stats;

/**
 * A snapshot of an election.
 */
typedef struct subetha_leader_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * The process id leading right now, or `SUBETHA_LEADER_NONE`.
     */
    uint32_t leader_pid;
    /**
     * How many times leadership has changed hands. A follower watching
     * for a change polls this rather than the process id, which can
     * come back to a value it held before.
     */
    uint32_t election_term;
    /**
     * The epoch the leader last beat at.
     */
    uint64_t leader_heartbeat;
    /**
     * The epoch the grace window is measured against.
     */
    uint64_t global_epoch;
} subetha_leader_stats;

/**
 * The element layout a stack or deque is created with and every attach
 * states. `element_size` bytes are stored per element; `alignment` is
 * the element's alignment, a power of two; `tag` is a value the caller
 * chooses to name the element's type, and an attach whose tag differs
 * from the creator's is refused with `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 */
typedef struct subetha_element_layout {
    /**
     * Bytes per element; a push copies at most this many.
     */
    uint64_t element_size;
    /**
     * The element's alignment in bytes, a power of two.
     */
    uint64_t alignment;
    /**
     * A caller-chosen value naming the element's type.
     */
    uint64_t tag;
} subetha_element_layout;

/**
 * A snapshot of a shared linked list.
 */
typedef struct subetha_list_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Nodes the list can hold, the sentinel head among them.
     */
    uint64_t capacity;
    /**
     * Nodes in the list, the sentinel head excluded.
     */
    uint64_t len;
    /**
     * Bytes per value.
     */
    uint64_t element_size;
    /**
     * The value alignment the layout declares.
     */
    uint64_t alignment;
    /**
     * The layout tag the list was created with.
     */
    uint64_t tag;
    /**
     * Bytes one node takes, its two link words included.
     */
    uint64_t node_size;
} subetha_list_stats;

/**
 * A snapshot of a locale-adaptive ring.
 */
typedef struct subetha_locale_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * The live locale, one of the `SUBETHA_LOCALE_` constants.
     */
    uint32_t current_locale;
    /**
     * The live ordering mode, one of the `SUBETHA_ORDERING_` constants.
     */
    uint32_t ordering_mode;
    /**
     * Bumped by every migration; shared across processes.
     */
    uint64_t locale_generation;
    /**
     * Items in the live backing, read without a lock.
     */
    uint64_t approx_len;
    /**
     * Cross-producer inversions across the three backings; zero unstamped.
     */
    uint64_t inversions;
    /**
     * Migrations the managed-mode sidecar issued; zero in strict mode.
     */
    uint64_t sidecar_migrations;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
    /**
     * Whether the backings carry ordering stamps.
     */
    bool stamped;
} subetha_locale_stats;

/**
 * A snapshot of one producer of a blocking MPMC grid.
 */
typedef struct subetha_mpmc_producer_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in this producer's ring.
     */
    uint64_t capacity;
    /**
     * Items this producer pushed so far.
     */
    uint64_t head;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_mpmc_producer_stats;

/**
 * A snapshot of one consumer of a blocking MPMC grid.
 */
typedef struct subetha_mpmc_consumer_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Producer rings in this consumer's subset.
     */
    uint32_t n_rings;
    /**
     * Items pending across the subset, read without a lock.
     */
    uint64_t approx_subset_len;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_mpmc_consumer_stats;

/**
 * A snapshot of a shared bit vector.
 */
typedef struct subetha_bit_vec_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Bits the vector holds.
     */
    uint64_t capacity_bits;
    /**
     * Machine words those bits occupy.
     */
    uint64_t capacity_words;
    /**
     * Bits set when this was read. Another process writing during the
     * count moves it; the number is a real count of a moment that has
     * passed rather than a held snapshot.
     */
    uint64_t ones;
    /**
     * Bits clear on the same reading, by the same caution.
     */
    uint64_t zeros;
} subetha_bit_vec_stats;

/**
 * A snapshot of a shared HyperLogLog.
 */
typedef struct subetha_hll_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * The precision it was built with.
     */
    uint32_t precision;
    /**
     * Registers that precision implies, which is two to its power.
     */
    uint32_t n_registers;
    /**
     * Distinct items the registers imply. An estimate, and one that
     * another process inserting during the read can move.
     */
    uint64_t estimated_distinct;
} subetha_hll_stats;

/**
 * A snapshot of a shared graph.
 */
typedef struct subetha_graph_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Nodes it holds now.
     */
    uint64_t node_count;
    /**
     * Edges it holds now.
     */
    uint64_t edge_count;
    /**
     * Nodes it can hold.
     */
    uint64_t max_nodes;
    /**
     * Edges it can hold.
     */
    uint64_t max_edges;
    /**
     * Bytes in a node's value.
     */
    uint64_t node_value_size;
    /**
     * Bytes in an edge's value.
     */
    uint64_t edge_value_size;
} subetha_graph_stats;

/**
 * A snapshot of a shared LRU cache.
 */
typedef struct subetha_lru_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Entries the cache can hold before it evicts.
     */
    uint32_t capacity;
    /**
     * Entries it holds now.
     */
    uint64_t len;
    /**
     * Bytes in a key.
     */
    uint64_t key_size;
    /**
     * Bytes in a value.
     */
    uint64_t value_size;
} subetha_lru_stats;

/**
 * A snapshot of a shared tile.
 */
typedef struct subetha_tile_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Lanes the tile has, which is always `SUBETHA_TILE_LANES`.
     */
    uint32_t capacity;
    /**
     * Lanes occupied now, whether or not any snapshot can see them.
     */
    uint32_t len;
    /**
     * Bytes in a lane's payload.
     */
    uint64_t payload_size;
} subetha_tile_stats;

/**
 * A snapshot of a shared strategy-switching set.
 */
typedef struct subetha_universal_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of `SUBETHA_UNIVERSAL_VEC`, `SUBETHA_UNIVERSAL_MAP`.
     */
    uint32_t strategy;
    /**
     * Migrations since creation, rolling into `generation` at overflow.
     */
    uint32_t version;
    /**
     * Carries the version's overflow, so the pair is unique.
     */
    uint32_t generation;
    /**
     * The word a reader compares across an operation. Equal before and
     * after means no migration interleaved.
     */
    uint64_t stamp;
    /**
     * Elements held now.
     */
    uint64_t len;
    /**
     * Inserts attempted since this handle attached.
     */
    uint64_t inserts;
    /**
     * Membership tests since this handle attached.
     */
    uint64_t contains;
    /**
     * Bytes in an element.
     */
    uint64_t element_size;
} subetha_universal_stats;

/**
 * A snapshot of a shared tower.
 */
typedef struct subetha_tower_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Levels including the leaf. A path has this many entries.
     */
    uint32_t depth;
    /**
     * Values stored at the leaf.
     */
    uint64_t len;
    /**
     * Bytes in a leaf value.
     */
    uint64_t value_size;
} subetha_tower_stats;

/**
 * A policy read in one call.
 */
typedef struct subetha_qos {
    /**
     * One of `SUBETHA_QOS_VOLATILE`, `_TRANSIENT`, `_PERSISTENT`.
     */
    uint32_t durability;
    /**
     * One of `SUBETHA_QOS_BEST_EFFORT`, `_RELIABLE`.
     */
    uint32_t reliability;
    /**
     * One of `SUBETHA_QOS_KEEP_LAST`, `_KEEP_ALL`.
     */
    uint32_t history_kind;
    /**
     * How many items to keep, when `history_kind` is
     * `SUBETHA_QOS_KEEP_LAST`. Zero and meaningless otherwise.
     */
    uint32_t history_depth;
    /**
     * The latency budget in nanoseconds.
     */
    uint64_t max_latency_nanos;
    /**
     * One of `SUBETHA_QOS_PER_PRODUCER`, `_GLOBAL_FIFO`.
     */
    uint32_t ordering;
} subetha_qos;

/**
 * What one id resolves to right now.
 */
typedef struct subetha_endpoint_target {
    /**
     * One of the `SUBETHA_ENDPOINT_` constants.
     */
    uint32_t kind;
    /**
     * The registry generation this was read at. Pass it to
     * `subetha_endpoint_still_valid` before acting on what was read: a
     * rebind between the two calls makes it stale, and nothing else
     * reveals that.
     */
    uint64_t generation;
} subetha_endpoint_target;

/**
 * A snapshot of one producer of a blocking MPSC pool.
 */
typedef struct subetha_mpsc_producer_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in this producer's ring.
     */
    uint64_t capacity;
    /**
     * Items this producer pushed so far.
     */
    uint64_t head;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_mpsc_producer_stats;

/**
 * A snapshot of the consumer of a blocking MPSC pool.
 */
typedef struct subetha_mpsc_consumer_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Producer rings this consumer drains.
     */
    uint32_t n_producers;
    /**
     * Items pending across every ring, read without a lock.
     */
    uint64_t approx_total_len;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_mpsc_consumer_stats;

/**
 * A snapshot of a work-stealing deque.
 */
typedef struct subetha_deque_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * `SUBETHA_DEQUE_OWNER` or `SUBETHA_DEQUE_THIEF`.
     */
    uint32_t role;
    /**
     * Slots in the deque, a power of two.
     */
    uint64_t capacity;
    /**
     * Bytes per slot: the element size rounded up to eight.
     */
    uint64_t slot_bytes;
    /**
     * Bytes per element.
     */
    uint64_t element_size;
    /**
     * The element alignment the layout declares.
     */
    uint64_t alignment;
    /**
     * The layout tag the deque was created with.
     */
    uint64_t tag;
    /**
     * Elements taken from the top so far, by steals and by the owner's
     * pop of the last element.
     */
    int64_t top;
    /**
     * Elements the owner has pushed, less the ones it popped.
     */
    int64_t bottom;
    /**
     * `bottom - top`, racing concurrent pushes and steals.
     */
    uint64_t approx_len;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_deque_stats;

/**
 * A snapshot of a shared hash map.
 */
typedef struct subetha_hashmap_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in the table.
     */
    uint64_t capacity;
    /**
     * Entries present.
     */
    uint64_t len;
    /**
     * Bytes per key.
     */
    uint64_t key_size;
    /**
     * Bytes per value.
     */
    uint64_t value_size;
    /**
     * Slots holding a removed entry, reclaimed by an insert or a compact.
     */
    uint64_t tombstones;
    /**
     * `len / capacity`.
     */
    double load_factor;
} subetha_hashmap_stats;

/**
 * One slot's state, read under its version.
 */
typedef struct subetha_heartbeat_slot {
    /**
     * The process holding the slot, or `SUBETHA_HEARTBEAT_EMPTY_PID`.
     */
    uint32_t pid;
    /**
     * The role the holder set; the meaning is the caller's.
     */
    uint32_t role;
    /**
     * The epoch the holder last beat at.
     */
    uint64_t last_seen_epoch;
    /**
     * The work units the holder has taken, one per bit.
     */
    uint64_t in_flight_bitmap;
} subetha_heartbeat_slot;

/**
 * A snapshot of a heartbeat table.
 */
typedef struct subetha_heartbeat_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the table holds.
     */
    uint32_t capacity;
    /**
     * Slots a process holds right now.
     */
    uint32_t registered;
    /**
     * The epoch a grace window is measured against.
     */
    uint64_t global_epoch;
    /**
     * Bytes the table takes on disk.
     */
    uint64_t file_size;
} subetha_heartbeat_stats;

/**
 * A snapshot of a shared histogram.
 */
typedef struct subetha_histogram_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Buckets, which is one more than the boundaries the caller gave.
     */
    uint64_t n_buckets;
    /**
     * Values recorded.
     */
    uint64_t total_count;
} subetha_histogram_stats;

/**
 * A snapshot of a shared rate limiter.
 */
typedef struct subetha_rate_limiter_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Tokens the bucket holds when full, which is the largest burst.
     */
    uint32_t capacity;
    /**
     * Tokens added per second, toward the capacity.
     */
    uint32_t refill_rate_per_sec;
    /**
     * Tokens available right now.
     */
    uint32_t available;
} subetha_rate_limiter_stats;

/**
 * A snapshot of a barrier.
 */
typedef struct subetha_epoch_barrier_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * The epoch the barrier stands at.
     */
    uint32_t epoch;
    /**
     * Peers that have arrived at that epoch and not yet been released.
     */
    uint32_t arrived;
    /**
     * Peers the heartbeat table shows as live, which is how many an
     * unqualified wait waits for.
     */
    uint32_t live_peers;
    /**
     * Epochs a heartbeat slot may be stale before it stops counting.
     */
    uint64_t grace_epochs;
} subetha_epoch_barrier_stats;

/**
 * A hybrid logical clock: a microsecond reading and a counter that
 * breaks ties within it. Two of them compare lexicographically, the
 * physical part first, which is the order every participant agrees on.
 */
typedef struct subetha_hlc {
    /**
     * Microseconds, from the clock the participants share.
     */
    uint64_t physical_us;
    /**
     * Ticks within one microsecond, so two events in the same
     * microsecond still order.
     */
    uint64_t logical;
} subetha_hlc;

/**
 * What one participant's slot holds.
 */
typedef struct subetha_fence_clock_slot {
    /**
     * The process holding the slot; a caller reclaims a slot whose
     * process is gone by unregistering it.
     */
    uint32_t pid;
    /**
     * That participant's clock as it last published it.
     */
    struct subetha_hlc hlc;
    /**
     * When it last did, on the shared microsecond clock.
     */
    uint64_t last_updated_us;
} subetha_fence_clock_slot;

/**
 * A snapshot of a fence clock.
 */
typedef struct subetha_fence_clock_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the clock holds.
     */
    uint64_t capacity;
    /**
     * The shared microsecond reading right now.
     */
    uint64_t shared_clock_us;
    /**
     * The global fence as last published.
     */
    struct subetha_hlc fence;
    /**
     * How many times a fence has been published.
     */
    uint64_t fence_epoch;
} subetha_fence_clock_stats;

/**
 * A snapshot of a lazy value.
 */
typedef struct subetha_lazy_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of the `SUBETHA_LAZY_` state constants.
     */
    uint32_t state;
    /**
     * Bytes the value holds.
     */
    uint64_t value_bytes;
} subetha_lazy_stats;

/**
 * A snapshot of a shared value.
 */
typedef struct subetha_shared_arc_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * What becomes of the backing when the last holder releases.
     */
    uint32_t on_last;
    /**
     * Bytes the shared region holds.
     */
    uint64_t value_bytes;
    /**
     * Holder slots the backing carries.
     */
    uint64_t capacity;
    /**
     * Slots held right now, this process included. A holder whose
     * process has died still counts until something reaps it.
     */
    uint64_t strong_count;
} subetha_shared_arc_stats;

/**
 * A snapshot of a QUIC bridge.
 */
typedef struct subetha_quic_bridge_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of `SUBETHA_BRIDGE_CLIENT` or `SUBETHA_BRIDGE_SERVER`.
     */
    uint32_t role;
    /**
     * Items shipped or received so far.
     */
    uint64_t items;
    /**
     * Whether a run is under way right now.
     */
    bool running;
    /**
     * Whether the last run finished.
     */
    bool finished;
    /**
     * The code the last run ended with, or `SUBETHA_OK`.
     */
    int32_t last_code;
} subetha_quic_bridge_stats;

/**
 * A snapshot of one half of a Sens-O-Matic link.
 */
typedef struct subetha_sens_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of `SUBETHA_SENS_SENDER` or `SUBETHA_SENS_RECEIVER`.
     */
    uint32_t role;
    /**
     * The code carrying the stream now: `SUBETHA_SENS_CODE_RLC` or
     * `SUBETHA_SENS_CODE_RS`.
     */
    uint32_t code;
    /**
     * Times the code has changed since the link came up.
     */
    uint64_t switches;
    /**
     * Items sent, or items delivered on a receiver.
     */
    uint64_t items;
    /**
     * The sender's forward-loss estimate, from zero to one. Zero on a
     * receiver, which measures loss for the sender rather than itself.
     */
    double loss;
    /**
     * Datagrams this half put on the wire that the far end never reported
     * receiving, whatever became of them: a slow reader there, a full
     * buffer, a lossy link. Read `missed_report` before this number.
     *
     * Only a sender can compute it, since only a sender learns both
     * counts. It advances a feedback window at a time and skips windows
     * too small to trust, so it lags the wire.
     */
    uint64_t missed;
    /**
     * Whether `missed` is a real number: `SUBETHA_DROPS_EXACT` on a
     * unified sender, `SUBETHA_DROPS_UNKNOWN` on every receiver and on the
     * standalone codes, which carry no count of what the far end saw.
     */
    uint32_t missed_report;
    /**
     * Datagrams this half's kernel dropped because its receive buffer was
     * full, which is what a caller pumping too slowly loses. Read
     * `kernel_dropped_report` before this number.
     */
    uint64_t kernel_dropped;
    /**
     * What this host will say about `kernel_dropped`. Hosts differ: one
     * gives a per-socket count, one says only that it happened, one says
     * nothing. A zero count paired with `SUBETHA_DROPS_OCCURRED` or
     * `SUBETHA_DROPS_UNKNOWN` is not a claim that none were lost.
     */
    uint32_t kernel_dropped_report;
    /**
     * Datagrams a unified receiver read and no decoder claimed: traffic
     * that reached the process and was discarded before any decoder saw
     * it. Zero on every other half.
     */
    uint64_t unroutable;
    /**
     * Data datagrams a sealed receiver dropped because their peer had not
     * completed its handshake. The peer's own code and retransmits carry
     * the items again once it has; a count that keeps climbing is a peer
     * sending data with no handshake at all. Zero without the sealed path.
     */
    uint64_t preauth_dropped;
    /**
     * Items a sealed receiver took from a decoder and could not open: no
     * completed handshake bound to the item's tag, or a failed seal. Zero
     * without the sealed path.
     */
    uint64_t unopened;
    /**
     * Handshakes a sealed receiver dropped: a flight its handshake state
     * rejected, or a peer that went silent before finishing. Zero without
     * the sealed path.
     */
    uint64_t handshake_failures;
} subetha_sens_stats;

/**
 * A snapshot of a bridge.
 */
typedef struct subetha_tcp_bridge_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of `SUBETHA_BRIDGE_CLIENT` or `SUBETHA_BRIDGE_SERVER`.
     */
    uint32_t role;
    /**
     * Items shipped or received so far.
     */
    uint64_t items;
    /**
     * Whether a run is under way right now.
     */
    bool running;
    /**
     * Whether the last run finished.
     */
    bool finished;
    /**
     * The code the last run ended with, or `SUBETHA_OK`.
     */
    int32_t last_code;
} subetha_tcp_bridge_stats;

/**
 * A snapshot of a shared topology map.
 */
typedef struct subetha_topology_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Nodes the matrix covers.
     */
    uint32_t n_nodes;
    /**
     * Messages recorded across every edge.
     */
    uint64_t total_msgs;
    /**
     * The largest number of distinct destinations any one node sent to.
     */
    uint32_t max_fan_out;
    /**
     * The node that sent to that many.
     */
    uint32_t max_fan_out_src;
    /**
     * The largest number of distinct sources any one node received from.
     */
    uint32_t max_fan_in;
    /**
     * The node that received from that many.
     */
    uint32_t max_fan_in_dst;
    /**
     * The shape the traffic reads as: one of the `SUBETHA_TOPOLOGY_`
     * constants.
     */
    uint32_t recommendation;
    /**
     * Bumped whenever the recommendation changes, so a caller can tell a
     * re-read from a re-decision.
     */
    uint64_t recommendation_epoch;
} subetha_topology_stats;

/**
 * A snapshot of a waker.
 */
typedef struct subetha_waker_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the waker holds.
     */
    uint64_t capacity;
    /**
     * Parks this handle has taken and not given back.
     */
    uint64_t parked;
} subetha_waker_stats;

/**
 * A snapshot of a holder table.
 */
typedef struct subetha_holders_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the table holds.
     */
    uint32_t capacity;
    /**
     * Slots held right now, across every process. A slot that is
     * reserved but not published does not count.
     */
    uint32_t live;
    /**
     * Bytes the table takes on disk.
     */
    uint64_t file_size;
} subetha_holders_stats;

/**
 * A snapshot of an ordered receiver.
 */
typedef struct subetha_ordered_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * One of the `SUBETHA_ORDERED_` constants.
     */
    uint32_t strategy;
    /**
     * The re-ordering window, in items; zero outside the reorder strategy.
     */
    uint32_t window;
    /**
     * Items held in the window right now.
     */
    uint32_t buffered;
    /**
     * Times the window had to grow past the producer count.
     */
    uint64_t corrections;
    /**
     * Ordering-mode flips the ring refused while this receiver ran.
     */
    uint64_t mode_refusals;
} subetha_ordered_stats;

/**
 * A snapshot of an owner lease.
 */
typedef struct subetha_owner_lease_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Bytes the payload holds, as the region records it.
     */
    uint32_t payload_size;
    /**
     * The process id holding the lease, or `SUBETHA_LEASE_NO_OWNER`.
     */
    uint32_t owner_pid;
    /**
     * How many times the lease has changed hands.
     */
    uint32_t lease_term;
    /**
     * The payload's version, odd while a write is in flight.
     */
    uint32_t seq_version;
    /**
     * The epoch the owner last beat at.
     */
    uint64_t heartbeat_epoch;
    /**
     * The epoch the grace window is measured against.
     */
    uint64_t global_epoch;
} subetha_owner_lease_stats;

/**
 * A snapshot of a shared stack.
 */
typedef struct subetha_stack_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Elements the stack can hold.
     */
    uint64_t capacity;
    /**
     * Bytes per element.
     */
    uint64_t element_size;
    /**
     * The element alignment the layout declares.
     */
    uint64_t alignment;
    /**
     * The layout tag the stack was created with.
     */
    uint64_t tag;
    /**
     * Elements on the stack, counted by walking it; racing pushes and pops.
     */
    uint64_t approx_len;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_stack_stats;

/**
 * A snapshot of a pub/sub ring.
 */
typedef struct subetha_pubsub_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in the ring.
     */
    uint64_t capacity;
    /**
     * The position the next publish takes; items published so far.
     */
    uint64_t head;
} subetha_pubsub_stats;

/**
 * A snapshot of a shared region.
 */
typedef struct subetha_region_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the region holds.
     */
    uint64_t capacity;
    /**
     * Slots allocated right now; allocations and frees race this.
     */
    uint64_t len;
    /**
     * Slots on the free list, walked.
     */
    uint64_t free_count;
    /**
     * Bytes per element.
     */
    uint64_t element_size;
    /**
     * The element alignment the layout declares.
     */
    uint64_t alignment;
    /**
     * The layout tag the region was created with.
     */
    uint64_t tag;
    /**
     * Where the slot array starts in the file.
     */
    uint64_t slots_offset;
} subetha_region_stats;

/**
 * A snapshot of a shared reservoir sampler.
 */
typedef struct subetha_reservoir_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the reservoir holds.
     */
    uint64_t capacity;
    /**
     * Items offered to the sampler, which is the stream's length and not
     * the sample's size.
     */
    uint64_t total_seen;
    /**
     * Slots filled, which is the capacity once the stream passes it.
     */
    uint64_t filled;
} subetha_reservoir_stats;

/**
 * A snapshot of a ring's state.
 */
typedef struct subetha_ring_stats {
    /**
     * One of the `SUBETHA_RING_SHAPE_` constants.
     */
    uint32_t shape;
    /**
     * The mode this handle was created in, strict or managed.
     */
    uint32_t mode;
    /**
     * Producers currently registered.
     */
    uint32_t active_producers;
    /**
     * Consumers currently registered.
     */
    uint32_t active_consumers;
    /**
     * The producer count the ring was sized for; registration grows past it.
     */
    uint32_t max_producers;
    /**
     * The consumer count the ring was sized for.
     */
    uint32_t max_consumers;
    /**
     * Slots per backing.
     */
    uint64_t capacity;
    /**
     * Items in the current shape's backing, read without a lock.
     */
    uint64_t approx_len;
    /**
     * Shape morphs the ring refused because the previous shape still held
     * a backlog.
     */
    uint64_t morph_refusals;
    /**
     * Ordering-mode flips the ring refused.
     */
    uint64_t mode_refusals;
    /**
     * Waits that found every waiter slot in use and returned
     * `SUBETHA_E_RING_WAKER_FULL` instead of parking.
     */
    uint64_t waker_full;
    /**
     * Morphs the managed-mode sidecar issued; zero in strict mode.
     */
    uint64_t sidecar_morphs;
    /**
     * Cross-producer inversions observed at pop since the ordering
     * region was created; zero on an unstamped ring.
     */
    uint64_t inversions;
    /**
     * One of the `SUBETHA_STAMPS_` constants, never the default: the
     * source the stamps actually come from, or none.
     */
    uint32_t stamps;
    /**
     * The live ordering mode, one of the `SUBETHA_ORDERING_` constants;
     * unordered on an unstamped ring.
     */
    uint32_t ordering_mode;
} subetha_ring_stats;

/**
 * A snapshot of a lock.
 */
typedef struct subetha_rwlock_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Holds taken for reading right now, across every process.
     */
    uint32_t readers;
    /**
     * Writers waiting, across every process. A new reader is refused
     * while this is above zero.
     */
    uint32_t waiting_writers;
    /**
     * Whether a hold is taken for writing right now.
     */
    bool has_writer;
    /**
     * Acquires through this handle that gave up when their timeout
     * elapsed.
     */
    uint64_t timeouts;
} subetha_rwlock_stats;

/**
 * A snapshot of a semaphore.
 */
typedef struct subetha_semaphore_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * The ceiling the semaphore was created with.
     */
    uint32_t max_permits;
    /**
     * Permits free to take right now, across every process.
     */
    uint32_t available;
    /**
     * Callers waiting for a permit, across every process.
     */
    uint32_t waiters;
    /**
     * The generation a release advances, which waiters watch.
     */
    uint64_t wakeup_generation;
    /**
     * Releases refused because the count was already at `max_permits`:
     * permits given back by some path that never took them.
     */
    uint64_t release_overflows;
    /**
     * Acquires through this handle that gave up when their timeout
     * elapsed.
     */
    uint64_t timeouts;
} subetha_semaphore_stats;

/**
 * A snapshot of a shared slab.
 */
typedef struct subetha_slab_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Whether this handle may set; false for one opened read-only.
     */
    bool writable;
    /**
     * Slots the slab addresses.
     */
    uint64_t capacity;
    /**
     * Bytes per record.
     */
    uint64_t element_size;
    /**
     * The record alignment the layout declares.
     */
    uint64_t alignment;
    /**
     * The layout tag the slab was created with.
     */
    uint64_t tag;
    /**
     * Bytes from one slot to the next in the region.
     */
    uint64_t slot_stride;
} subetha_slab_stats;

/**
 * A snapshot of a blocking SPSC ring.
 */
typedef struct subetha_spsc_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in the ring.
     */
    uint64_t capacity;
    /**
     * Items pushed so far.
     */
    uint64_t head;
    /**
     * Items popped so far.
     */
    uint64_t tail;
    /**
     * Items in the ring, read without a lock.
     */
    uint64_t approx_len;
    /**
     * Items the predictive wait caught without a wake, since creation.
     */
    uint64_t predictive_catches;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
    /**
     * Whether predictive waiting is enabled on the consumer.
     */
    bool phase_locking;
} subetha_spsc_stats;

/**
 * A snapshot of a shared vec.
 */
typedef struct subetha_vec_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Whether this handle may push, pop, set and clear; false for one
     * opened read-only.
     */
    bool writable;
    /**
     * Elements the vec can hold.
     */
    uint64_t capacity;
    /**
     * Elements pushed and published.
     */
    uint64_t len;
    /**
     * Bytes per element.
     */
    uint64_t element_size;
    /**
     * The element alignment the layout declares.
     */
    uint64_t alignment;
    /**
     * The layout tag the vec was created with.
     */
    uint64_t tag;
    /**
     * Bytes from one slot to the next in the region.
     */
    uint64_t slot_stride;
} subetha_vec_stats;

/**
 * A snapshot of a shared versioned chain.
 */
typedef struct subetha_versioned_chain_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Nodes the chain can hold at once.
     */
    uint64_t capacity;
    /**
     * Versions in the chain now.
     */
    uint64_t len;
    /**
     * The head's version, or zero when the chain is empty.
     */
    uint64_t current_version;
} subetha_versioned_chain_stats;

/**
 * A snapshot of a shared versioned map.
 */
typedef struct subetha_versioned_map_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Nodes the tree addresses.
     */
    uint64_t capacity;
    /**
     * Entries present, tombstones not yet reclaimed included.
     */
    uint64_t len;
    /**
     * Bytes a key takes.
     */
    uint64_t key_size;
    /**
     * Bytes a value takes.
     */
    uint64_t value_size;
    /**
     * The epoch the table stands at now.
     */
    uint64_t epoch;
} subetha_versioned_map_stats;

/**
 * A snapshot of a shared versioned slab.
 */
typedef struct subetha_versioned_slab_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots the slab addresses.
     */
    uint64_t capacity;
    /**
     * Bytes one version's value takes.
     */
    uint64_t value_size;
    /**
     * Versions a slot keeps before a push has to drop one.
     */
    uint64_t depth;
    /**
     * The epoch the table stands at now.
     */
    uint64_t epoch;
} subetha_versioned_slab_stats;

/**
 * A snapshot of a Vyukov ring.
 */
typedef struct subetha_vyukov_stats {
    /**
     * The mode this handle was created in.
     */
    uint32_t mode;
    /**
     * Slots in the ring.
     */
    uint64_t capacity;
    /**
     * Slots claimed by producers so far.
     */
    uint64_t producer_seq;
    /**
     * Slots drained by consumers so far.
     */
    uint64_t consumer_seq;
    /**
     * Items waiting, read without a lock.
     */
    uint64_t approx_len;
    /**
     * Waits refused because every waiter slot was in use.
     */
    uint64_t waker_full;
} subetha_vyukov_stats;

/**
 * The handle no object has.
 */
#define SUBETHA_HANDLE_NONE 0

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

/**
 * Obtain the arena at `path` with `capacity_bytes` of room: an empty one
 * is initialized when the file does not exist, an existing one is
 * attached with its bytes in place, so references other processes hold
 * keep resolving. A file built with another capacity is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`. `capacity_bytes` is at most
 * `SUBETHA_ARENA_CAPACITY_MAX`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_arena_create(const char *path,
                             uint64_t capacity_bytes,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Attach to the arena another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_arena_open(const char *path,
                           uint64_t capacity_bytes,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Attach to the arena at `path` with read access alone, for a process
 * that may not write the file. `get` and `view` behave as on any handle;
 * `intern` and `clear` return `SUBETHA_E_READ_ONLY`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_arena_open_read_only(const char *path,
                                     uint64_t capacity_bytes,
                                     uint32_t mode,
                                     subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty arena there,
 * invalidating every reference other handles hold. On Windows the file
 * must not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_arena_reset(const char *path,
                            uint64_t capacity_bytes,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Append `len` bytes from `data` and write the reference naming them into
 * `out_ref`; an empty value is a reference of length zero at the current
 * end. `SUBETHA_E_ARENA_FULL` when the arena has no room for the whole
 * value, in which case nothing was written; `len` is at most
 * `SUBETHA_ARENA_VALUE_MAX`.
 *
 * # Safety
 * `data` points to `len` readable bytes, or is null with `len` zero;
 * `out_ref` is a valid pointer.
 */
int32_t subetha_arena_intern(subetha_handle handle,
                             const uint8_t *data,
                             size_t len,
                             uint64_t *out_ref);

/**
 * Intern `count` values of `len` bytes each from the caller's own array,
 * the one at `items + i * stride`, and write the reference naming each
 * into `out_refs`. One handle lookup and one panic guard for the run.
 *
 * Every value in the run is the same length, which is what lets one
 * stride address them all. A caller with values of differing lengths
 * interns them one at a time; the arena stores each at its own length
 * either way, so nothing is padded.
 *
 * The batch stops at the first refusal, `SUBETHA_E_ARENA_FULL` being the
 * one to expect, and reports how many landed, so `out_refs` holds
 * `out_done` of them.
 *
 * Despite the name, this appends rather than deduplicating: the same
 * bytes interned twice occupy the arena twice and answer with two
 * different references, both of which read back the same value. A caller
 * wanting one copy per distinct value keeps its own map from value to
 * reference; the arena does not keep one, which is what makes an intern
 * a bump of the cursor rather than a lookup.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_refs`
 * addresses `count` writable `uint64_t`s; `out_done` is a valid pointer.
 */
int32_t subetha_arena_intern_many(subetha_handle handle,
                                  const uint8_t *items,
                                  size_t stride,
                                  size_t len,
                                  size_t count,
                                  uint64_t *out_refs,
                                  size_t *out_done);

/**
 * Copy the bytes `reference` names into `out`, at least their length,
 * and their length into `out_len`. `SUBETHA_E_ARENA_INVALID_REF` when the
 * reference reaches past what the arena holds.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_arena_get(subetha_handle handle,
                          uint64_t reference,
                          uint8_t *out,
                          size_t cap,
                          size_t *out_len);

/**
 * Hand out the bytes `reference` names in place: `out_data` receives a
 * pointer into the arena's mapping and `out_len` their length. The
 * pointer stays valid while this handle is live and until the arena is
 * cleared or reset; destroying the handle unmaps it.
 * `SUBETHA_E_ARENA_INVALID_REF` when the reference reaches past what the
 * arena holds.
 *
 * # Safety
 * `out_data` and `out_len` are valid pointers.
 */
int32_t subetha_arena_view(subetha_handle handle,
                           uint64_t reference,
                           const uint8_t **out_data,
                           size_t *out_len);

/**
 * The byte offset a reference names.
 */
uint64_t subetha_arena_ref_offset(uint64_t reference);

/**
 * The length a reference names.
 */
uint32_t subetha_arena_ref_len(uint64_t reference);

/**
 * Pack `offset` and `len` into the reference an intern of `len` bytes at
 * `offset` returns, for a caller that knows where a value lies. Refused
 * past `SUBETHA_ARENA_CAPACITY_MAX` or `SUBETHA_ARENA_VALUE_MAX`.
 *
 * # Safety
 * `out_ref` is a valid pointer.
 */
int32_t subetha_arena_ref_pack(uint64_t offset, uint32_t len, uint64_t *out_ref);

/**
 * Empty the arena for every handle: the next intern lands at offset zero
 * and every reference handed out so far stops resolving. Not safe against
 * an intern or a get running in any process. `SUBETHA_E_READ_ONLY` on a
 * handle opened read-only.
 */
int32_t subetha_arena_clear(subetha_handle handle);

/**
 * A snapshot of the arena into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_arena_read_stats(subetha_handle handle, struct subetha_arena_stats *out);

/**
 * Write the arena's dirty pages to its file.
 */
int32_t subetha_arena_flush(subetha_handle handle);

/**
 * Remove the arena file at `path`. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_arena_unlink(const char *path, struct subetha_unlink_report *report);

#if defined(SUBETHA_TEST_HOOKS)
/**
 * Reach the 64-bit atomic a handle names and return at once, so a bench
 * can price this family's dispatch apart from the borrow beneath it and
 * the load above it. Present only with the `test-hooks` feature.
 */
int32_t subetha_test_atomic_borrow_only(subetha_handle handle);
#endif

/**
 * Obtain the 32-bit atomic at `path`, initialized to `init` when the file
 * does not exist and attached with its live value when it does, so a
 * racing peer never resets a counter another process is already using. A
 * file built for another width is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_u32_create(const char *path,
                                  uint32_t init,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Truncate the file at `path` and initialize a 32-bit atomic holding
 * `init`, discarding the value other handles share.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_u32_reset(const char *path,
                                 uint32_t init,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the 32-bit atomic another process created at `path`; the file
 * must exist.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_u32_open(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Obtain the 64-bit atomic at `path`. The contract is
 * `subetha_atomic_u32_create`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_u64_create(const char *path,
                                  uint64_t init,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Truncate the file at `path` and initialize a 64-bit atomic holding
 * `init`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_u64_reset(const char *path,
                                 uint64_t init,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the 64-bit atomic another process created at `path`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_u64_open(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Obtain the boolean atomic at `path`. The contract is
 * `subetha_atomic_u32_create`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_bool_create(const char *path, bool init, uint32_t mode, subetha_handle *out);

/**
 * Truncate the file at `path` and initialize a boolean atomic holding
 * `init`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_bool_reset(const char *path, bool init, uint32_t mode, subetha_handle *out);

/**
 * Attach to the boolean atomic another process created at `path`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_atomic_bool_open(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Read the 32-bit value into `out`, sequentially consistent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_u32_load(subetha_handle handle, uint32_t *out);

/**
 * Read the 32-bit value into `out` under `order`, which is relaxed,
 * acquire or sequentially consistent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_u32_load_explicit(subetha_handle handle, uint32_t order, uint32_t *out);

/**
 * Write the 32-bit value, sequentially consistent.
 */
int32_t subetha_atomic_u32_store(subetha_handle handle, uint32_t value);

/**
 * Write the 32-bit value under `order`, which is relaxed, release or
 * sequentially consistent.
 */
int32_t subetha_atomic_u32_store_explicit(subetha_handle handle, uint32_t value, uint32_t order);

/**
 * Replace the 32-bit value, the previous one into `out_prev` when that is
 * not null, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_swap(subetha_handle handle, uint32_t value, uint32_t *out_prev);

/**
 * Replace the 32-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_swap_explicit(subetha_handle handle,
                                         uint32_t value,
                                         uint32_t order,
                                         uint32_t *out_prev);

/**
 * Add to the 32-bit value, wrapping, the previous one into `out_prev`
 * when that is not null, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_add(subetha_handle handle, uint32_t value, uint32_t *out_prev);

/**
 * Add to the 32-bit value, wrapping, under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_add_explicit(subetha_handle handle,
                                              uint32_t value,
                                              uint32_t order,
                                              uint32_t *out_prev);

/**
 * Subtract from the 32-bit value, wrapping, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_sub(subetha_handle handle, uint32_t value, uint32_t *out_prev);

/**
 * Subtract from the 32-bit value, wrapping, under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_sub_explicit(subetha_handle handle,
                                              uint32_t value,
                                              uint32_t order,
                                              uint32_t *out_prev);

/**
 * Bitwise-and the 32-bit value, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_and(subetha_handle handle, uint32_t value, uint32_t *out_prev);

/**
 * Bitwise-and the 32-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_and_explicit(subetha_handle handle,
                                              uint32_t value,
                                              uint32_t order,
                                              uint32_t *out_prev);

/**
 * Bitwise-or the 32-bit value, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_or(subetha_handle handle, uint32_t value, uint32_t *out_prev);

/**
 * Bitwise-or the 32-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_or_explicit(subetha_handle handle,
                                             uint32_t value,
                                             uint32_t order,
                                             uint32_t *out_prev);

/**
 * Bitwise-exclusive-or the 32-bit value, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_xor(subetha_handle handle, uint32_t value, uint32_t *out_prev);

/**
 * Bitwise-exclusive-or the 32-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u32_fetch_xor_explicit(subetha_handle handle,
                                              uint32_t value,
                                              uint32_t order,
                                              uint32_t *out_prev);

/**
 * Replace the 32-bit value with `new` only if it is `expected`.
 * `out_swapped` receives whether it was, and `out_current`, when not
 * null, the value found. Sequentially consistent.
 *
 * # Safety
 * `out_swapped` is a valid pointer; `out_current` is null or a valid
 * pointer.
 */
int32_t subetha_atomic_u32_compare_exchange(subetha_handle handle,
                                            uint32_t expected,
                                            uint32_t new_,
                                            uint32_t *out_current,
                                            bool *out_swapped);

/**
 * Replace the 32-bit value with `new` only if it is `expected`, under
 * `success` when it is replaced and `failure` for the load when it is
 * not; `failure` cannot be release or acquire-release.
 *
 * # Safety
 * `out_swapped` is a valid pointer; `out_current` is null or a valid
 * pointer.
 */
int32_t subetha_atomic_u32_compare_exchange_explicit(subetha_handle handle,
                                                     uint32_t expected,
                                                     uint32_t new_,
                                                     uint32_t success,
                                                     uint32_t failure,
                                                     uint32_t *out_current,
                                                     bool *out_swapped);

/**
 * Read the 64-bit value into `out`, sequentially consistent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_u64_load(subetha_handle handle, uint64_t *out);

/**
 * Read the 64-bit value into `out` under `order`, which is relaxed,
 * acquire or sequentially consistent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_u64_load_explicit(subetha_handle handle, uint32_t order, uint64_t *out);

/**
 * Write the 64-bit value, sequentially consistent.
 */
int32_t subetha_atomic_u64_store(subetha_handle handle, uint64_t value);

/**
 * Write the 64-bit value under `order`, which is relaxed, release or
 * sequentially consistent.
 */
int32_t subetha_atomic_u64_store_explicit(subetha_handle handle, uint64_t value, uint32_t order);

/**
 * Replace the 64-bit value, the previous one into `out_prev` when that is
 * not null, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_swap(subetha_handle handle, uint64_t value, uint64_t *out_prev);

/**
 * Replace the 64-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_swap_explicit(subetha_handle handle,
                                         uint64_t value,
                                         uint32_t order,
                                         uint64_t *out_prev);

/**
 * Add to the 64-bit value, wrapping, the previous one into `out_prev`
 * when that is not null, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_add(subetha_handle handle, uint64_t value, uint64_t *out_prev);

/**
 * Add to the 64-bit value, wrapping, under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_add_explicit(subetha_handle handle,
                                              uint64_t value,
                                              uint32_t order,
                                              uint64_t *out_prev);

/**
 * Subtract from the 64-bit value, wrapping, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_sub(subetha_handle handle, uint64_t value, uint64_t *out_prev);

/**
 * Subtract from the 64-bit value, wrapping, under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_sub_explicit(subetha_handle handle,
                                              uint64_t value,
                                              uint32_t order,
                                              uint64_t *out_prev);

/**
 * Bitwise-and the 64-bit value, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_and(subetha_handle handle, uint64_t value, uint64_t *out_prev);

/**
 * Bitwise-and the 64-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_and_explicit(subetha_handle handle,
                                              uint64_t value,
                                              uint32_t order,
                                              uint64_t *out_prev);

/**
 * Bitwise-or the 64-bit value, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_or(subetha_handle handle, uint64_t value, uint64_t *out_prev);

/**
 * Bitwise-or the 64-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_or_explicit(subetha_handle handle,
                                             uint64_t value,
                                             uint32_t order,
                                             uint64_t *out_prev);

/**
 * Bitwise-exclusive-or the 64-bit value, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_xor(subetha_handle handle, uint64_t value, uint64_t *out_prev);

/**
 * Bitwise-exclusive-or the 64-bit value under `order`.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_u64_fetch_xor_explicit(subetha_handle handle,
                                              uint64_t value,
                                              uint32_t order,
                                              uint64_t *out_prev);

/**
 * Replace the 64-bit value with `new` only if it is `expected`.
 * `out_swapped` receives whether it was, and `out_current`, when not
 * null, the value found. Sequentially consistent.
 *
 * # Safety
 * `out_swapped` is a valid pointer; `out_current` is null or a valid
 * pointer.
 */
int32_t subetha_atomic_u64_compare_exchange(subetha_handle handle,
                                            uint64_t expected,
                                            uint64_t new_,
                                            uint64_t *out_current,
                                            bool *out_swapped);

/**
 * Replace the 64-bit value with `new` only if it is `expected`, under
 * `success` when it is replaced and `failure` for the load when it is
 * not; `failure` cannot be release or acquire-release.
 *
 * # Safety
 * `out_swapped` is a valid pointer; `out_current` is null or a valid
 * pointer.
 */
int32_t subetha_atomic_u64_compare_exchange_explicit(subetha_handle handle,
                                                     uint64_t expected,
                                                     uint64_t new_,
                                                     uint32_t success,
                                                     uint32_t failure,
                                                     uint64_t *out_current,
                                                     bool *out_swapped);

/**
 * Read the flag into `out`, sequentially consistent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_bool_load(subetha_handle handle, bool *out);

/**
 * Read the flag into `out` under `order`, which is relaxed, acquire or
 * sequentially consistent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_bool_load_explicit(subetha_handle handle, uint32_t order, bool *out);

/**
 * Write the flag, sequentially consistent.
 */
int32_t subetha_atomic_bool_store(subetha_handle handle, bool value);

/**
 * Write the flag under `order`, which is relaxed, release or
 * sequentially consistent.
 */
int32_t subetha_atomic_bool_store_explicit(subetha_handle handle, bool value, uint32_t order);

/**
 * Replace the flag, the previous value into `out_prev` when that is not
 * null, sequentially consistent.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_bool_swap(subetha_handle handle, bool value, bool *out_prev);

/**
 * Replace the flag under `order`, the previous value into `out_prev`
 * when that is not null.
 *
 * # Safety
 * `out_prev` is null or a valid pointer.
 */
int32_t subetha_atomic_bool_swap_explicit(subetha_handle handle,
                                          bool value,
                                          uint32_t order,
                                          bool *out_prev);

/**
 * A snapshot of the atomic into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_atomic_read_stats(subetha_handle handle, struct subetha_atomic_stats *out);

/**
 * Write the atomic's page to its file.
 */
int32_t subetha_atomic_flush(subetha_handle handle);

/**
 * Remove the atomic's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_atomic_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Make the client half: it parks on the blocking SPSC ring `ring` names
 * until an item arrives, drains what is there, and ships it to `addr`, a
 * host and port such as `127.0.0.1:9000`.
 *
 * # Safety
 * `addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_blocking_tcp_bridge_client(subetha_handle ring,
                                           const char *addr,
                                           uint32_t mode,
                                           subetha_handle *out);

/**
 * Make the server half: it binds `addr`, accepts one connection and
 * pushes what arrives into the blocking SPSC ring `ring` names, parking
 * while that ring is full. Port zero binds a port the system chooses,
 * which `subetha_blocking_tcp_bridge_local_port` reports.
 *
 * # Safety
 * `addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_blocking_tcp_bridge_server(subetha_handle ring,
                                           const char *addr,
                                           uint32_t mode,
                                           subetha_handle *out);

/**
 * The port the server half listens on, into `out`. For a server bound to
 * port zero this is the port the system chose.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_blocking_tcp_bridge_local_port(subetha_handle handle, uint16_t *out);

/**
 * Carry `items` slots across the connection.
 *
 * In strict mode this blocks the calling thread until the transfer ends
 * or `timeout_ms` elapses, driving the work on that thread. In managed
 * mode it returns at once and the object's own thread carries the
 * transfer, which `subetha_blocking_tcp_bridge_read_stats` reports on.
 *
 * A negative `timeout_ms` waits without a deadline.
 */
int32_t subetha_blocking_tcp_bridge_run(subetha_handle handle, uint64_t items, int64_t timeout_ms);

/**
 * A snapshot of the bridge into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_blocking_tcp_bridge_read_stats(subetha_handle handle,
                                               struct subetha_blocking_tcp_bridge_stats *out);

/**
 * Create a broadcast ring in anonymous memory. `capacity` is at least 2;
 * `scan_interval_us` in the options is ignored, the ring has no sidecar.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_broadcast_create_anon(uint32_t capacity,
                                      const struct subetha_ring_options *options,
                                      subetha_handle *out);

/**
 * Create a file-backed broadcast ring at `path`, or attach to one that
 * exists there with the same capacity, with the wakers at
 * `<path>.cwaker.bin` and `<path>.pwaker.bin`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_broadcast_create(const char *path,
                                 uint32_t capacity,
                                 const struct subetha_ring_options *options,
                                 subetha_handle *out);

/**
 * Attach to a file-backed broadcast ring another process created at
 * `path`. `SUBETHA_E_RING_LAYOUT_MISMATCH` when the file is absent or was
 * created with another capacity.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_broadcast_open(const char *path,
                               uint32_t expected_capacity,
                               const struct subetha_ring_options *options,
                               subetha_handle *out);

/**
 * Create a broadcast ring in the named shared-memory region `name`, with
 * its wakers in `{name}_cwaker` and `{name}_pwaker`.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_broadcast_create_shm(const char *name,
                                     uint32_t capacity,
                                     uint32_t shm_namespace,
                                     const struct subetha_ring_options *options,
                                     subetha_handle *out);

/**
 * Attach to a broadcast ring another process created in named shared
 * memory; the namespace must match the creator's.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_broadcast_open_shm(const char *name,
                                   uint32_t expected_capacity,
                                   uint32_t shm_namespace,
                                   const struct subetha_ring_options *options,
                                   subetha_handle *out);

/**
 * Register as a consumer and receive the index every receive takes. The
 * consumer starts at the producer's current position, not at history.
 * `SUBETHA_E_BROADCAST_NO_CONSUMER_SLOT` when all
 * `SUBETHA_BROADCAST_MAX_CONSUMERS` are taken.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_broadcast_register_consumer(subetha_handle handle, uint32_t *out);

/**
 * Give a consumer index back; the producer no longer waits for it.
 */
int32_t subetha_broadcast_unregister_consumer(subetha_handle handle, uint32_t consumer);

/**
 * Push up to `SUBETHA_BROADCAST_PAYLOAD_BYTES` bytes without waiting.
 * `SUBETHA_E_RING_FULL` while a registered consumer has not read the slot
 * this push would overwrite.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_broadcast_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Push `count` payloads of `len` bytes each, the first at `items` and
 * each next one `stride` bytes on, under one handle lookup and one panic
 * guard. Every consumer sees every one of them, as with a single push.
 * The batch contract is `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_broadcast_try_push_many(subetha_handle handle,
                                        const uint8_t *items,
                                        size_t stride,
                                        size_t len,
                                        size_t count,
                                        size_t *out_done);

/**
 * Receive up to `count` unread slots for `consumer`, the first written at
 * `out` and each next one `stride` bytes on; `stride` is at least
 * `SUBETHA_BROADCAST_PAYLOAD_BYTES`. The batch contract is
 * `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_broadcast_try_recv_many(subetha_handle handle,
                                        uint32_t consumer,
                                        uint8_t *out,
                                        size_t stride,
                                        size_t count,
                                        size_t *out_done);

/**
 * Receive the next unread slot for `consumer` into `out` without
 * waiting; `cap` must be at least `SUBETHA_BROADCAST_PAYLOAD_BYTES` and
 * `out_len` receives that size.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_broadcast_try_recv(subetha_handle handle,
                                   uint32_t consumer,
                                   uint8_t *out,
                                   size_t cap,
                                   size_t *out_len);

/**
 * Push, parking until the slowest consumer frees the slot or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_broadcast_push_wait(subetha_handle handle,
                                    const uint8_t *data,
                                    size_t len,
                                    int64_t timeout_ms);

/**
 * Receive, parking until the producer pushes or `timeout_ms` elapses; the
 * waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_broadcast_recv_wait(subetha_handle handle,
                                    uint32_t consumer,
                                    uint8_t *out,
                                    size_t cap,
                                    size_t *out_len,
                                    int64_t timeout_ms);

/**
 * Items `consumer` has not read yet, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_broadcast_lag(subetha_handle handle, uint32_t consumer, uint64_t *out);

/**
 * Wait until `want` consumers have registered, writing how many there
 * are when the wait ends into `out`.
 *
 * A count below `want` is not an error. It is the shortfall, and it is
 * what the caller needs in order to say which of its readers never
 * arrived.
 *
 * # Why a producer wants this
 *
 * A consumer registers at the head, so everything published before it
 * registered is lost to it and nothing reports that: the push
 * succeeds, the ring does not error, and a reader that started late is
 * indistinguishable from one that is slow. Across processes the window
 * is however long starting one takes. Publishing only once the readers
 * are here is the only thing that closes it, because afterwards there
 * is nothing left to detect.
 *
 * `timeout_ms` must be a real timeout. `SUBETHA_WAIT_FOREVER` is
 * refused rather than honored: a producer waiting without end for a
 * worker that will never start is a hang with nothing to diagnose it,
 * which is the failure this call exists to replace with a number.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_broadcast_wait_for_consumers(subetha_handle handle,
                                             uint32_t want,
                                             int64_t timeout_ms,
                                             uint32_t *out);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_broadcast_read_stats(subetha_handle handle, struct subetha_broadcast_stats *out);

/**
 * Write the ring's dirty pages to its file. Nothing to do in the
 * anonymous and shared-memory locales.
 */
int32_t subetha_broadcast_flush(subetha_handle handle);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the
 * ring and parks again unless it finds what it waited for.
 */
int32_t subetha_broadcast_wake_all(subetha_handle handle);

/**
 * Remove the ring file at `path` and its two waker files. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_broadcast_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * The dimensions that hold `n_items` members at a false-positive rate of
 * `p`, into `out_bits` and `out_hashes`.
 *
 * # Safety
 * `out_bits` and `out_hashes` are valid pointers.
 */
int32_t subetha_blocked_bloom_suggest(uint64_t n_items,
                                      double p,
                                      uint64_t *out_bits,
                                      uint32_t *out_hashes);

/**
 * Obtain the filter at `path` with `n_bits` bits and `n_hashes` hash
 * functions: an empty one is initialized when the file does not exist, an
 * existing one is attached with its members in place. The bits are
 * rounded up to whole 512-bit blocks, which `read_stats` reports.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_blocked_bloom_create(const char *path,
                                     uint64_t n_bits,
                                     uint32_t n_hashes,
                                     uint32_t mode,
                                     subetha_handle *out);

/**
 * Truncate the filter's file at `path` and initialize an empty one,
 * discarding every member live peers share.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_blocked_bloom_reset(const char *path,
                                    uint64_t n_bits,
                                    uint32_t n_hashes,
                                    uint32_t mode,
                                    subetha_handle *out);

/**
 * Attach to the filter another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_blocked_bloom_open(const char *path,
                                   uint64_t n_bits,
                                   uint32_t n_hashes,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Add the `len` bytes at `item` to the filter.
 *
 * # Safety
 * `item` points to `len` readable bytes.
 */
int32_t subetha_blocked_bloom_insert(subetha_handle handle, const uint8_t *item, size_t len);

/**
 * Whether the `len` bytes at `item` may be a member, into `out`. False is
 * certain; true carries the filter's false-positive rate.
 *
 * # Safety
 * `item` points to `len` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_blocked_bloom_contains(subetha_handle handle,
                                       const uint8_t *item,
                                       size_t len,
                                       bool *out);

/**
 * Clear every block, so the filter holds no members.
 */
int32_t subetha_blocked_bloom_clear(subetha_handle handle);

/**
 * Push the filter's dirty pages to disk, returning when they are durable.
 */
int32_t subetha_blocked_bloom_flush(subetha_handle handle);

/**
 * A snapshot of the filter into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_blocked_bloom_read_stats(subetha_handle handle,
                                         struct subetha_blocked_bloom_stats *out);

/**
 * The dimensions that hold `n_items` members at a false-positive rate of
 * `p`, into `out_bits` and `out_hashes`. A caller sizes a filter from
 * what it expects to hold rather than from bit counts.
 *
 * # Safety
 * `out_bits` and `out_hashes` are valid pointers.
 */
int32_t subetha_bloom_suggest(uint64_t n_items, double p, uint64_t *out_bits, uint32_t *out_hashes);

/**
 * Obtain the filter at `path` with `n_bits` bits and `n_hashes` hash
 * functions: an empty one is initialized when its files do not exist, an
 * existing one is attached with its members in place. A filter built
 * with other dimensions is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_bloom_create(const char *path,
                             uint64_t n_bits,
                             uint32_t n_hashes,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Truncate the filter's files at `path` and initialize an empty one,
 * discarding every member live peers share. For a caller that knows it
 * owns the path.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_bloom_reset(const char *path,
                            uint64_t n_bits,
                            uint32_t n_hashes,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to the filter another process created at `path`; its files must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_bloom_open(const char *path,
                           uint64_t n_bits,
                           uint32_t n_hashes,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Add the `len` bytes at `item` to the filter.
 *
 * # Safety
 * `item` points to `len` readable bytes.
 */
int32_t subetha_bloom_insert(subetha_handle handle, const uint8_t *item, size_t len);

/**
 * Whether the `len` bytes at `item` may be a member, into `out`. False is
 * certain; true carries the filter's false-positive rate.
 *
 * # Safety
 * `item` points to `len` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_bloom_contains(subetha_handle handle, const uint8_t *item, size_t len, bool *out);

/**
 * Clear every bit, so the filter holds no members. Peers sharing the
 * files see the same empty filter.
 */
int32_t subetha_bloom_clear(subetha_handle handle);

/**
 * Push the filter's dirty pages to disk, returning when they are
 * durable. `subetha_bloom_flush_async` starts the same work and returns
 * at once.
 */
int32_t subetha_bloom_flush(subetha_handle handle);

/**
 * Start pushing the filter's dirty pages to disk and return at once.
 */
int32_t subetha_bloom_flush_async(subetha_handle handle);

/**
 * A snapshot of the filter into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_bloom_read_stats(subetha_handle handle, struct subetha_bloom_stats *out);

/**
 * Obtain the map at `path` for `capacity` nodes of `key_size`-byte keys
 * and `value_size`-byte values: an empty one is initialized when the file
 * does not exist, an existing one is attached with its tree in place. A
 * file built with another capacity, sizes or `tag` is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`, and so is one written by the Rust
 * `SharedBTreeMap`, which orders its keys by their type rather than as
 * bytes.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_btree_create(const char *path,
                             uint32_t capacity,
                             uint32_t key_size,
                             uint32_t value_size,
                             uint64_t tag,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Attach to the map another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_btree_open(const char *path,
                           uint32_t capacity,
                           uint32_t key_size,
                           uint32_t value_size,
                           uint64_t tag,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty map there,
 * discarding the tree other handles share. On Windows the file must not
 * be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_btree_reset(const char *path,
                            uint32_t capacity,
                            uint32_t key_size,
                            uint32_t value_size,
                            uint64_t tag,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Insert or update `key` with `value`. `out_replaced`, when not null,
 * receives whether a value was already there. A caller that also wants
 * the value it replaced passes `previous` and `previous_len`; one that
 * does not passes null for both. `SUBETHA_E_RING_FULL` when the map has
 * no node for the split the insert needs. One writer: serialize this
 * against other writers yourself.
 *
 * # Safety
 * `key` and `value` point to readable bytes; `previous` is null or
 * points to `cap` writable bytes; `previous_len` and `out_replaced` are
 * null or valid pointers.
 */
int32_t subetha_btree_insert(subetha_handle handle,
                             const uint8_t *key,
                             size_t key_len,
                             const uint8_t *value,
                             size_t value_len,
                             uint8_t *previous,
                             size_t cap,
                             size_t *previous_len,
                             bool *out_replaced);

/**
 * Copy `key`'s value into `out`, at least `value_size` bytes;
 * `SUBETHA_E_MAP_KEY_ABSENT` when the key has no entry.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_btree_get(subetha_handle handle,
                          const uint8_t *key,
                          size_t key_len,
                          uint8_t *out,
                          size_t cap,
                          size_t *out_len);

/**
 * Whether `key` has an entry.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_btree_contains(subetha_handle handle,
                               const uint8_t *key,
                               size_t key_len,
                               bool *out);

/**
 * Remove `key`, copying the value it held into `out`, at least
 * `value_size` bytes; `SUBETHA_E_MAP_KEY_ABSENT` when it had none. One
 * writer, as insert is.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_btree_remove(subetha_handle handle,
                             const uint8_t *key,
                             size_t key_len,
                             uint8_t *out,
                             size_t cap,
                             size_t *out_len);

/**
 * Copy the smallest key and its value into `key_out` and `value_out`,
 * each at least its own size. `SUBETHA_E_MAP_KEY_ABSENT` when the map is
 * empty.
 *
 * # Safety
 * `key_out` and `value_out` point to `key_cap` and `value_cap` writable
 * bytes; `key_len` and `value_len` are valid pointers.
 */
int32_t subetha_btree_first(subetha_handle handle,
                            uint8_t *key_out,
                            size_t key_cap,
                            size_t *key_len,
                            uint8_t *value_out,
                            size_t value_cap,
                            size_t *value_len);

/**
 * Copy the largest key and its value out, as `subetha_btree_first`.
 *
 * # Safety
 * `key_out` and `value_out` point to `key_cap` and `value_cap` writable
 * bytes; `key_len` and `value_len` are valid pointers.
 */
int32_t subetha_btree_last(subetha_handle handle,
                           uint8_t *key_out,
                           size_t key_cap,
                           size_t *key_len,
                           uint8_t *value_out,
                           size_t value_cap,
                           size_t *value_len);

/**
 * Empty the map for every handle. One writer: not safe against an insert
 * or a remove running in any process.
 */
int32_t subetha_btree_clear(subetha_handle handle);

/**
 * A snapshot of the map into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_btree_read_stats(subetha_handle handle, struct subetha_btree_stats *out);

/**
 * Write the map's dirty pages to its file.
 */
int32_t subetha_btree_flush(subetha_handle handle);

/**
 * Remove the map file at `path`. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_btree_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the cell at `path` for a `value_size`-byte value: an empty one
 * is initialized when the file does not exist, an existing one is
 * attached with its value and version in place, so a peer that is already
 * using it is never reset out from under. The size is recorded in the
 * region, so a handle that declares another is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`, and a cell of four bytes is the same
 * region as a Rust `SharedCell<u32>`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_cell_create(const char *path,
                            uint32_t value_size,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to the cell another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_cell_open(const char *path,
                          uint32_t value_size,
                          uint32_t mode,
                          subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty cell there,
 * discarding the value other handles share. On Windows the file must not
 * be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_cell_reset(const char *path,
                           uint32_t value_size,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Replace the value with `data`, exactly `value_size` bytes. One writer:
 * two processes writing at once race, and serializing them is the
 * caller's contract.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_cell_set(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Copy the value into `out`, at least `value_size` bytes, and its length
 * into `out_len`. Retries while a write is in flight, so it never yields
 * half of one value and half of another.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_cell_get(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * The cell's version into `out`: twice the writes it has taken, and odd
 * while one is in flight. A reader that only wants to know whether the
 * value changed watches this rather than comparing bytes.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_cell_version(subetha_handle handle, uint32_t *out);

/**
 * A snapshot of the cell into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_cell_read_stats(subetha_handle handle, struct subetha_cell_stats *out);

/**
 * Write the cell's page to its file.
 */
int32_t subetha_cell_flush(subetha_handle handle);

/**
 * Remove the cell's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_cell_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * The dimensions that hold an estimate within `epsilon` of the true
 * count with probability `1 - delta`, into `out_d` and `out_w`. A caller
 * sizes a sketch from the error it will tolerate rather than from a
 * counter matrix.
 *
 * # Safety
 * `out_d` and `out_w` are valid pointers.
 */
int32_t subetha_cms_suggest(double epsilon, double delta, uint32_t *out_d, uint32_t *out_w);

/**
 * Obtain the sketch at `path` with `d` rows of `w` counters: an empty one
 * is initialized when the file does not exist, an existing one is
 * attached with its counts in place. A sketch built with other
 * dimensions is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_cms_create(const char *path,
                           uint32_t d,
                           uint32_t w,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Attach to the sketch another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_cms_open(const char *path,
                         uint32_t d,
                         uint32_t w,
                         uint32_t mode,
                         subetha_handle *out);

/**
 * Count one occurrence of the `len` bytes at `item`.
 *
 * # Safety
 * `item` points to `len` readable bytes.
 */
int32_t subetha_cms_insert(subetha_handle handle, const uint8_t *item, size_t len);

/**
 * Count `count` occurrences of the `len` bytes at `item`.
 *
 * # Safety
 * `item` points to `len` readable bytes.
 */
int32_t subetha_cms_insert_n(subetha_handle handle,
                             const uint8_t *item,
                             size_t len,
                             uint64_t count);

/**
 * The estimated count of the `len` bytes at `item`, into `out`. At or
 * above the true count, never below it.
 *
 * # Safety
 * `item` points to `len` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_cms_estimate(subetha_handle handle, const uint8_t *item, size_t len, uint64_t *out);

/**
 * Zero every counter, so the sketch holds no counts. Peers sharing the
 * file see the same empty sketch.
 */
int32_t subetha_cms_reset(subetha_handle handle);

/**
 * Push the sketch's dirty pages to disk, returning when they are
 * durable. `subetha_cms_flush_async` starts the same work and returns at
 * once.
 */
int32_t subetha_cms_flush(subetha_handle handle);

/**
 * Start pushing the sketch's dirty pages to disk and return at once.
 */
int32_t subetha_cms_flush_async(subetha_handle handle);

/**
 * A snapshot of the sketch into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_cms_read_stats(subetha_handle handle, struct subetha_cms_stats *out);

/**
 * Create the condition variable at `path` with room for `max_waiters`
 * parked callers at once. Both backing files are initialized, so the
 * generation starts at zero.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_condvar_create(const char *path,
                               uint32_t max_waiters,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Attach to the condition variable another process created at `path`,
 * with the same `max_waiters` the creator used; the backing files must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_condvar_open(const char *path,
                             uint32_t max_waiters,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * The generation, which every notify from any process advances by one.
 * Read it first, ahead of checking your own predicate, and hand it to
 * `subetha_condvar_wait`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_condvar_generation(subetha_handle handle, uint64_t *out);

/**
 * Park until the generation moves past `from_generation`, waiting up to
 * `timeout_ms`; `SUBETHA_WAIT_FOREVER` waits without a deadline.
 *
 * Returns `SUBETHA_OK` once a notify has advanced the generation,
 * `SUBETHA_E_TIMEOUT` when the wait elapses first,
 * `SUBETHA_E_DESTROYED` when the condvar's handle is destroyed under it,
 * and `SUBETHA_E_RING_WAKER_FULL` when every waiter slot is parked. A
 * caller passing a `from_generation` the condvar has already moved past
 * returns at once with `SUBETHA_OK`, which is what makes reading the
 * generation ahead of the predicate close the lost-notify window.
 */
int32_t subetha_condvar_wait(subetha_handle handle, uint64_t from_generation, int64_t timeout_ms);

/**
 * Advance the generation and wake at most one parked caller, writing how
 * many were woken into `out_woken`. Change what the waiters are watching
 * before calling, since a woken caller re-checks its own predicate.
 *
 * # Safety
 * `out_woken` is null or a valid pointer.
 */
int32_t subetha_condvar_notify_one(subetha_handle handle, uint32_t *out_woken);

/**
 * Advance the generation and wake every parked caller, writing how many
 * were woken into `out_woken`. The contract is
 * `subetha_condvar_notify_one`'s.
 *
 * # Safety
 * `out_woken` is null or a valid pointer.
 */
int32_t subetha_condvar_notify_all(subetha_handle handle, uint32_t *out_woken);

/**
 * A snapshot of the condition variable into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_condvar_read_stats(subetha_handle handle, struct subetha_condvar_stats *out);

/**
 * Remove the condition variable's two files at `path`:
 * `<path>.waker.bin` and `<path>.gen.bin`. The contract is
 * `subetha_ring_unlink`'s, and the report counts both.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_condvar_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the epoch table at `path` holding `capacity` pins and as many
 * tickets: an empty one is initialized when the file does not exist, an
 * existing one is attached with its counter and its live pins in place.
 * `capacity` is how many scans may hold a pin at once. A file built with
 * another capacity is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_epochs_create(const char *path,
                              uint32_t capacity,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Attach to the epoch table another process created at `path`; the file
 * must exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_epochs_open(const char *path,
                            uint32_t capacity,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * The published epoch, which is what a pin taken now would hold. With
 * no ticket open this is the counter; with tickets open it is one below
 * the oldest of them, so nothing a compound write has stamped is
 * visible before that write publishes.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_now(subetha_handle handle, uint64_t *out);

/**
 * Take the next epoch, for a single-record write that stamps as it goes.
 * A write touching several records wants one epoch across all of them
 * and takes a ticket instead.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_advance(subetha_handle handle, uint64_t *out);

/**
 * Epochs at or below this have no reader, so anything superseded in one
 * of them may be reclaimed. With no pin outstanding it is the published
 * epoch: nothing is being read, so everything superseded is
 * reclaimable.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_reclaim_horizon(subetha_handle handle, uint64_t *out);

/**
 * Pins outstanding across every process attached to the table.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_live_pins(subetha_handle handle, uint64_t *out);

/**
 * Tickets open across every process attached to the table.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_open_tickets(subetha_handle handle, uint64_t *out);

/**
 * Free every pin slot whose owning process is gone and report how many
 * went. A pin outlives its process only when that process died holding
 * it, so the slot names a scan that will never finish and the horizon it
 * holds down is stale. `subetha_epochs_pin` does this itself before
 * reporting the table full; this is for a caller reclaiming on its own
 * schedule.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_reap_dead_pins(subetha_handle handle, uint64_t *out);

/**
 * Pin the published epoch and write a handle naming the pin into `out`.
 * Nothing superseded at or after that epoch is reclaimable until the pin
 * handle is destroyed. `SUBETHA_E_EPOCHS_PINS_EXHAUSTED` when every slot
 * is held by a live process; a slot left by a process that died is
 * reclaimed here rather than counting against the table.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_pin(subetha_handle handle, uint64_t *out);

/**
 * The epoch the pin `token` names. A reader shows it to each record to
 * pick the version it should see.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_pin_epoch(subetha_handle handle, uint64_t token, uint64_t *out);

/**
 * Whether a version superseded at `superseded_at` is visible to this
 * pin: true when it was superseded after the pin was taken, and for
 * `SUBETHA_EPOCH_LIVE`, which is the version still current.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_pin_sees(subetha_handle handle, uint64_t token, uint64_t superseded_at, bool *out);

/**
 * Let the pin `token` names go. The reclaim horizon is free to pass its
 * epoch as soon as this returns, and the token names nothing afterwards.
 *
 * A token released twice is refused rather than freeing a pin slot
 * another scan has since taken.
 */
int32_t subetha_pin_release(subetha_handle handle, uint64_t token);

/**
 * Reserve an epoch for one compound write and write a handle naming the
 * ticket into `out`. Every record the write stamps carries
 * `subetha_ticket_epoch`, and none of it is visible to a pin until the
 * ticket is published. `SUBETHA_E_EPOCHS_TICKETS_EXHAUSTED` when every
 * ticket slot is held by a live process; a slot left by a process that
 * died is reported by `subetha_epochs_dead_tickets` rather than reused
 * here, since its epoch may be half-written.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_begin(subetha_handle handle, uint64_t *out);

/**
 * The epoch every record of the compound write `token` names is stamped
 * with.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ticket_epoch(subetha_handle handle, uint64_t token, uint64_t *out);

/**
 * Pins and tickets this handle has handed out and not had back, which a
 * caller reads to find a leak: both return to zero once every token
 * taken has been released or published.
 *
 * # Safety
 * `out_pins` and `out_tickets` are valid pointers.
 */
int32_t subetha_epochs_held(subetha_handle handle, uint64_t *out_pins, uint64_t *out_tickets);

/**
 * Make the write visible: every record stamped with this ticket's epoch
 * is seen by every pin taken from now on, all at once. The token names
 * nothing afterwards.
 *
 * A token published twice is refused rather than freeing a ticket slot
 * another writer has since taken. A caller abandoning a compound write
 * publishes too, having removed what it stamped first.
 */
int32_t subetha_ticket_publish(subetha_handle handle, uint64_t token);

/**
 * The epochs whose ticket is held by a process that is gone: compound
 * writes that will never publish. Each structure sharing the table undoes
 * what it stamped at those epochs, and then
 * `subetha_epochs_free_dead_ticket` releases the slot. Writes at most
 * `cap` epochs into `out` and the number found into `out_count`, which
 * may exceed `cap`; a caller that sees it exceed `cap` calls again with a
 * larger buffer.
 *
 * # Safety
 * `out` points to `cap` writable `uint64_t`s; `out_count` is a valid
 * pointer.
 */
int32_t subetha_epochs_dead_tickets(subetha_handle handle,
                                    uint64_t *out,
                                    size_t cap,
                                    size_t *out_count);

/**
 * Release the ticket for `epoch`, once every structure has undone what it
 * stamped there, and write whether it went into `out_freed`. Only a
 * ticket whose process is gone is released: a live writer's ticket is
 * left alone and `out_freed` is false, as it is for an epoch no ticket
 * holds.
 *
 * # Safety
 * `out_freed` is a valid pointer.
 */
int32_t subetha_epochs_free_dead_ticket(subetha_handle handle, uint64_t epoch, bool *out_freed);

/**
 * A snapshot of the table into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epochs_read_stats(subetha_handle handle, struct subetha_epochs_stats *out);

/**
 * Remove the epoch table's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_epochs_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the region at `path` with `block_count` blocks of `block_size`
 * bytes: an empty one is initialized when the file does not exist, an
 * existing one is attached with its allocated blocks and free list in
 * place, so a late joiner never wipes what a producer already filled. A
 * file built with another geometry is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_frame_region_create(const char *path,
                                    uint32_t block_size,
                                    uint32_t block_count,
                                    uint32_t mode,
                                    subetha_handle *out);

/**
 * Attach to the region another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_frame_region_open(const char *path,
                                  uint32_t block_size,
                                  uint32_t block_count,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty region there,
 * invalidating every block index other handles hold. On Windows the file
 * must not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_frame_region_reset(const char *path,
                                   uint32_t block_size,
                                   uint32_t block_count,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Take a block and write its index into `out_block`: the free list first,
 * then the never-yet-allocated ones. `SUBETHA_E_RING_FULL` when every
 * block is out.
 *
 * # Safety
 * `out_block` is a valid pointer.
 */
int32_t subetha_frame_region_alloc(subetha_handle handle, uint32_t *out_block);

/**
 * Return block `block` to the free list, for the next allocation to take.
 * Any process may free any block. The free list is threaded through the
 * blocks themselves, so this overwrites the first four bytes of the one it
 * takes: a block's payload does not survive being freed, and a caller that
 * wants the bytes reads them first. `SUBETHA_E_OUT_OF_BOUNDS` for an index
 * past the region.
 */
int32_t subetha_frame_region_free(subetha_handle handle, uint32_t block);

/**
 * Copy `len` bytes from `data` into block `block`, at most one block's
 * worth. The bytes past what is written keep whatever the block held, so
 * a reader takes the length from wherever it took the block's index.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_frame_region_write(subetha_handle handle,
                                   uint32_t block,
                                   const uint8_t *data,
                                   size_t len);

/**
 * Copy `len` bytes out of block `block` into `out`, at least `len` bytes,
 * and what was copied into `out_len`.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_frame_region_read(subetha_handle handle,
                                  uint32_t block,
                                  size_t len,
                                  uint8_t *out,
                                  size_t cap,
                                  size_t *out_len);

/**
 * A snapshot of the region into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_frame_region_read_stats(subetha_handle handle,
                                        struct subetha_frame_region_stats *out);

/**
 * Remove the region file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_frame_region_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Create a capacity-adaptive ring in anonymous memory.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_capacity_create_anon(uint32_t max_producers,
                                     uint32_t max_consumers,
                                     uint32_t initial_capacity,
                                     const struct subetha_ring_options *options,
                                     subetha_handle *out);

/**
 * Create a file-backed capacity-adaptive ring under `base_path`, or
 * attach to one that exists there: the active backing is
 * `<base_path>.cap_<initial_capacity>.bin` with the adaptive ring's own
 * files under it, and the wakers `<base_path>.cwaker.bin` and
 * `<base_path>.pwaker.bin` are created or attached.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_create(const char *base_path,
                                uint32_t max_producers,
                                uint32_t max_consumers,
                                uint32_t initial_capacity,
                                const struct subetha_ring_options *options,
                                subetha_handle *out);

/**
 * Attach to a file-backed capacity-adaptive ring another handle created
 * under `base_path`, with the counts and initial capacity it was created
 * with; the backing is opened, not re-initialized, and the wakers are
 * attached. This handle's morph state starts at the initial capacity. A
 * capacity other than the creator's names a backing of its own, which is
 * absent and refused as `SUBETHA_E_RING_IO`.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_open(const char *base_path,
                              uint32_t max_producers,
                              uint32_t max_consumers,
                              uint32_t initial_capacity,
                              const struct subetha_ring_options *options,
                              subetha_handle *out);

/**
 * Create a capacity-adaptive ring in named shared memory under
 * `name_prefix`, in the session namespace: the active backing is
 * `{name_prefix}_cap_<initial_capacity>` and the wakers `{name_prefix}_cwaker`
 * and `{name_prefix}_pwaker`.
 *
 * # Safety
 * `name_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_create_shm(const char *name_prefix,
                                    uint32_t max_producers,
                                    uint32_t max_consumers,
                                    uint32_t initial_capacity,
                                    const struct subetha_ring_options *options,
                                    subetha_handle *out);

/**
 * Attach to a capacity-adaptive ring another process created in named
 * shared memory under `name_prefix`, with the counts and capacity it was
 * created with; the wakers are attached, not reset.
 *
 * # Safety
 * `name_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_open_shm(const char *name_prefix,
                                  uint32_t max_producers,
                                  uint32_t max_consumers,
                                  uint32_t initial_capacity,
                                  const struct subetha_ring_options *options,
                                  subetha_handle *out);

/**
 * Register as a producer on the active backing; every morph mirrors the
 * registration onto the next backing.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_register_producer(subetha_handle handle, uint32_t *out);

/**
 * Register as a consumer on the active backing.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_register_consumer(subetha_handle handle, uint32_t *out);

/**
 * Push into the active backing without waiting; the slot semantics are
 * `subetha_ring_try_push`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_capacity_try_push(subetha_handle handle,
                                  uint32_t producer_id,
                                  const uint8_t *data,
                                  size_t len);

/**
 * Pop without waiting, draining stale backings before the active one so
 * items keep their order across a morph; the slot semantics are
 * `subetha_ring_try_pop`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_capacity_try_pop(subetha_handle handle,
                                 uint32_t consumer_id,
                                 uint8_t *out,
                                 size_t cap,
                                 size_t *out_len);

/**
 * Push, parking until there is room or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_capacity_push_wait(subetha_handle handle,
                                   uint32_t producer_id,
                                   const uint8_t *data,
                                   size_t len,
                                   int64_t timeout_ms);

/**
 * Pop, parking until an item arrives or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_capacity_pop_wait(subetha_handle handle,
                                  uint32_t consumer_id,
                                  uint8_t *out,
                                  size_t cap,
                                  size_t *out_len,
                                  int64_t timeout_ms);

/**
 * Morph the ring's capacity to `new_capacity`, a power of two of at least
 * 2. Producers move to the fresh backing; the consumer drains the old one
 * first. Concurrent morphs serialize; the data path is never blocked.
 */
int32_t subetha_capacity_morph_capacity(subetha_handle handle, uint32_t new_capacity);

/**
 * Change any of shape, capacity and locale in one transition: `shape` is
 * a `SUBETHA_RING_SHAPE_` constant or `SUBETHA_KEEP`, `capacity` a power
 * of two or zero to keep, `target_kind` a `SUBETHA_TARGET_` constant with
 * `target_name` the base path or shared-memory prefix it needs. A new
 * locale stays the locale of every later morph and prewarm. A change of
 * shape alone morphs the active backing in place.
 *
 * # Safety
 * `target_name` is null or a NUL-terminated UTF-8 string.
 */
int32_t subetha_capacity_morph(subetha_handle handle,
                               uint32_t shape,
                               uint32_t capacity,
                               uint32_t target_kind,
                               const char *target_name);

/**
 * Build the backing a coming morph to `capacity` needs, off the data
 * path, so that morph pays only the swap. One backing is held; a prewarm
 * at another capacity replaces it.
 */
int32_t subetha_capacity_prewarm(subetha_handle handle, uint32_t capacity);

/**
 * Drop the warm backing, releasing its memory or files.
 */
int32_t subetha_capacity_clear_warm(subetha_handle handle);

/**
 * Set the ordering mode of a stamped capacity ring across the active
 * backing and every stale one still draining; the contract is
 * `subetha_ring_set_ordering_mode`'s.
 */
int32_t subetha_capacity_set_ordering_mode(subetha_handle handle, uint32_t mode);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_read_stats(subetha_handle handle, struct subetha_capacity_stats *out);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the
 * ring and parks again unless it finds what it waited for.
 */
int32_t subetha_capacity_wake_all(subetha_handle handle);

/**
 * Remove every file under `base_path`'s directory whose name starts with
 * `<base name>.cap_`, which is every backing the ring ever built there,
 * plus the two waker files. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_capacity_unlink(const char *base_path, struct subetha_unlink_report *report);

/**
 * Create a capacity-adaptive broadcast ring in anonymous memory.
 * `initial_capacity` is a power of two of at least 2.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_capacity_broadcast_create_anon(uint32_t initial_capacity,
                                               const struct subetha_ring_options *options,
                                               subetha_handle *out);

/**
 * Create a file-backed capacity-adaptive broadcast ring under
 * `base_path`, or attach to one that exists there.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_broadcast_create(const char *base_path,
                                          uint32_t initial_capacity,
                                          const struct subetha_ring_options *options,
                                          subetha_handle *out);

/**
 * Create a capacity-adaptive broadcast ring in named shared memory under
 * `name_prefix`, in the session namespace, initializing its wakers.
 *
 * # Safety
 * `name_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_broadcast_create_shm(const char *name_prefix,
                                              uint32_t initial_capacity,
                                              const struct subetha_ring_options *options,
                                              subetha_handle *out);

/**
 * Attach to a capacity-adaptive broadcast ring another process created
 * in named shared memory under `name_prefix`; the wakers are attached,
 * not reset.
 *
 * # Safety
 * `name_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_broadcast_open_shm(const char *name_prefix,
                                            uint32_t initial_capacity,
                                            const struct subetha_ring_options *options,
                                            subetha_handle *out);

/**
 * Register as a consumer; every morph mirrors the registration onto the
 * next backing, and there is no unregistering. The contract is
 * `subetha_broadcast_register_consumer`'s.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_broadcast_register_consumer(subetha_handle handle, uint32_t *out);

/**
 * Push without waiting; the contract is `subetha_broadcast_try_push`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_capacity_broadcast_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Receive without waiting, draining stale backings before the active
 * one; the contract is `subetha_broadcast_try_recv`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_capacity_broadcast_try_recv(subetha_handle handle,
                                            uint32_t consumer,
                                            uint8_t *out,
                                            size_t cap,
                                            size_t *out_len);

/**
 * Push, parking until the slowest consumer frees the slot or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_capacity_broadcast_push_wait(subetha_handle handle,
                                             const uint8_t *data,
                                             size_t len,
                                             int64_t timeout_ms);

/**
 * Receive, parking until the producer pushes or `timeout_ms` elapses; the
 * waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_capacity_broadcast_recv_wait(subetha_handle handle,
                                             uint32_t consumer,
                                             uint8_t *out,
                                             size_t cap,
                                             size_t *out_len,
                                             int64_t timeout_ms);

/**
 * Morph the ring's capacity to `new_capacity`, a power of two of at
 * least 2; every registered consumer follows.
 */
int32_t subetha_capacity_broadcast_morph_capacity(subetha_handle handle, uint32_t new_capacity);

/**
 * Build the backing a coming morph to `capacity` needs, off the data
 * path; the contract is `subetha_capacity_prewarm`'s.
 */
int32_t subetha_capacity_broadcast_prewarm(subetha_handle handle, uint32_t capacity);

/**
 * Drop the warm backing.
 */
int32_t subetha_capacity_broadcast_clear_warm(subetha_handle handle);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_broadcast_read_stats(subetha_handle handle,
                                              struct subetha_capacity_broadcast_stats *out);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the
 * ring and parks again unless it finds what it waited for.
 */
int32_t subetha_capacity_broadcast_wake_all(subetha_handle handle);

/**
 * Remove every backing file under `base_path` and the two waker files;
 * the contract is `subetha_capacity_unlink`'s.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_capacity_broadcast_unlink(const char *base_path,
                                          struct subetha_unlink_report *report);

/**
 * Create a capacity-adaptive pub/sub ring in anonymous memory.
 * `initial_capacity` is a power of two of at least 2.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_capacity_pubsub_create_anon(uint32_t initial_capacity,
                                            const struct subetha_ring_options *options,
                                            subetha_handle *out);

/**
 * Create a file-backed capacity-adaptive pub/sub ring under `base_path`,
 * or attach to one that exists there.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_pubsub_create(const char *base_path,
                                       uint32_t initial_capacity,
                                       const struct subetha_ring_options *options,
                                       subetha_handle *out);

/**
 * Create a capacity-adaptive pub/sub ring in named shared memory under
 * `name_prefix`, in the session namespace, initializing its waker.
 *
 * # Safety
 * `name_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_pubsub_create_shm(const char *name_prefix,
                                           uint32_t initial_capacity,
                                           const struct subetha_ring_options *options,
                                           subetha_handle *out);

/**
 * Attach to a capacity-adaptive pub/sub ring another process created in
 * named shared memory under `name_prefix`; the waker is attached, not
 * reset.
 *
 * # Safety
 * `name_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_capacity_pubsub_open_shm(const char *name_prefix,
                                         uint32_t initial_capacity,
                                         const struct subetha_ring_options *options,
                                         subetha_handle *out);

/**
 * Publish up to `SUBETHA_PUBSUB_PAYLOAD_BYTES` bytes into the newest
 * backing, never waiting; the position within that backing lands in
 * `out_position` when it is not null.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_position` is null or a
 * valid pointer.
 */
int32_t subetha_capacity_pubsub_publish(subetha_handle handle,
                                        const uint8_t *data,
                                        size_t len,
                                        uint64_t *out_position);

/**
 * Subscribe from the newest backing's current head: only what is
 * published from now on.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_pubsub_subscribe_from_now(subetha_handle handle,
                                                   uint32_t mode,
                                                   subetha_handle *out);

/**
 * Subscribe from the start of the oldest backing still in the chain:
 * every item still held, backing by backing.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_pubsub_subscribe_from_oldest(subetha_handle handle,
                                                      uint32_t mode,
                                                      subetha_handle *out);

/**
 * Morph the ring's capacity to `new_capacity`, a power of two of at
 * least 2, appending a fresh backing to the chain.
 */
int32_t subetha_capacity_pubsub_morph_capacity(subetha_handle handle, uint32_t new_capacity);

/**
 * Build the backing a coming morph to `capacity` needs, off the data
 * path; the contract is `subetha_capacity_prewarm`'s.
 */
int32_t subetha_capacity_pubsub_prewarm(subetha_handle handle, uint32_t capacity);

/**
 * Drop the warm backing.
 */
int32_t subetha_capacity_pubsub_clear_warm(subetha_handle handle);

/**
 * Reclaim the oldest backings no subscriber still reads; the number
 * reclaimed lands in `out` when it is not null. The active backing is
 * never reclaimed.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_capacity_pubsub_gc(subetha_handle handle, uint32_t *out);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_pubsub_read_stats(subetha_handle handle,
                                           struct subetha_capacity_pubsub_stats *out);

/**
 * Wake every subscriber parked in a wait on this ring.
 */
int32_t subetha_capacity_pubsub_wake_all(subetha_handle handle);

/**
 * Read the next item into `out`, at least `SUBETHA_PUBSUB_PAYLOAD_BYTES`,
 * crossing into the next backing when this one is drained.
 * `SUBETHA_E_PUBSUB_PENDING` when nothing newer is published,
 * `SUBETHA_E_PUBSUB_LOST` when the position was overwritten.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_capacity_subscriber_try_next(subetha_handle handle,
                                             uint8_t *out,
                                             size_t cap,
                                             size_t *out_len);

/**
 * `subetha_capacity_subscriber_try_next`, parking while nothing newer is
 * published; the waiting contract is `subetha_subscriber_next_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_capacity_subscriber_next_wait(subetha_handle handle,
                                              uint8_t *out,
                                              size_t cap,
                                              size_t *out_len,
                                              int64_t timeout_ms);

/**
 * Where the subscriber stands: the backing's index in the chain into
 * `out_backing` and the position within it into `out_position`; either
 * may be null.
 *
 * # Safety
 * `out_backing` and `out_position` are null or valid pointers.
 */
int32_t subetha_capacity_subscriber_position(subetha_handle handle,
                                             uint64_t *out_backing,
                                             uint64_t *out_position);

/**
 * The mode a subscriber was created in, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_capacity_subscriber_mode(subetha_handle handle, uint32_t *out);

/**
 * Remove every backing file under `base_path` and the waker file; the
 * contract is `subetha_capacity_unlink`'s.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_capacity_pubsub_unlink(const char *base_path, struct subetha_unlink_report *report);

/**
 * The name of a code, as a static NUL-terminated string. Safe from any
 * thread at any time; an unknown code names itself as unknown.
 */
const char *subetha_strerror(int32_t code);

/**
 * Copy the detail of the last failing call on this thread into `buf`,
 * NUL-terminated, and return the number of bytes the whole detail needs
 * including the terminator. A return larger than `cap` means the copy was
 * cut short; call again with a buffer of the returned size. With `buf` null
 * or `cap` zero nothing is copied and only the size is returned. The detail
 * describes the most recent failure and is meaningful only right after a
 * call returned a code other than SUBETHA_OK.
 *
 * # Safety
 * `buf` is null or points to `cap` writable bytes.
 */
size_t subetha_last_error_detail(char *buf, size_t cap);

/**
 * Create a Lamport pair in anonymous memory: the producer handle into
 * `out_producer`, the consumer handle into `out_consumer`.
 *
 * # Safety
 * `out_producer` and `out_consumer` are valid pointers.
 */
int32_t subetha_lamport_create_anon_pair(uint32_t capacity,
                                         uint32_t mode,
                                         subetha_handle *out_producer,
                                         subetha_handle *out_consumer);

/**
 * Create a file-backed Lamport pair at `path`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out_producer` and
 * `out_consumer` are valid pointers.
 */
int32_t subetha_lamport_create_pair(const char *path,
                                    uint32_t capacity,
                                    uint32_t mode,
                                    subetha_handle *out_producer,
                                    subetha_handle *out_consumer);

/**
 * Attach to a file-backed Lamport ring another process created at `path`
 * and receive both handles; a process plays one side and destroys the
 * other. One producer and one consumer across every process is the
 * caller's contract.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out_producer` and
 * `out_consumer` are valid pointers.
 */
int32_t subetha_lamport_open_pair(const char *path,
                                  uint32_t expected_capacity,
                                  uint32_t mode,
                                  subetha_handle *out_producer,
                                  subetha_handle *out_consumer);

/**
 * Push `len` bytes without waiting; the slot semantics are the adaptive
 * ring's. `SUBETHA_E_RING_FULL` when there is no room.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_lamport_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Push `count` payloads of `len` bytes each, the first at `items` and
 * each next one `stride` bytes on, under one handle lookup and one panic
 * guard. The batch contract is `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_lamport_try_push_many(subetha_handle handle,
                                      const uint8_t *items,
                                      size_t stride,
                                      size_t len,
                                      size_t count,
                                      size_t *out_done);

/**
 * Pop up to `count` slots, the first written at `out` and each next one
 * `stride` bytes on. The batch contract is
 * `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_lamport_try_pop_many(subetha_handle handle,
                                     uint8_t *out,
                                     size_t stride,
                                     size_t count,
                                     size_t *out_done);

/**
 * Pop one slot into `out` without waiting; `cap` must be at least
 * `SUBETHA_RING_SLOT_BYTES`. `SUBETHA_E_RING_EMPTY` when there is nothing
 * to take.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_lamport_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * A snapshot of the producer side into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_lamport_producer_read_stats(subetha_handle handle,
                                            struct subetha_lamport_producer_stats *out);

/**
 * A snapshot of the consumer side into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_lamport_consumer_read_stats(subetha_handle handle,
                                            struct subetha_lamport_consumer_stats *out);

/**
 * Remove the ring file a Lamport pair was created at. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_lamport_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the laned map under `dir`: `lanes` trees of `nodes_per_lane`
 * nodes each for `key_size`-byte keys and `value_size`-byte values, one
 * epoch table they share holding `max_pins` pins, and the claims table
 * beside them.
 *
 * # Safety
 * `dir` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_laned_map_create(const char *dir,
                                 uint32_t lanes,
                                 uint64_t nodes_per_lane,
                                 size_t key_size,
                                 size_t value_size,
                                 uint64_t max_pins,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the laned map another process created under `dir`; its files
 * must exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `dir` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_laned_map_open(const char *dir,
                               uint32_t lanes,
                               uint64_t nodes_per_lane,
                               size_t key_size,
                               size_t value_size,
                               uint64_t max_pins,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Claim any free lane, for a statement inserting keys that do not yet
 * exist, and report which into `out_lane`. `SUBETHA_E_WOULD_BLOCK` when
 * every lane is held.
 *
 * # Safety
 * `out_lane` is a valid pointer.
 */
int32_t subetha_laned_map_claim_lane(subetha_handle handle, uint32_t *out_lane);

/**
 * Claim the lane that owns `key`, for a statement that must remove or
 * rewrite a key that already exists, and report which into `out_lane`.
 * `SUBETHA_E_MAP_KEY_ABSENT` when no lane holds it and
 * `SUBETHA_E_WOULD_BLOCK` when its lane is held by another statement,
 * which the caller retries rather than writing elsewhere, because
 * elsewhere is a different tree.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out_lane` is a valid pointer.
 */
int32_t subetha_laned_map_claim_lane_for(subetha_handle handle,
                                         const uint8_t *key,
                                         size_t key_len,
                                         uint32_t *out_lane);

/**
 * Release a lane this process claimed, so another statement may take it.
 */
int32_t subetha_laned_map_release_lane(subetha_handle handle, uint32_t lane);

/**
 * Release the lanes of holders whose process is gone, reporting how many
 * came back through `out_reaped` when that is not null.
 *
 * # Safety
 * `out_reaped` is null or a valid pointer.
 */
int32_t subetha_laned_map_reap_dead_claims(subetha_handle handle, uint64_t *out_reaped);

/**
 * Make `value` current at `key` in `lane`, at a fresh epoch. The caller
 * holds `lane`.
 *
 * # Safety
 * `key` points to `key_len` readable bytes and `value` to `value_len`.
 */
int32_t subetha_laned_map_insert(subetha_handle handle,
                                 uint32_t lane,
                                 const uint8_t *key,
                                 size_t key_len,
                                 const uint8_t *value,
                                 size_t value_len);

/**
 * Stamp `key` superseded in `lane` at a fresh epoch, writing what was
 * current into `out`. `SUBETHA_E_WRONG_KIND` names the lane that holds
 * the key when this one does not.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_laned_map_remove(subetha_handle handle,
                                 uint32_t lane,
                                 const uint8_t *key,
                                 size_t key_len,
                                 uint8_t *out,
                                 size_t cap,
                                 size_t *out_len);

/**
 * The value current at `key` into `out`, from whichever lane holds it.
 * `SUBETHA_E_MAP_KEY_ABSENT` when no lane does.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_laned_map_get(subetha_handle handle,
                              const uint8_t *key,
                              size_t key_len,
                              uint8_t *out,
                              size_t cap,
                              size_t *out_len);

/**
 * The lane holding `key` into `out_lane`.
 * `SUBETHA_E_MAP_KEY_ABSENT` when no lane does.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out_lane` is a valid pointer.
 */
int32_t subetha_laned_map_lane_of(subetha_handle handle,
                                  const uint8_t *key,
                                  size_t key_len,
                                  uint32_t *out_lane);

/**
 * One page of the entries current at one moment, merged across every
 * lane in key order, packed into `out` as key bytes then value bytes per
 * entry.
 *
 * `after` is null to start, or the frontier of the previous page to
 * resume strictly past it. `out_frontier` receives the frontier this page
 * reached and `out_has_frontier` whether there is one: no frontier means
 * every lane reached the end and the walk is done. Rows beyond the
 * frontier are withheld even where a lane already walked them, because a
 * lane that stopped earlier may still hold a smaller key.
 *
 * # Safety
 * `after` and `out_frontier` are null or point to `key_size` bytes;
 * `out` points to `cap` writable bytes; the out pointers are valid.
 */
int32_t subetha_laned_map_range(subetha_handle handle,
                                const uint8_t *after,
                                size_t limit,
                                uint8_t *out,
                                size_t cap,
                                size_t *out_len,
                                size_t *out_count,
                                uint8_t *out_frontier,
                                bool *out_has_frontier);

/**
 * Drop every tombstone no pin can reach, in every lane, reporting how
 * many went through `out_freed` when that is not null.
 *
 * # Safety
 * `out_freed` is null or a valid pointer.
 */
int32_t subetha_laned_map_sweep(subetha_handle handle, uint64_t *out_freed);

/**
 * Undo every stamp at `epoch` in every lane, for an epoch whose holder
 * died part way through a compound write. Reports the entries touched
 * through `out_touched` when that is not null.
 *
 * # Safety
 * `out_touched` is null or a valid pointer.
 */
int32_t subetha_laned_map_void_epoch(subetha_handle handle, uint64_t epoch, uint64_t *out_touched);

/**
 * Push every lane's dirty pages to disk, returning when they are durable.
 */
int32_t subetha_laned_map_flush(subetha_handle handle);

/**
 * A snapshot of the map into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_laned_map_read_stats(subetha_handle handle, struct subetha_laned_map_stats *out);

/**
 * Obtain the election at `path`: an empty one is initialized when the
 * file does not exist, an existing one is attached with its leader and
 * term in place.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_leader_create(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Attach to the election another process created at `path`; the file
 * must exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_leader_open(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Truncate the file at `path` and lay out an election with no leader,
 * discarding whatever a live one holds.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_leader_reset(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Try to take leadership for `pid`, and write whether it was taken into
 * `out_claimed`. It is taken when nobody leads, when `pid` is below the
 * current leader's and so preempts it, or when the leader's heartbeat
 * has fallen more than `grace_epochs` behind the global epoch.
 *
 * # Safety
 * `out_claimed` is a valid pointer.
 */
int32_t subetha_leader_try_claim(subetha_handle handle,
                                 uint32_t pid,
                                 uint64_t grace_epochs,
                                 bool *out_claimed);

/**
 * Refresh the leader's heartbeat, and write whether `pid` still leads
 * into `out_still_leader`. A leader calls this faster than the epoch is
 * ticked, or the grace window expires under it.
 *
 * # Safety
 * `out_still_leader` is null or a valid pointer.
 */
int32_t subetha_leader_beat(subetha_handle handle, uint32_t pid, bool *out_still_leader);

/**
 * Give up leadership, and write whether `pid` was leading into
 * `out_stepped`. A process shutting down cleanly does this so the next
 * leader takes over at once rather than waiting out the grace window.
 *
 * # Safety
 * `out_stepped` is null or a valid pointer.
 */
int32_t subetha_leader_step_down(subetha_handle handle, uint32_t pid, bool *out_stepped);

/**
 * Advance the global epoch and write it into `out_epoch`. The grace
 * window is measured against this, so a departed leader is only noticed
 * once something ticks it.
 *
 * # Safety
 * `out_epoch` is null or a valid pointer.
 */
int32_t subetha_leader_tick_epoch(subetha_handle handle, uint64_t *out_epoch);

/**
 * A snapshot of the election into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_leader_read_stats(subetha_handle handle, struct subetha_leader_stats *out);

/**
 * Write the election's page to its file.
 */
int32_t subetha_leader_flush(subetha_handle handle);

/**
 * Remove the election's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_leader_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the list at `path` holding up to `capacity` nodes, the sentinel
 * head among them, at `layout`: an empty one is initialized when the file
 * does not exist, an existing one is attached with its nodes in place. A
 * file built with another capacity or layout is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_list_create(const char *path,
                            uint32_t capacity,
                            const struct subetha_element_layout *layout,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to the list another process created at `path`; the file must
 * exist and already hold the sentinel head. `SUBETHA_E_RING_IO` names an
 * absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_list_open(const char *path,
                          uint32_t capacity,
                          const struct subetha_element_layout *layout,
                          uint32_t mode,
                          subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty list there,
 * invalidating every index other handles hold. On Windows the file must
 * not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_list_reset(const char *path,
                           uint32_t capacity,
                           const struct subetha_element_layout *layout,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Add `data`, exactly `element_size` bytes, at the front and write the
 * new node's index into `out_index`. `SUBETHA_E_RING_FULL` when every
 * slot is taken.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_index` is a valid pointer.
 */
int32_t subetha_list_push_front(subetha_handle handle,
                                const uint8_t *data,
                                size_t len,
                                uint32_t *out_index);

/**
 * Add `data`, exactly `element_size` bytes, at the back and write the new
 * node's index into `out_index`.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_index` is a valid pointer.
 */
int32_t subetha_list_push_back(subetha_handle handle,
                               const uint8_t *data,
                               size_t len,
                               uint32_t *out_index);

/**
 * Remove the first node, its value into `out`, at least `element_size`
 * bytes, and its length into `out_len`. `SUBETHA_E_RING_EMPTY` when the
 * list holds no node.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_list_pop_front(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Remove the last node, its value into `out`, at least `element_size`
 * bytes.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_list_pop_back(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Remove the node at `index`, wherever it sits, its value into `out`, at
 * least `element_size` bytes. `SUBETHA_E_OUT_OF_BOUNDS` for the sentinel
 * head, the nil index, or an index past the capacity.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_list_remove(subetha_handle handle,
                            uint32_t index,
                            uint8_t *out,
                            size_t cap,
                            size_t *out_len);

/**
 * Copy the value at `index` into `out`, at least `element_size` bytes.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_list_get(subetha_handle handle,
                         uint32_t index,
                         uint8_t *out,
                         size_t cap,
                         size_t *out_len);

/**
 * Overwrite the value at `index` with `data`, exactly `element_size`
 * bytes, leaving the node where it is.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_list_set(subetha_handle handle, uint32_t index, const uint8_t *data, size_t len);

/**
 * The first node's index into `out`, or `SUBETHA_LIST_HEAD_INDEX` when
 * the list holds no node.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_list_first(subetha_handle handle, uint32_t *out);

/**
 * The last node's index into `out`, or `SUBETHA_LIST_HEAD_INDEX` when the
 * list holds no node.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_list_last(subetha_handle handle, uint32_t *out);

/**
 * The index after `index` into `out`, which is
 * `SUBETHA_LIST_HEAD_INDEX` once the walk has passed the last node.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_list_next(subetha_handle handle, uint32_t index, uint32_t *out);

/**
 * The index before `index` into `out`, which is
 * `SUBETHA_LIST_HEAD_INDEX` once the walk has passed the first node.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_list_prev(subetha_handle handle, uint32_t index, uint32_t *out);

/**
 * A snapshot of the list into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_list_read_stats(subetha_handle handle, struct subetha_list_stats *out);

/**
 * Write the list's dirty pages to its file.
 */
int32_t subetha_list_flush(subetha_handle handle);

/**
 * Remove the list file at `path`. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_list_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Create a locale-adaptive ring under `base_path`, or attach to one that
 * exists there. The ring starts in anonymous memory. Stamps come from the
 * host's default source or not at all, and the ring declares no
 * contract.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_locale_ring_create(const char *base_path,
                                   uint32_t max_producers,
                                   uint32_t max_consumers,
                                   uint32_t capacity,
                                   const struct subetha_ring_options *options,
                                   subetha_handle *out);

/**
 * Attach to the locale ring another process created under `base_path`
 * with the same counts and capacity, without re-initializing any
 * backing; the locale tag, the generation and the file and shared-memory
 * backings are the creator's, and this handle's anonymous backing is its
 * own. `stamps` in the options must match the creator's choice.
 * `SUBETHA_E_RING_IO` names an absent backing and
 * `SUBETHA_E_RING_LAYOUT_MISMATCH` one of another shape.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_locale_ring_open(const char *base_path,
                                 uint32_t max_producers,
                                 uint32_t max_consumers,
                                 uint32_t capacity,
                                 const struct subetha_ring_options *options,
                                 subetha_handle *out);

/**
 * Register as a producer on all three backings, so the live one always
 * has the registration.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_locale_ring_register_producer(subetha_handle handle, uint32_t *out);

/**
 * Register as a consumer on all three backings.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_locale_ring_register_consumer(subetha_handle handle, uint32_t *out);

/**
 * Push into the live locale without waiting; the slot semantics are
 * `subetha_ring_try_push`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_locale_ring_try_push(subetha_handle handle,
                                     uint32_t producer_id,
                                     const uint8_t *data,
                                     size_t len);

/**
 * Pop from the live locale without waiting; the slot semantics are
 * `subetha_ring_try_pop`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_locale_ring_try_pop(subetha_handle handle,
                                    uint32_t consumer_id,
                                    uint8_t *out,
                                    size_t cap,
                                    size_t *out_len);

/**
 * Push, parking until there is room or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_locale_ring_push_wait(subetha_handle handle,
                                      uint32_t producer_id,
                                      const uint8_t *data,
                                      size_t len,
                                      int64_t timeout_ms);

/**
 * Pop, parking until an item arrives or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_locale_ring_pop_wait(subetha_handle handle,
                                     uint32_t consumer_id,
                                     uint8_t *out,
                                     size_t cap,
                                     size_t *out_len,
                                     int64_t timeout_ms);

/**
 * Move the ring to `locale`, one of the `SUBETHA_LOCALE_` constants,
 * carrying the items in flight across. A migration to the live locale
 * is a no-op.
 */
int32_t subetha_locale_ring_migrate(subetha_handle handle, uint32_t locale);

/**
 * Managed mode: tell the sidecar the locale the caller wants; it migrates
 * once its hysteresis has elapsed. `SUBETHA_E_NOT_SUPPORTED` in strict
 * mode, where `subetha_locale_ring_migrate` is the call.
 */
int32_t subetha_locale_ring_request(subetha_handle handle, uint32_t locale);

/**
 * Set the ordering mode of a stamped locale ring on all three backings;
 * the contract is `subetha_ring_set_ordering_mode`'s.
 */
int32_t subetha_locale_ring_set_ordering_mode(subetha_handle handle, uint32_t mode);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_locale_ring_read_stats(subetha_handle handle, struct subetha_locale_stats *out);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the
 * ring and parks again unless it finds what it waited for.
 */
int32_t subetha_locale_ring_wake_all(subetha_handle handle);

/**
 * Remove what a locale ring under `base_path` leaves behind. The ring
 * removes its tag, its generation and its file backing when its handle is
 * destroyed, in every process that holds one, so a process that keeps
 * its handle keeps its mapping while the files are already gone; this
 * call removes the two wakers and any file a peer's destroy could not,
 * for the creation hint `max_producers`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_locale_ring_unlink(const char *base_path,
                                   uint32_t max_producers,
                                   struct subetha_unlink_report *report);

/**
 * Create a blocking MPMC grid in anonymous memory: `n_producers` rings of
 * `capacity` slots partitioned across `n_consumers` consumers, one handle
 * each into `out_producers` and `out_consumers`.
 *
 * # Safety
 * `out_producers` points to `n_producers` writable handles and
 * `out_consumers` to `n_consumers`.
 */
int32_t subetha_mpmc_create_anon_grid(uint32_t n_producers,
                                      uint32_t n_consumers,
                                      uint32_t capacity,
                                      uint32_t mode,
                                      subetha_handle *out_producers,
                                      subetha_handle *out_consumers);

/**
 * Create a file-backed blocking MPMC grid under `path_prefix`.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `out_producers` points to
 * `n_producers` writable handles and `out_consumers` to `n_consumers`.
 */
int32_t subetha_mpmc_create_grid(const char *path_prefix,
                                 uint32_t n_producers,
                                 uint32_t n_consumers,
                                 uint32_t capacity,
                                 uint32_t mode,
                                 subetha_handle *out_producers,
                                 subetha_handle *out_consumers);

/**
 * Attach to a file-backed blocking MPMC grid another process created
 * under `path_prefix`, with the counts and capacity it was created with.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `out_producers` points to
 * `n_producers` writable handles and `out_consumers` to `n_consumers`.
 */
int32_t subetha_mpmc_open_grid(const char *path_prefix,
                               uint32_t n_producers,
                               uint32_t n_consumers,
                               uint32_t expected_capacity,
                               uint32_t mode,
                               subetha_handle *out_producers,
                               subetha_handle *out_consumers);

/**
 * Push `len` bytes into this producer's ring without waiting; the slot
 * semantics are the adaptive ring's. `SUBETHA_E_RING_FULL` when there is
 * no room.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_mpmc_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Push, parking until this producer's ring has room or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_mpmc_push_wait(subetha_handle handle,
                               const uint8_t *data,
                               size_t len,
                               int64_t timeout_ms);

/**
 * Push `count` payloads of `len` bytes each into this producer's ring,
 * the first at `items` and each next one `stride` bytes on, under one
 * handle lookup and one panic guard. The batch contract is
 * `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_mpmc_try_push_many(subetha_handle handle,
                                   const uint8_t *items,
                                   size_t stride,
                                   size_t len,
                                   size_t count,
                                   size_t *out_done);

/**
 * Pop up to `count` slots from the grid, the first written at `out` and
 * each next one `stride` bytes on. The batch contract is
 * `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_mpmc_try_pop_many(subetha_handle handle,
                                  uint8_t *out,
                                  size_t stride,
                                  size_t count,
                                  size_t *out_done);

/**
 * Pop one slot from whichever ring of this consumer's subset has one,
 * without waiting; `cap` must be at least `SUBETHA_RING_SLOT_BYTES`.
 * `SUBETHA_E_RING_EMPTY` when the subset is empty.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_mpmc_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Pop, parking until a producer of this consumer's subset pushes or
 * `timeout_ms` elapses; the waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_mpmc_pop_wait(subetha_handle handle,
                              uint8_t *out,
                              size_t cap,
                              size_t *out_len,
                              int64_t timeout_ms);

/**
 * A snapshot of one producer into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_mpmc_producer_read_stats(subetha_handle handle,
                                         struct subetha_mpmc_producer_stats *out);

/**
 * A snapshot of one consumer into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_mpmc_consumer_read_stats(subetha_handle handle,
                                         struct subetha_mpmc_consumer_stats *out);

/**
 * Remove every file a file-backed blocking MPMC grid under `path_prefix`
 * names, for the counts it was created with. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_mpmc_unlink(const char *path_prefix,
                            uint32_t n_producers,
                            uint32_t n_consumers,
                            struct subetha_unlink_report *report);

/**
 * Create the bit vector at `path`, or attach to one already there with
 * the same capacity. Every bit starts clear.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_bit_vec_create(const char *path,
                               uint64_t capacity_bits,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an all-clear vector,
 * discarding the bits live peers share. For a caller that knows it owns
 * the path.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_bit_vec_reset(const char *path,
                              uint64_t capacity_bits,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Attach to a vector another process created at `path`. The file must
 * exist and must be at least as large as `capacity_bits` needs, or the
 * answer is `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_bit_vec_open(const char *path,
                             uint64_t capacity_bits,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Set bit `index`, writing whether it had already been set into `was`.
 *
 * The previous value is what makes this usable as a claim: two processes
 * setting the same bit both succeed, and exactly one is told it was the
 * one that changed it. `was` may be null when that is not wanted.
 *
 * This and `subetha_bit_vec_clear` report the value from before the
 * call. `subetha_bit_vec_toggle` reports the value after it.
 *
 * # Safety
 * `was` is null or a valid pointer.
 */
int32_t subetha_bit_vec_set(subetha_handle handle, uint64_t index, bool *was);

/**
 * Clear bit `index`, writing whether it had been set into `was`.
 *
 * # Safety
 * `was` is null or a valid pointer.
 */
int32_t subetha_bit_vec_clear(subetha_handle handle, uint64_t index, bool *was);

/**
 * Flip bit `index`, writing the value it now holds into `now`.
 *
 * The parameter is `now` and not `was` because this one differs from its
 * two siblings: `subetha_bit_vec_set` and `subetha_bit_vec_clear` report
 * the value from before the call, and this reports the value after it.
 * A caller using all three must not read them alike.
 *
 * # Safety
 * `now` is null or a valid pointer.
 */
int32_t subetha_bit_vec_toggle(subetha_handle handle, uint64_t index, bool *now);

/**
 * Read bit `index` into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_bit_vec_get(subetha_handle handle, uint64_t index, bool *out);

/**
 * Set every bit from `lo` up to but not including `hi`.
 *
 * The range is not one atomic step: a reader during it sees some of the
 * bits changed and some not. Only the individual bits are atomic.
 */
int32_t subetha_bit_vec_set_range(subetha_handle handle, uint64_t lo, uint64_t hi);

/**
 * Clear every bit from `lo` up to but not including `hi`, with the same
 * caution about the range not being one step.
 */
int32_t subetha_bit_vec_clear_range(subetha_handle handle, uint64_t lo, uint64_t hi);

/**
 * Set every bit in the vector.
 */
int32_t subetha_bit_vec_set_all(subetha_handle handle);

/**
 * Clear every bit in the vector.
 */
int32_t subetha_bit_vec_clear_all(subetha_handle handle);

/**
 * Push the mapped bytes to disk and wait for them.
 */
int32_t subetha_bit_vec_flush(subetha_handle handle);

/**
 * Ask for the mapped bytes to reach disk without waiting.
 */
int32_t subetha_bit_vec_flush_async(subetha_handle handle);

/**
 * A snapshot of this vector into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_bit_vec_read_stats(subetha_handle handle, struct subetha_bit_vec_stats *out);

/**
 * Create the sketch at `path`, or attach to one already there with the
 * same precision.
 *
 * Precision decides both the accuracy and the size: the file holds two
 * to the `precision` registers, so raising it costs memory and lowers
 * the error.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_hll_create(const char *path,
                           uint32_t precision,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Attach to a sketch another process created at `path`. The file must
 * exist and match `precision`, or the answer is
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_hll_open(const char *path, uint32_t precision, uint32_t mode, subetha_handle *out);

/**
 * Record that `item` was seen.
 *
 * Inserting the same item again changes nothing, which is what makes the
 * estimate a distinct count rather than a total. There is no answer to
 * return: the sketch cannot tell a caller whether this item was new.
 *
 * # Safety
 * `item` points to `len` readable bytes.
 */
int32_t subetha_hll_insert(subetha_handle handle, const uint8_t *item, size_t len);

/**
 * The number of distinct items the registers imply, into `out`.
 *
 * An estimate. A sketch that has seen nothing answers zero, and one that
 * has seen a great many answers a number near but rarely equal to the
 * truth. Another process inserting during this read moves it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_hll_estimate(subetha_handle handle, uint64_t *out);

/**
 * Return every register to zero, so the sketch reads as having seen
 * nothing.
 *
 * This is visible to every process sharing the file, not only to this
 * handle: a peer mid-insert keeps whatever it writes next, and the
 * counts before the reset are gone for all of them.
 */
int32_t subetha_hll_reset(subetha_handle handle);

/**
 * Push the mapped bytes to disk and wait for them.
 */
int32_t subetha_hll_flush(subetha_handle handle);

/**
 * Ask for the mapped bytes to reach disk without waiting.
 */
int32_t subetha_hll_flush_async(subetha_handle handle);

/**
 * A snapshot of this sketch into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_hll_read_stats(subetha_handle handle, struct subetha_hll_stats *out);

/**
 * Create a graph under `base_path`, holding up to `max_nodes` nodes and
 * `max_edges` edges whose values are the given sizes.
 *
 * Two files are made beside that prefix: the nodes and the edges.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_graph_create(const char *base_path,
                             uint64_t max_nodes,
                             uint64_t max_edges,
                             uint64_t node_value_size,
                             uint64_t edge_value_size,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Attach to a graph another process created under `base_path`, with the
 * same shape.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_graph_open(const char *base_path,
                           uint64_t max_nodes,
                           uint64_t max_edges,
                           uint64_t node_value_size,
                           uint64_t edge_value_size,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Add a node carrying `value`, writing the index it took into `node_out`.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `node_out` is valid.
 */
int32_t subetha_graph_add_node(subetha_handle handle,
                               const uint8_t *value,
                               uint64_t value_len,
                               uint32_t *node_out);

/**
 * Add an edge from `src` to `dst` carrying `value`, writing the index it
 * took into `edge_out`. Both nodes must be live.
 *
 * The edge goes at the front of the source's chain, so a walk sees the
 * reverse of the order edges were added. No order is promised.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `edge_out` is valid.
 */
int32_t subetha_graph_add_edge(subetha_handle handle,
                               uint32_t src,
                               uint32_t dst,
                               const uint8_t *value,
                               uint64_t value_len,
                               uint32_t *edge_out);

/**
 * Read the value node `node` carries into `value_out`.
 *
 * # Safety
 * `value_out` points to at least `value_len` bytes.
 */
int32_t subetha_graph_node_value(subetha_handle handle,
                                 uint32_t node,
                                 uint8_t *value_out,
                                 uint64_t value_len);

/**
 * Read the value edge `edge` carries into `value_out`, and write the node
 * it points at into `dst_out`.
 *
 * # Safety
 * `value_out` points to at least `value_len` bytes; `dst_out` is valid.
 */
int32_t subetha_graph_edge_target(subetha_handle handle,
                                  uint32_t edge,
                                  uint8_t *value_out,
                                  uint64_t value_len,
                                  uint32_t *dst_out);

/**
 * How many edges leave `node`, into `degree_out`.
 *
 * # Safety
 * `degree_out` is a valid pointer.
 */
int32_t subetha_graph_out_degree(subetha_handle handle, uint32_t node, uint32_t *degree_out);

/**
 * The first edge leaving `node`, into `edge_out`, or `SUBETHA_GRAPH_NIL`
 * where it has none.
 *
 * # Safety
 * `edge_out` is a valid pointer.
 */
int32_t subetha_graph_first_edge(subetha_handle handle, uint32_t node, uint32_t *edge_out);

/**
 * The edge after `edge` in the same source's chain, into `edge_out`, or
 * `SUBETHA_GRAPH_NIL` at the end.
 *
 * # Safety
 * `edge_out` is a valid pointer.
 */
int32_t subetha_graph_next_edge(subetha_handle handle, uint32_t edge, uint32_t *edge_out);

/**
 * Remove `edge` from `src`'s chain and free its slot.
 *
 * Refused where `src` does not own that edge, since freeing it would
 * leave whichever chain does own it pointing at a free slot.
 */
int32_t subetha_graph_remove_edge(subetha_handle handle, uint32_t src, uint32_t edge);

/**
 * Read the graph's shape and occupancy into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_graph_read_stats(subetha_handle handle, struct subetha_graph_stats *out);

/**
 * Push both of the graph's files to disk and wait for them.
 */
int32_t subetha_graph_flush(subetha_handle handle);

/**
 * Create an LRU cache under `base_path`, holding `capacity` entries of
 * `key_size` and `value_size` bytes.
 *
 * Two files are made beside that prefix: the index and the recency order.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_lru_create(const char *base_path,
                           uint32_t capacity,
                           uint64_t key_size,
                           uint64_t value_size,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Attach to a cache another process created under `base_path`. The files
 * must exist and have been built with the same shape.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_lru_open(const char *base_path,
                         uint32_t capacity,
                         uint64_t key_size,
                         uint64_t value_size,
                         uint32_t mode,
                         subetha_handle *out);

/**
 * Store `value` under `key` at the most recent end, evicting the least
 * recent entry first where the cache is full.
 *
 * `replaced` is set to true where the key was already present and its
 * value overwritten. It may be null when the caller does not care.
 *
 * # Safety
 * `key` and `value` point to at least their declared sizes; `replaced` is
 * null or a valid pointer.
 */
int32_t subetha_lru_put(subetha_handle handle,
                        const uint8_t *key,
                        uint64_t key_len,
                        const uint8_t *value,
                        uint64_t value_len,
                        bool *replaced);

/**
 * Read the value for `key` into `value_out` without changing its recency.
 *
 * `found` is set to whether the cache held it. When it did not, nothing
 * is written to `value_out`.
 *
 * # Safety
 * `key` and `value_out` point to at least their declared sizes; `found`
 * is a valid pointer.
 */
int32_t subetha_lru_get(subetha_handle handle,
                        const uint8_t *key,
                        uint64_t key_len,
                        uint8_t *value_out,
                        uint64_t value_len,
                        bool *found);

/**
 * Read the value for `key` into `value_out` and move it to the most
 * recent end.
 *
 * # Safety
 * As [`subetha_lru_get`].
 */
int32_t subetha_lru_get_and_touch(subetha_handle handle,
                                  const uint8_t *key,
                                  uint64_t key_len,
                                  uint8_t *value_out,
                                  uint64_t value_len,
                                  bool *found);

/**
 * Whether the cache holds `key`, without changing its recency.
 *
 * # Safety
 * `key` points to at least `key_len` bytes; `present` is a valid pointer.
 */
int32_t subetha_lru_contains(subetha_handle handle,
                             const uint8_t *key,
                             uint64_t key_len,
                             bool *present);

/**
 * Move `key` to the most recent end without reading its value.
 *
 * `promoted` is set to whether the cache held it, and may be null.
 *
 * # Safety
 * `key` points to at least `key_len` bytes; `promoted` is null or valid.
 */
int32_t subetha_lru_touch(subetha_handle handle,
                          const uint8_t *key,
                          uint64_t key_len,
                          bool *promoted);

/**
 * Drop `key`, writing its value into `value_out`.
 *
 * `removed` is set to whether the cache held it. When it did not,
 * nothing is written to `value_out`.
 *
 * # Safety
 * `key` and `value_out` point to at least their declared sizes;
 * `removed` is a valid pointer.
 */
int32_t subetha_lru_remove(subetha_handle handle,
                           const uint8_t *key,
                           uint64_t key_len,
                           uint8_t *value_out,
                           uint64_t value_len,
                           bool *removed);

/**
 * Drop the least recently used entry, writing its key and value out.
 *
 * `evicted` is set to whether there was anything to evict.
 *
 * # Safety
 * `key_out` and `value_out` point to at least their declared sizes;
 * `evicted` is a valid pointer.
 */
int32_t subetha_lru_evict_oldest(subetha_handle handle,
                                 uint8_t *key_out,
                                 uint64_t key_len,
                                 uint8_t *value_out,
                                 uint64_t value_len,
                                 bool *evicted);

/**
 * Read the cache's shape and occupancy into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_lru_read_stats(subetha_handle handle, struct subetha_lru_stats *out);

/**
 * Push both of the cache's files to disk and wait for them.
 */
int32_t subetha_lru_flush(subetha_handle handle);

/**
 * The nil value.
 */
uint64_t subetha_nan_nil(void);

/**
 * Pack a double.
 *
 * A NaN going in comes back as the one canonical quiet NaN, so its bits
 * can never be mistaken for a boxed value. That means a caller cannot
 * round-trip a particular NaN payload through here: every NaN is the
 * same NaN afterwards.
 */
uint64_t subetha_nan_from_f64(double value);

/**
 * Pack a signed 32-bit integer.
 */
uint64_t subetha_nan_from_i32(int32_t value);

/**
 * Pack an unsigned 32-bit integer.
 */
uint64_t subetha_nan_from_u32(uint32_t value);

/**
 * Pack a boolean.
 */
uint64_t subetha_nan_from_bool(bool value);

/**
 * Which of the `SUBETHA_NAN_` kinds `value` holds.
 */
uint32_t subetha_nan_type(uint64_t value);

/**
 * Read `value` as a double, into `out`. Answers false and writes nothing
 * when it holds something else.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
bool subetha_nan_as_f64(uint64_t value, double *out);

/**
 * Read `value` as a signed 32-bit integer, into `out`. Answers false and
 * writes nothing when it holds something else.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
bool subetha_nan_as_i32(uint64_t value, int32_t *out);

/**
 * Read `value` as an unsigned 32-bit integer, into `out`. Answers false
 * and writes nothing when it holds something else.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
bool subetha_nan_as_u32(uint64_t value, uint32_t *out);

/**
 * Read `value` as a boolean, into `out`. Answers false and writes
 * nothing when it holds something else.
 *
 * Note that a false answer and a stored `false` are different things:
 * the answer says whether the word held a boolean at all, and `out`
 * says which boolean it was.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
bool subetha_nan_as_bool(uint64_t value, bool *out);

/**
 * Whether `value` is nil.
 */
bool subetha_nan_is_nil(uint64_t value);

/**
 * Pack an index and a tag into one word, as a two-level value: the outer
 * tag says it is a tagged pointer, and `tag_bits` of the payload hold
 * the inner tag with the rest holding the index.
 *
 * `tag_bits` is not stored in the word. A caller reading it back must
 * pass the same width it wrote, and nothing here can check that: a wrong
 * width yields a different index and tag rather than a refusal.
 *
 * Answers false and writes nothing when `tag_bits` is above
 * `SUBETHA_NAN_MAX_TAG_BITS`, or when either component does not fit the
 * width it leaves. A component is refused rather than truncated, since a
 * silently narrowed index points somewhere else.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
bool subetha_nan_from_tagged(uint32_t index, uint32_t tag, uint32_t tag_bits, uint64_t *out);

/**
 * Read `value` as an index and a tag at `tag_bits`.
 *
 * Answers false and writes nothing when the word is not a tagged
 * pointer, or `tag_bits` is above `SUBETHA_NAN_MAX_TAG_BITS`.
 *
 * # Safety
 * `index_out` and `tag_out` are each null or a valid pointer.
 */
bool subetha_nan_as_tagged(uint64_t value,
                           uint32_t tag_bits,
                           uint32_t *index_out,
                           uint32_t *tag_out);

/**
 * Whether `value` is a tagged pointer.
 *
 * A word packed by `subetha_nan_from_tagged` reads as
 * `SUBETHA_NAN_RESERVED` from `subetha_nan_type`, which knows only the
 * one-level form. This is how the two-level form is recognized.
 */
bool subetha_nan_is_tagged(uint64_t value);

/**
 * Create a tile at `path` whose lanes carry `payload_size` bytes, at most
 * `SUBETHA_TILE_MAX_PAYLOAD`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_tile_create(const char *path,
                            uint64_t payload_size,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to a tile another process created. Its header must agree about
 * the payload size, since otherwise the two callers would disagree about
 * how many of a lane's bytes mean anything.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_tile_open(const char *path,
                          uint64_t payload_size,
                          uint32_t mode,
                          subetha_handle *out);

/**
 * Publish `value` at `version`, writing the lane it took into `lane_out`.
 *
 * `version` must not be zero. The payload reaches the lane before the
 * version does, so a reader that sees the version sees the whole payload.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `lane_out` is valid.
 */
int32_t subetha_tile_insert(subetha_handle handle,
                            uint64_t version,
                            const uint8_t *value,
                            uint64_t value_len,
                            uint32_t *lane_out);

/**
 * Free `lane`.
 *
 * The version is cleared before the lane is released, so a lane reclaimed
 * afterwards cannot be seen through its predecessor's version.
 */
int32_t subetha_tile_remove(subetha_handle handle, uint32_t lane);

/**
 * Read the payload at `lane` into `value_out` and its version into
 * `version_out`.
 *
 * `present` is set to whether the lane holds a published value. A lane
 * that is claimed but not yet published reads as absent, which is what
 * keeps a half-written payload from being seen.
 *
 * # Safety
 * `value_out` points to at least `value_len` bytes; `version_out` and
 * `present` are valid pointers.
 */
int32_t subetha_tile_at(subetha_handle handle,
                        uint32_t lane,
                        uint8_t *value_out,
                        uint64_t value_len,
                        uint64_t *version_out,
                        bool *present);

/**
 * A bit per lane visible at `snapshot`, into `mask_out`.
 *
 * Bit `n` is set when lane `n` is occupied, published, and carries a
 * version no greater than `snapshot`. The boundary is inclusive.
 *
 * # Safety
 * `mask_out` is a valid pointer.
 */
int32_t subetha_tile_visible_mask(subetha_handle handle, uint64_t snapshot, uint32_t *mask_out);

/**
 * How many lanes `snapshot` sees, into `count_out`.
 *
 * # Safety
 * `count_out` is a valid pointer.
 */
int32_t subetha_tile_visible_count(subetha_handle handle, uint64_t snapshot, uint32_t *count_out);

/**
 * Read the tile's shape and occupancy into `out`.
 *
 * `len` counts occupied lanes whether or not any snapshot can see them,
 * so it can exceed what `subetha_tile_visible_count` answers.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_tile_read_stats(subetha_handle handle, struct subetha_tile_stats *out);

/**
 * Push the tile to disk and wait for it.
 */
int32_t subetha_tile_flush(subetha_handle handle);

/**
 * The four-byte prefix `value` would carry, without storing anything.
 *
 * Lets a caller compute a query prefix once and test many pointers
 * against it with `subetha_umbra_matches_prefix`.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `prefix_out` is valid.
 */
int32_t subetha_umbra_content_prefix(const uint8_t *value,
                                     uint64_t value_len,
                                     uint32_t *prefix_out);

/**
 * A pointer at nothing, into `out`.
 *
 * # Safety
 * `out` points to at least `SUBETHA_UMBRA_BYTES` writable bytes.
 */
int32_t subetha_umbra_nil(uint8_t *out);

/**
 * Store `value` in the region `region` names and write a pointer at it
 * into `out`, taking the prefix from the value's own first four bytes.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `out` points to at least
 * `SUBETHA_UMBRA_BYTES` writable bytes.
 */
int32_t subetha_umbra_allocate(subetha_handle region,
                               const uint8_t *value,
                               uint64_t value_len,
                               uint8_t *out);

/**
 * Store `value` and write a pointer at it carrying `prefix`, rather than
 * one taken from the bytes.
 *
 * For a record opening with a header its first four bytes share with
 * every other record, a caller-chosen prefix is what makes the filter
 * worth having at all.
 *
 * # Safety
 * As [`subetha_umbra_allocate`].
 */
int32_t subetha_umbra_allocate_with_prefix(subetha_handle region,
                                           const uint8_t *value,
                                           uint64_t value_len,
                                           uint32_t prefix,
                                           uint8_t *out);

/**
 * Read the value `pointer` names into `value_out`.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `value_out` points to at least `value_len` writable bytes.
 */
int32_t subetha_umbra_resolve(subetha_handle region,
                              const uint8_t *pointer,
                              uint8_t *value_out,
                              uint64_t value_len);

/**
 * The slot `pointer` names, into `target_out`, or `SUBETHA_UMBRA_NIL`.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `target_out` is a valid pointer.
 */
int32_t subetha_umbra_target(const uint8_t *pointer, uint32_t *target_out);

/**
 * The prefix `pointer` carries, into `prefix_out`.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `prefix_out` is a valid pointer.
 */
int32_t subetha_umbra_prefix(const uint8_t *pointer, uint32_t *prefix_out);

/**
 * Whether `pointer` aims at nothing, into `nil_out`.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `nil_out` is a valid pointer.
 */
int32_t subetha_umbra_is_nil(const uint8_t *pointer, bool *nil_out);

/**
 * Whether two pointers could aim at equal values, into `equal_out`.
 *
 * A false answer is certain. A true answer means resolve both and
 * compare: four bytes agreeing is not equality.
 *
 * # Safety
 * Both pointers point to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `equal_out` is a valid pointer.
 */
int32_t subetha_umbra_prefix_eq(const uint8_t *left, const uint8_t *right, bool *equal_out);

/**
 * Whether `pointer` could aim at a value starting with `query`, into
 * `matches_out`.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `matches_out` is a valid pointer.
 */
int32_t subetha_umbra_matches_prefix(const uint8_t *pointer, uint32_t query, bool *matches_out);

/**
 * Attach a caller tag and up to `SUBETHA_UMBRA_EXT_BYTES` of payload,
 * writing the changed pointer back into `pointer`.
 *
 * A payload longer than that is refused and the pointer is left alone.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` bytes readable and
 * writable; `payload` points to at least `payload_len` bytes.
 */
int32_t subetha_umbra_set_ext(uint8_t *pointer,
                              uint8_t tag,
                              const uint8_t *payload,
                              uint64_t payload_len);

/**
 * The tag and payload `pointer` carries.
 *
 * The payload bytes are handed over whatever the tag says; a caller that
 * has not checked the tag is reading bytes whose meaning nobody agreed.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` readable bytes;
 * `tag_out` is valid; `payload_out` points to at least
 * `SUBETHA_UMBRA_EXT_BYTES` writable bytes.
 */
int32_t subetha_umbra_get_ext(const uint8_t *pointer, uint8_t *tag_out, uint8_t *payload_out);

/**
 * Drop any caller tag and zero its payload, writing the changed pointer
 * back into `pointer`.
 *
 * # Safety
 * `pointer` points to at least `SUBETHA_UMBRA_BYTES` bytes readable and
 * writable.
 */
int32_t subetha_umbra_clear_ext(uint8_t *pointer);

/**
 * Obtain the set under `base_path` holding up to `capacity` elements of
 * `element_size` bytes: a new one starts on `strategy`, and one that
 * already exists keeps the strategy in force, which lives in a state
 * file beside the two backings and is what every handle reads.
 *
 * Both backings are created up front, so a later migration creates no
 * file while callers are attached.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_universal_create(const char *base_path,
                                 uint64_t capacity,
                                 uint64_t element_size,
                                 uint32_t strategy,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to a set another process created under `base_path`. The
 * strategy in force is read from the set's state file, so this handle
 * holds no belief of its own; a migration through any handle is what
 * this one reads on its next call, and the stamp in the stats says when
 * that happened.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_universal_open(const char *base_path,
                               uint64_t capacity,
                               uint64_t element_size,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Add `value` if it is not already present.
 *
 * `added` is set to true when it was added and false when the set
 * already held it. It may be null when the caller does not care.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `added` is null or valid.
 */
int32_t subetha_universal_insert(subetha_handle handle,
                                 const uint8_t *value,
                                 uint64_t value_len,
                                 bool *added);

/**
 * Whether the set holds `value`, into `present`.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `present` is valid.
 */
int32_t subetha_universal_contains(subetha_handle handle,
                                   const uint8_t *value,
                                   uint64_t value_len,
                                   bool *present);

/**
 * Move the set to `strategy`, carrying every element across.
 *
 * Elements reach the target backing before the stamp changes, so a
 * reader whose stamp is unchanged was reading a complete backing
 * throughout. A failure part-way leaves the stamp untouched and the set
 * on its old backing. Migrating to the strategy already in force changes
 * nothing and does not advance the stamp.
 */
int32_t subetha_universal_migrate(subetha_handle handle, uint32_t strategy);

/**
 * Empty the live backing.
 */
int32_t subetha_universal_clear(subetha_handle handle);

/**
 * Read the strategy word, the counts and the occupancy into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_universal_read_stats(subetha_handle handle, struct subetha_universal_stats *out);

/**
 * Push both backings to disk and wait for them.
 */
int32_t subetha_universal_flush(subetha_handle handle);

/**
 * Create a tower whose leaf at `leaf_path` holds `leaf_capacity` values
 * of `value_size` bytes, with one intermediate level per entry in
 * `level_paths` / `level_capacities`, top first.
 *
 * Depth is `n_levels + 1`. Zero levels makes a depth-1 tower, which is a
 * bare region with a one-entry path.
 *
 * # Safety
 * `leaf_path` is a NUL-terminated UTF-8 string; `level_paths` points to
 * `n_levels` such strings and `level_capacities` to `n_levels` values;
 * `out` is a valid pointer.
 */
int32_t subetha_tower_create(const char *leaf_path,
                             uint64_t leaf_capacity,
                             uint64_t value_size,
                             const char *const *level_paths,
                             const uint64_t *level_capacities,
                             uint64_t n_levels,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Attach to a tower another process created, with the same shape.
 *
 * # Safety
 * As [`subetha_tower_create`].
 */
int32_t subetha_tower_open(const char *leaf_path,
                           uint64_t leaf_capacity,
                           uint64_t value_size,
                           const char *const *level_paths,
                           const uint64_t *level_capacities,
                           uint64_t n_levels,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Store `value` and hang it off the next free slot on the top level,
 * writing the path into `path_out`, which must hold exactly `depth`
 * entries.
 *
 * # Safety
 * `value` points to at least `value_len` bytes; `path_out` points to at
 * least `path_len` writable `u32`s.
 */
int32_t subetha_tower_append(subetha_handle handle,
                             const uint8_t *value,
                             uint64_t value_len,
                             uint32_t *path_out,
                             uint64_t path_len);

/**
 * Store `value` and build the chain of links down to it from `top_slot`
 * on the top level, writing the path into `path_out`.
 *
 * The leaf is written first and the top link last, so a reader walking
 * down never reaches a level that does not yet name a live slot below.
 *
 * # Safety
 * As [`subetha_tower_append`].
 */
int32_t subetha_tower_insert_at_top(subetha_handle handle,
                                    uint32_t top_slot,
                                    const uint8_t *value,
                                    uint64_t value_len,
                                    uint32_t *path_out,
                                    uint64_t path_len);

/**
 * Read the value `path` names into `value_out`, checking every level.
 *
 * Refused with the level that failed rather than resolving a path the
 * tower no longer agrees with, and nothing is written on a refusal.
 *
 * # Safety
 * `path` points to at least `path_len` readable `u32`s; `value_out`
 * points to at least `value_len` writable bytes.
 */
int32_t subetha_tower_get(subetha_handle handle,
                          const uint32_t *path,
                          uint64_t path_len,
                          uint8_t *value_out,
                          uint64_t value_len);

/**
 * Read the tower's shape and occupancy into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_tower_read_stats(subetha_handle handle, struct subetha_tower_stats *out);

/**
 * Push every level to disk and wait for them.
 */
int32_t subetha_tower_flush(subetha_handle handle);

/**
 * Make a policy from one of the `SUBETHA_QOS_PRESET_` constants.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_qos_create_preset(uint32_t preset, uint32_t mode, subetha_handle *out);

/**
 * Make a policy from every field at once. Every field is validated
 * before anything is built, so a refusal leaves no handle.
 *
 * # Safety
 * `initial` and `out` are valid pointers.
 */
int32_t subetha_qos_create(const struct subetha_qos *initial, uint32_t mode, subetha_handle *out);

/**
 * Read the whole policy into `out`.
 *
 * Five atomics are read one after another, so a writer running during
 * this can leave a mixture of two policies: every field was real, the
 * combination need not have been.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_qos_read(subetha_handle handle, struct subetha_qos *out);

/**
 * Set the durability. Atomic on its own.
 */
int32_t subetha_qos_set_durability(subetha_handle handle, uint32_t durability);

/**
 * Set the reliability. Atomic on its own.
 */
int32_t subetha_qos_set_reliability(subetha_handle handle, uint32_t reliability);

/**
 * Set the history. `depth` is read only for `SUBETHA_QOS_KEEP_LAST`, and
 * a depth of zero there is refused rather than kept as a policy that
 * keeps nothing. Atomic on its own: the kind and the depth share one
 * word.
 */
int32_t subetha_qos_set_history(subetha_handle handle, uint32_t kind, uint32_t depth);

/**
 * Set the latency budget in nanoseconds. Atomic on its own.
 */
int32_t subetha_qos_set_max_latency(subetha_handle handle, uint64_t nanos);

/**
 * Set the ordering. Atomic on its own.
 */
int32_t subetha_qos_set_ordering(subetha_handle handle, uint32_t ordering);

/**
 * The locale this policy's durability asks for, into `out`, as one of the
 * `SUBETHA_LOCALE_` constants.
 *
 * It is a recommendation, not a move: a ring changes locale when a caller
 * tells it to.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_qos_recommended_locale(subetha_handle handle, uint32_t *out);

/**
 * The ring capacity this policy's history asks for, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_qos_recommended_capacity(subetha_handle handle, uint64_t *out);

/**
 * The mode this policy was created in, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_qos_mode(subetha_handle handle, uint32_t *out);

/**
 * Make a registry. `mode` is one of the `SUBETHA_MODE_` constants and is
 * recorded rather than acted on, since a registry runs nothing.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_endpoint_registry_create(uint32_t mode, subetha_handle *out);

/**
 * Bind `id` to the locale-adaptive ring `ring` names. Binding an id that
 * is already bound replaces it and steps the generation, which is how a
 * rebind reaches a caller holding an older reading.
 * The ring is taken before the registry is borrowed, and not inside that
 * borrow: a borrow of the handle table does not nest, so holding one
 * while asking for another refuses the second.
 */
int32_t subetha_endpoint_bind_local(subetha_handle registry, uint64_t id, subetha_handle ring);

/**
 * Bind `id` to a remote host: `server_addr` is where its bridge listens
 * and `server_name` is the identity its certificate must carry.
 *
 * Binding a remote target needs no transport built into this library. It
 * records where the bytes would go; carrying them is the bridge's job and
 * its feature's.
 *
 * # Safety
 * `server_addr` and `server_name` are NUL-terminated UTF-8 strings.
 */
int32_t subetha_endpoint_bind_remote(subetha_handle registry,
                                     uint64_t id,
                                     const char *server_addr,
                                     const char *server_name);

/**
 * Remove `id`. `SUBETHA_E_MAP_KEY_ABSENT` when it named nothing, so a caller
 * can tell an unbind that did something from one that did not.
 */
int32_t subetha_endpoint_unbind(subetha_handle registry, uint64_t id);

/**
 * Read what `id` resolves to, and the generation it was read at, into
 * `out`.
 *
 * An unbound id is not an error: it reports `SUBETHA_ENDPOINT_NONE` with
 * a generation, because "nothing is bound here right now" is an answer a
 * caller acts on and can also go stale.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_endpoint_read(subetha_handle registry,
                              uint64_t id,
                              struct subetha_endpoint_target *out);

/**
 * Copy the address and name of a remote binding out as text.
 *
 * `SUBETHA_E_MAP_KEY_ABSENT` when `id` is unbound, `SUBETHA_E_WRONG_KIND` when
 * it names a local ring, and `SUBETHA_E_BUFFER_TOO_SMALL` when a buffer
 * is short, with the needed length written so a caller can ask again.
 *
 * # Safety
 * `addr_out` points to `addr_cap` writable bytes and `name_out` to
 * `name_cap`; both length pointers are valid.
 */
int32_t subetha_endpoint_read_remote(subetha_handle registry,
                                     uint64_t id,
                                     uint8_t *addr_out,
                                     size_t addr_cap,
                                     size_t *addr_len,
                                     uint8_t *name_out,
                                     size_t name_cap,
                                     size_t *name_len);

/**
 * Whether a generation read earlier still stands, into `out`.
 *
 * False means something was bound or unbound since, so anything read at
 * that generation is to be read again. It does not mean this id changed:
 * the counter is registry-wide, so a rebind of another id also moves it.
 * Re-reading is cheap and being wrong is not.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_endpoint_still_valid(subetha_handle registry, uint64_t generation, bool *out);

/**
 * The registry's current generation into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_endpoint_generation(subetha_handle registry, uint64_t *out);

/**
 * How many ids are bound, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_endpoint_count(subetha_handle registry, uint64_t *out);

/**
 * The mode this registry was created in, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_endpoint_registry_mode(subetha_handle registry, uint32_t *out);

/**
 * Create a blocking MPSC pool in anonymous memory: `n_producers` rings of
 * `capacity` slots, one producer handle each into `out_producers`, and
 * the consumer handle into `out_consumer`.
 *
 * # Safety
 * `out_producers` points to `n_producers` writable handles; `out_consumer`
 * is a valid pointer.
 */
int32_t subetha_mpsc_create_anon_pool(uint32_t n_producers,
                                      uint32_t capacity,
                                      uint32_t mode,
                                      subetha_handle *out_producers,
                                      subetha_handle *out_consumer);

/**
 * Create a file-backed blocking MPSC pool under `path_prefix`.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `out_producers` points
 * to `n_producers` writable handles; `out_consumer` is a valid pointer.
 */
int32_t subetha_mpsc_create_pool(const char *path_prefix,
                                 uint32_t n_producers,
                                 uint32_t capacity,
                                 uint32_t mode,
                                 subetha_handle *out_producers,
                                 subetha_handle *out_consumer);

/**
 * Attach to a file-backed blocking MPSC pool another process created
 * under `path_prefix`, with the producer count and capacity it was
 * created with.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `out_producers` points
 * to `n_producers` writable handles; `out_consumer` is a valid pointer.
 */
int32_t subetha_mpsc_open_pool(const char *path_prefix,
                               uint32_t n_producers,
                               uint32_t expected_capacity,
                               uint32_t mode,
                               subetha_handle *out_producers,
                               subetha_handle *out_consumer);

/**
 * Push `len` bytes into this producer's ring without waiting; the slot
 * semantics are the adaptive ring's. `SUBETHA_E_RING_FULL` when there is
 * no room.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_mpsc_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Push, parking until this producer's ring has room or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_mpsc_push_wait(subetha_handle handle,
                               const uint8_t *data,
                               size_t len,
                               int64_t timeout_ms);

/**
 * Push `count` payloads of `len` bytes each onto this producer's ring,
 * the first at `items` and each next one `stride` bytes on, under one
 * handle lookup and one panic guard. The batch contract is
 * `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_mpsc_try_push_many(subetha_handle handle,
                                   const uint8_t *items,
                                   size_t stride,
                                   size_t len,
                                   size_t count,
                                   size_t *out_done);

/**
 * Pop up to `count` slots from the pool, the first written at `out` and
 * each next one `stride` bytes on. The batch contract is
 * `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_mpsc_try_pop_many(subetha_handle handle,
                                  uint8_t *out,
                                  size_t stride,
                                  size_t count,
                                  size_t *out_done);

/**
 * Pop one slot from whichever ring has one, round robin, without waiting;
 * `cap` must be at least `SUBETHA_RING_SLOT_BYTES`. `SUBETHA_E_RING_EMPTY`
 * when every ring is empty.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_mpsc_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Pop, parking until any producer pushes or `timeout_ms` elapses; the
 * waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_mpsc_pop_wait(subetha_handle handle,
                              uint8_t *out,
                              size_t cap,
                              size_t *out_len,
                              int64_t timeout_ms);

/**
 * A snapshot of one producer into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_mpsc_producer_read_stats(subetha_handle handle,
                                         struct subetha_mpsc_producer_stats *out);

/**
 * A snapshot of the consumer into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_mpsc_consumer_read_stats(subetha_handle handle,
                                         struct subetha_mpsc_consumer_stats *out);

/**
 * Remove every file a file-backed blocking MPSC pool under `path_prefix`
 * names, for the producer count it was created with. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_mpsc_unlink(const char *path_prefix,
                            uint32_t n_producers,
                            struct subetha_unlink_report *report);

/**
 * Create the deque at `path` as its owner, truncating any file there.
 * `capacity` is a power of two of at least 1; the calling process is
 * recorded as the owner, and single-owner discipline is the caller's
 * contract. `scan_interval_us` in the options is ignored, the deque has
 * no sidecar.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout`, `options` and `out`
 * are valid pointers.
 */
int32_t subetha_deque_create(const char *path,
                             uint32_t capacity,
                             const struct subetha_element_layout *layout,
                             const struct subetha_ring_options *options,
                             subetha_handle *out);

/**
 * Open the deque another process created at `path` as a thief: the
 * handle steals, and its push and pop return
 * `SUBETHA_E_DEQUE_NOT_OWNER`. `SUBETHA_E_RING_IO` names an absent file
 * and `SUBETHA_E_RING_LAYOUT_MISMATCH` one of another layout.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout`, `options` and `out`
 * are valid pointers.
 */
int32_t subetha_deque_open_thief(const char *path,
                                 const struct subetha_element_layout *layout,
                                 const struct subetha_ring_options *options,
                                 subetha_handle *out);

/**
 * Owner side: push up to `element_size` bytes onto the bottom without
 * waiting; the slot is zero past the payload. `SUBETHA_E_RING_FULL` when
 * the deque is at capacity, `SUBETHA_E_RING_PAYLOAD_TOO_LARGE` for more
 * bytes than an element, `SUBETHA_E_DEQUE_NOT_OWNER` from a thief.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_deque_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Owner side: push `count` elements of `len` bytes each, the first at
 * `items` and each next one `stride` bytes on, under one handle lookup
 * and one panic guard. The batch contract is
 * `subetha_ring_try_push_many`'s; a thief's handle is
 * `SUBETHA_E_DEQUE_NOT_OWNER` before anything is pushed.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_deque_try_push_many(subetha_handle handle,
                                    const uint8_t *items,
                                    size_t stride,
                                    size_t len,
                                    size_t count,
                                    size_t *out_done);

/**
 * Thief side: steal up to `count` elements, the first written at `out`
 * and each next one `stride` bytes on; `stride` is at least the element
 * size. The batch contract is `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_deque_try_steal_many(subetha_handle handle,
                                     uint8_t *out,
                                     size_t stride,
                                     size_t count,
                                     size_t *out_done);

/**
 * Owner side: push, parking until a steal makes room or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_deque_push_wait(subetha_handle handle,
                                const uint8_t *data,
                                size_t len,
                                int64_t timeout_ms);

/**
 * Owner side: pop the bottom element, `slot_bytes` bytes, into `out`;
 * `cap` must be at least that. `SUBETHA_E_RING_EMPTY` when the deque is
 * empty or a thief took the last element first,
 * `SUBETHA_E_DEQUE_NOT_OWNER` from a thief.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_deque_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Steal the top element, `slot_bytes` bytes, into `out` without waiting;
 * `cap` must be at least that. Any handle steals. `SUBETHA_E_RING_EMPTY`
 * when the deque is empty or another taker won the element.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_deque_try_steal(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Steal, parking until the owner pushes or `timeout_ms` elapses; the
 * waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_deque_steal_wait(subetha_handle handle,
                                 uint8_t *out,
                                 size_t cap,
                                 size_t *out_len,
                                 int64_t timeout_ms);

/**
 * A snapshot of the deque into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_deque_read_stats(subetha_handle handle, struct subetha_deque_stats *out);

/**
 * Write the deque's dirty pages to its file.
 */
int32_t subetha_deque_flush(subetha_handle handle);

/**
 * Wake every thread parked in a wait on this deque; each re-checks the
 * deque and parks again unless it finds what it waited for.
 */
int32_t subetha_deque_wake_all(subetha_handle handle);

/**
 * Remove the deque file at `path` and its two waker files. The contract
 * is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_deque_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the map at `path` for `key_size`-byte keys and `value_size`-byte
 * values, `capacity` slots: an empty one is initialized when the file
 * does not exist, an existing one is attached with its entries in place.
 * A file built with another capacity or other sizes is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`. Size the table at twice the entries
 * expected; the probe chain saturates past a load factor of 0.7.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_hashmap_create(const char *path,
                               uint32_t capacity,
                               uint32_t key_size,
                               uint32_t value_size,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Attach to the map another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_hashmap_open(const char *path,
                             uint32_t capacity,
                             uint32_t key_size,
                             uint32_t value_size,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty map there,
 * discarding every entry other handles share. On Windows the file must
 * not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_hashmap_reset(const char *path,
                              uint32_t capacity,
                              uint32_t key_size,
                              uint32_t value_size,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Insert or update `key` with `value`; `out_outcome`, when not null,
 * receives `SUBETHA_MAP_INSERTED` or `SUBETHA_MAP_UPDATED`.
 * `SUBETHA_E_MAP_FULL` when no slot is free.
 *
 * # Safety
 * `key` and `value` point to `key_len` and `value_len` readable bytes;
 * `out_outcome` is null or a valid pointer.
 */
int32_t subetha_hashmap_insert(subetha_handle handle,
                               const uint8_t *key,
                               size_t key_len,
                               const uint8_t *value,
                               size_t value_len,
                               uint32_t *out_outcome);

/**
 * Insert `count` entries from the caller's own two arrays, the key at
 * `keys + i * key_stride` and its value at `values + i * value_stride`.
 * One handle lookup and one panic guard for the run.
 *
 * The keys and the values stay in separate arrays because that is how a
 * caller already has them - a column of each - and packing them into
 * pairs to make one array would cost the copy this call exists to avoid.
 *
 * Whether each entry was new or replaced an existing one is not
 * reported: a run of inserts is a caller saying what the map should
 * hold, and the per-entry outcome is what `subetha_hashmap_insert`
 * answers for a caller that needs it. The batch stops at the first
 * refusal, `SUBETHA_E_MAP_FULL` being the one to expect, and reports how
 * many landed.
 *
 * # Safety
 * `keys` addresses `count * key_stride` readable bytes and `values`
 * `count * value_stride`; `out_done` is a valid pointer.
 */
int32_t subetha_hashmap_insert_many(subetha_handle handle,
                                    const uint8_t *keys,
                                    size_t key_stride,
                                    const uint8_t *values,
                                    size_t value_stride,
                                    size_t count,
                                    size_t *out_done);

/**
 * Read `count` entries into the caller's own array, the value for the
 * key at `keys + i * key_stride` into `out + i * value_stride`. One
 * handle lookup and one panic guard for the run.
 *
 * The batch stops at the first key the map does not hold and reports how
 * many it read, so a caller that wants every hit rather than a run of
 * them looks each up on its own. `SUBETHA_E_MAP_KEY_ABSENT` is what a
 * first miss reports.
 *
 * # Safety
 * `keys` addresses `count * key_stride` readable bytes; `out` addresses
 * `count * value_stride` writable bytes; `out_done` is a valid pointer.
 */
int32_t subetha_hashmap_get_many(subetha_handle handle,
                                 const uint8_t *keys,
                                 size_t key_stride,
                                 uint8_t *out,
                                 size_t value_stride,
                                 size_t count,
                                 size_t *out_done);

/**
 * Insert `key` only if it is absent. `out_present` receives whether a
 * value was already there, in which case nothing was written and that
 * value was copied into `existing`, at least `value_size` bytes.
 *
 * # Safety
 * `key` and `value` point to readable bytes; `existing` points to `cap`
 * writable bytes; `existing_len` and `out_present` are valid pointers.
 */
int32_t subetha_hashmap_insert_if_absent(subetha_handle handle,
                                         const uint8_t *key,
                                         size_t key_len,
                                         const uint8_t *value,
                                         size_t value_len,
                                         uint8_t *existing,
                                         size_t cap,
                                         size_t *existing_len,
                                         bool *out_present);

/**
 * Copy `key`'s value into `out`, at least `value_size` bytes;
 * `SUBETHA_E_MAP_KEY_ABSENT` when the key has no entry.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_hashmap_get(subetha_handle handle,
                            const uint8_t *key,
                            size_t key_len,
                            uint8_t *out,
                            size_t cap,
                            size_t *out_len);

/**
 * Whether `key` has an entry.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_hashmap_contains(subetha_handle handle,
                                 const uint8_t *key,
                                 size_t key_len,
                                 bool *out);

/**
 * Remove `key`, copying the value it had into `out`, at least
 * `value_size` bytes; `SUBETHA_E_MAP_KEY_ABSENT` when it had none.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_hashmap_remove(subetha_handle handle,
                               const uint8_t *key,
                               size_t key_len,
                               uint8_t *out,
                               size_t cap,
                               size_t *out_len);

/**
 * Replace `key`'s value with `new` only if its current value is
 * byte-for-byte `expected`. `out_swapped` receives whether it was; when
 * it was not, the current value is copied into `current`, at least
 * `value_size` bytes. `SUBETHA_E_MAP_KEY_ABSENT` when the key has no
 * entry. The comparison and the write happen under the entry's lock.
 *
 * # Safety
 * `key`, `expected` and `new` point to readable bytes; `current` points
 * to `cap` writable bytes; `current_len` and `out_swapped` are valid
 * pointers.
 */
int32_t subetha_hashmap_compare_exchange(subetha_handle handle,
                                         const uint8_t *key,
                                         size_t key_len,
                                         const uint8_t *expected,
                                         size_t expected_len,
                                         const uint8_t *new_,
                                         size_t new_len,
                                         uint8_t *current,
                                         size_t cap,
                                         size_t *current_len,
                                         bool *out_swapped);

/**
 * Mark every slot empty. Not safe against an insert or remove running in
 * any process.
 */
int32_t subetha_hashmap_clear(subetha_handle handle);

/**
 * Reclaim the tombstones removes left by rebuilding the table in place;
 * `out_reclaimed`, when not null, receives how many. Not safe against a
 * writer in any process; a reader may miss an entry mid-rebuild.
 *
 * # Safety
 * `out_reclaimed` is null or a valid pointer.
 */
int32_t subetha_hashmap_compact(subetha_handle handle, uint64_t *out_reclaimed);

/**
 * Walk the entries: with `cursor` at zero, the first entry; each call
 * copies one entry's key and value into `key_out` and `value_out` and
 * advances `cursor`. `out_found` receives false, with nothing copied,
 * once the table is walked. An entry is seen once its insert has
 * published it; a slot an insert has claimed and not yet written is
 * passed over, so a walk under concurrent writers sees the entries
 * published before it reached their slots and never a payload still
 * being written. A walker that must see every entry walks again once
 * the writers have returned.
 *
 * # Safety
 * `cursor`, `key_len`, `value_len` and `out_found` are valid pointers;
 * `key_out` and `value_out` point to `key_cap` and `value_cap` writable
 * bytes.
 */
int32_t subetha_hashmap_next(subetha_handle handle,
                             uint64_t *cursor,
                             uint8_t *key_out,
                             size_t key_cap,
                             size_t *key_len,
                             uint8_t *value_out,
                             size_t value_cap,
                             size_t *value_len,
                             bool *out_found);

/**
 * A snapshot of the map into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_hashmap_read_stats(subetha_handle handle, struct subetha_hashmap_stats *out);

/**
 * Write the map's dirty pages to its file.
 */
int32_t subetha_hashmap_flush(subetha_handle handle);

/**
 * Remove the map file at `path`. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_hashmap_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the table at `path` with `capacity` slots: an empty one is
 * initialized when the file does not exist, an existing one is attached
 * with its registered processes in place, so a late joiner does not
 * unregister anyone. A file built with another capacity is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_heartbeat_create(const char *path,
                                 uint32_t capacity,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the table another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_heartbeat_open(const char *path,
                               uint32_t capacity,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Truncate the file at `path` and lay out an empty table, unregistering
 * every process that held a slot.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_heartbeat_reset(const char *path,
                                uint32_t capacity,
                                uint32_t mode,
                                subetha_handle *out);

/**
 * Take a free slot for `pid` and write its index into `out_slot`. The
 * index is what every later call about this process uses.
 * `SUBETHA_E_RING_FULL` when every slot is held.
 *
 * # Safety
 * `out_slot` is a valid pointer.
 */
int32_t subetha_heartbeat_register(subetha_handle handle, uint32_t pid, uint32_t *out_slot);

/**
 * Free the slot at `slot`, so another process can take it. A process
 * shutting down cleanly does this rather than leaving a slot for a
 * watcher to reclaim.
 */
int32_t subetha_heartbeat_unregister(subetha_handle handle, uint32_t slot);

/**
 * Stamp the slot at `slot` with the current epoch, which is how its
 * holder says it is still there.
 */
int32_t subetha_heartbeat_beat(subetha_handle handle, uint32_t slot);

/**
 * Advance the global epoch and write it into `out_epoch`. Every grace
 * window is measured against this, so a departed process is only
 * noticed once something ticks it.
 *
 * # Safety
 * `out_epoch` is null or a valid pointer.
 */
int32_t subetha_heartbeat_tick_epoch(subetha_handle handle, uint64_t *out_epoch);

/**
 * Set bit `bit` in the slot's in-flight bitmap, saying its holder has
 * taken that work unit. `bit` runs from zero to one below
 * `SUBETHA_HEARTBEAT_IN_FLIGHT`.
 */
int32_t subetha_heartbeat_mark_in_flight(subetha_handle handle, uint32_t slot, uint32_t bit);

/**
 * Clear bit `bit` in the slot's in-flight bitmap, saying its holder has
 * finished that work unit.
 */
int32_t subetha_heartbeat_clear_in_flight(subetha_handle handle, uint32_t slot, uint32_t bit);

/**
 * Read the slot at `slot` into `out`, under its version so the answer is
 * never half of one write and half of another. Writes
 * `SUBETHA_HEARTBEAT_EMPTY_PID` into `out->pid` for a slot nobody holds,
 * which is not an error: a watcher walks every slot and reads the empty
 * ones as empty.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_heartbeat_read_slot(subetha_handle handle,
                                    uint32_t slot,
                                    struct subetha_heartbeat_slot *out);

/**
 * A snapshot of the table into `out`. The registered count is a walk of
 * every slot, so it races a register or an unregister in some process.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_heartbeat_read_stats(subetha_handle handle, struct subetha_heartbeat_stats *out);

/**
 * Remove the table's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_heartbeat_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the histogram at `path` with the `n_boundaries` ascending
 * upper bounds at `boundaries`: an empty one is initialized when the
 * file does not exist, an existing one is attached with its counts in
 * place. A histogram built with other boundaries is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `boundaries` points to
 * `n_boundaries` readable values; `out` is a valid pointer.
 */
int32_t subetha_histogram_create(const char *path,
                                 const uint64_t *boundaries,
                                 size_t n_boundaries,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the histogram another process created at `path`; the file
 * must exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `boundaries` points to
 * `n_boundaries` readable values; `out` is a valid pointer.
 */
int32_t subetha_histogram_open(const char *path,
                               const uint64_t *boundaries,
                               size_t n_boundaries,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Record `value`, and report the bucket it landed in through `out_bucket`
 * when that is not null.
 *
 * # Safety
 * `out_bucket` is null or a valid pointer.
 */
int32_t subetha_histogram_record(subetha_handle handle, uint64_t value, uint64_t *out_bucket);

/**
 * The bucket `value` would land in, into `out`, without recording it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_histogram_bucket_for(subetha_handle handle, uint64_t value, uint64_t *out);

/**
 * The count in bucket `bucket_idx`, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_histogram_count(subetha_handle handle, uint64_t bucket_idx, uint64_t *out);

/**
 * Every bucket's count into `out`, which holds `cap` values, and how many
 * were written into `out_len`. `SUBETHA_E_BUFFER_TOO_SMALL` when `cap` is
 * below the bucket count, with the count needed in `out_len`.
 *
 * # Safety
 * `out` points to `cap` writable values; `out_len` is a valid pointer.
 */
int32_t subetha_histogram_counts(subetha_handle handle, uint64_t *out, size_t cap, size_t *out_len);

/**
 * The boundaries the histogram was built with into `out`, the same way
 * `subetha_histogram_counts` reports counts.
 *
 * # Safety
 * `out` points to `cap` writable values; `out_len` is a valid pointer.
 */
int32_t subetha_histogram_boundaries(subetha_handle handle,
                                     uint64_t *out,
                                     size_t cap,
                                     size_t *out_len);

/**
 * The value at percentile `p`, from zero to one hundred, into `out`. The
 * answer is a bucket boundary, since that is what the counts record.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_histogram_percentile(subetha_handle handle, double p, uint64_t *out);

/**
 * Zero every bucket. Peers sharing the file see the same empty histogram.
 */
int32_t subetha_histogram_reset(subetha_handle handle);

/**
 * Push the histogram's dirty pages to disk, returning when they are
 * durable.
 */
int32_t subetha_histogram_flush(subetha_handle handle);

/**
 * Start pushing the histogram's dirty pages to disk and return at once.
 */
int32_t subetha_histogram_flush_async(subetha_handle handle);

/**
 * A snapshot of the histogram into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_histogram_read_stats(subetha_handle handle, struct subetha_histogram_stats *out);

/**
 * Obtain the limiter at `path` holding `capacity` tokens and refilling
 * at `refill_rate_per_sec`: a full bucket is initialized when the file
 * does not exist, an existing one is attached with its live token count
 * in place. A limiter built with another capacity or rate is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_rate_limiter_create(const char *path,
                                    uint32_t capacity,
                                    uint32_t refill_rate_per_sec,
                                    uint32_t mode,
                                    subetha_handle *out);

/**
 * Attach to the limiter another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_rate_limiter_open(const char *path,
                                  uint32_t capacity,
                                  uint32_t refill_rate_per_sec,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Take `n` tokens if they are there, and refuse with
 * `SUBETHA_E_WOULD_BLOCK` if they are not. A request above the capacity
 * is refused the same way, since no amount of waiting would meet it.
 */
int32_t subetha_rate_limiter_try_acquire(subetha_handle handle, uint32_t n);

/**
 * Take `n` tokens, waiting up to `timeout_ms` for the bucket to refill
 * enough. `SUBETHA_E_TIMEOUT` when the deadline passes first, and
 * `SUBETHA_E_WOULD_BLOCK` at once for a request above the capacity.
 *
 * The wait spins on the calling thread in either mode; the limiter
 * starts no thread of its own.
 */
int32_t subetha_rate_limiter_acquire_wait(subetha_handle handle, uint32_t n, uint64_t timeout_ms);

/**
 * Tokens available right now, into `out`. A snapshot: another process
 * may take them before the caller acts on it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rate_limiter_available(subetha_handle handle, uint32_t *out);

/**
 * Refill the bucket to its capacity. Peers sharing the file see the same
 * full bucket.
 */
int32_t subetha_rate_limiter_reset(subetha_handle handle);

/**
 * Push the limiter's dirty pages to disk, returning when they are
 * durable.
 */
int32_t subetha_rate_limiter_flush(subetha_handle handle);

/**
 * Start pushing the limiter's dirty pages to disk and return at once.
 */
int32_t subetha_rate_limiter_flush_async(subetha_handle handle);

/**
 * A snapshot of the limiter into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rate_limiter_read_stats(subetha_handle handle,
                                        struct subetha_rate_limiter_stats *out);

/**
 * Obtain the barrier at `path`, counting peers in the heartbeat table
 * `heartbeat` names: an empty one is initialized when the file does not
 * exist, an existing one is attached at the epoch it stands at. A
 * `grace_epochs` of zero takes `SUBETHA_BARRIER_GRACE_DEFAULT`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_epoch_barrier_create(const char *path,
                                     subetha_handle heartbeat,
                                     uint64_t grace_epochs,
                                     uint32_t mode,
                                     subetha_handle *out);

/**
 * Attach to the barrier another process created at `path`; the file must
 * exist.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_epoch_barrier_open(const char *path,
                                   subetha_handle heartbeat,
                                   uint64_t grace_epochs,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Wait at `epoch` until every live peer has reached it. Blocks the
 * calling thread. `SUBETHA_E_BARRIER_EPOCH_PASSED` when the barrier is
 * already past `epoch`, and `SUBETHA_E_BARRIER_NO_LIVE_PEERS` when the
 * heartbeat table shows nobody to wait for.
 */
int32_t subetha_epoch_barrier_wait(subetha_handle handle, uint32_t epoch);

/**
 * `subetha_epoch_barrier_wait`, released once `quorum` peers have
 * arrived rather than all of them, so one straggler cannot hold a round.
 */
int32_t subetha_epoch_barrier_wait_quorum(subetha_handle handle, uint32_t epoch, uint32_t quorum);

/**
 * `subetha_epoch_barrier_wait` with a deadline; `SUBETHA_E_TIMEOUT` when
 * `timeout_ms` elapses first. The arrival stands, so a later wait at the
 * same epoch does not count this caller twice.
 */
int32_t subetha_epoch_barrier_wait_timeout(subetha_handle handle,
                                           uint32_t epoch,
                                           uint64_t timeout_ms);

/**
 * `subetha_epoch_barrier_wait_quorum` with a deadline.
 */
int32_t subetha_epoch_barrier_wait_quorum_timeout(subetha_handle handle,
                                                  uint32_t epoch,
                                                  uint32_t quorum,
                                                  uint64_t timeout_ms);

/**
 * The epoch the barrier stands at, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epoch_barrier_epoch(subetha_handle handle, uint32_t *out);

/**
 * Peers the heartbeat table shows as live, into `out`: how many an
 * unqualified wait waits for.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epoch_barrier_live_peers(subetha_handle handle, uint32_t *out);

/**
 * A snapshot of the barrier into `out`. Every count races an arrival, so
 * it describes a moment that has already passed.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_epoch_barrier_read_stats(subetha_handle handle,
                                         struct subetha_epoch_barrier_stats *out);

/**
 * Flush the barrier's state to its file.
 */
int32_t subetha_epoch_barrier_flush(subetha_handle handle);

/**
 * Remove the barrier's file. On Windows the file must not be mapped by
 * any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_epoch_barrier_unlink(const char *path, struct subetha_unlink_report *out);

/**
 * Obtain the fence clock at `path` with room for `capacity`
 * participants: an empty one is initialized when the file does not
 * exist, an existing one is attached with its registered slots and its
 * published fence in place. A file built with another capacity is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_fence_clock_create(const char *path,
                                   uint32_t capacity,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Attach to the fence clock another process created at `path`; the file
 * must exist.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_fence_clock_open(const char *path,
                                 uint32_t capacity,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty clock there,
 * dropping every registration. On Windows the file must not be mapped by
 * any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_fence_clock_reset(const char *path,
                                  uint32_t capacity,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Take a slot for `pid` and write its index into `out_slot`. The index
 * is a plain number, not a handle: nothing is allocated for it and
 * nothing has to be destroyed, so a participant that registers and
 * unregisters repeatedly costs only the calls.
 * `SUBETHA_E_FENCE_CLOCK_FULL` when every slot is taken.
 *
 * # Safety
 * `out_slot` is a valid pointer.
 */
int32_t subetha_fence_clock_register(subetha_handle handle, uint32_t pid, uint32_t *out_slot);

/**
 * Give slot `slot` back, so it stops holding the global fence down and
 * the next participant can take it. A caller reclaiming after a crash
 * reads `pid` from `subetha_fence_clock_slot` and unregisters the ones
 * whose process is gone.
 */
int32_t subetha_fence_clock_unregister(subetha_handle handle, uint32_t slot);

/**
 * Step slot `slot`'s clock forward and write the new value into `out`.
 * Called as the participant does work, so what it publishes is at least
 * as late as anything it has done.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_tick(subetha_handle handle, uint32_t slot, struct subetha_hlc *out);

/**
 * Take `remote` into slot `slot`'s clock and write the result into
 * `out`: the later of the two, stepped, so an event that caused the
 * remote one compares below it. This is what a receiver calls with the
 * value that traveled beside a message.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_merge(subetha_handle handle,
                                  uint32_t slot,
                                  struct subetha_hlc remote,
                                  struct subetha_hlc *out);

/**
 * Slot `slot`'s clock as it stands, without stepping it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_get_local(subetha_handle handle,
                                      uint32_t slot,
                                      struct subetha_hlc *out);

/**
 * The latest clock across registered slots, computed now and written
 * into `out`: every event any participant has recorded stands at or
 * below it, so it is the point by which all of their work is accounted
 * for. This walks the slots without publishing what it finds.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_compute_global_fence(subetha_handle handle, struct subetha_hlc *out);

/**
 * Compute the fence and publish it, so every other process reads it
 * without walking the slots, and write it into `out`. One participant
 * doing this on a schedule is the usual arrangement.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_publish_global_fence(subetha_handle handle, struct subetha_hlc *out);

/**
 * The fence as last published, into `out`. Cheap: it reads one value
 * rather than walking the slots, which is what publishing is for.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_read_global_fence(subetha_handle handle, struct subetha_hlc *out);

/**
 * What slot `slot` holds, into `out`, and whether it is registered at
 * all into `out_registered`. An unregistered slot leaves `out`
 * untouched.
 *
 * # Safety
 * `out` and `out_registered` are valid pointers.
 */
int32_t subetha_fence_clock_read_slot(subetha_handle handle,
                                      uint32_t slot,
                                      struct subetha_fence_clock_slot *out,
                                      bool *out_registered);

/**
 * A snapshot of the clock into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_fence_clock_read_stats(subetha_handle handle,
                                       struct subetha_fence_clock_stats *out);

/**
 * Write the clock's dirty pages to its file.
 */
int32_t subetha_fence_clock_flush(subetha_handle handle);

/**
 * Remove the clock's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_fence_clock_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the lazy value at `path`, holding `value_bytes`: an unclaimed
 * one is initialized when the file does not exist, and an existing one
 * is attached in whatever state it stands.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_lazy_create(const char *path,
                            size_t value_bytes,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to the lazy value another process created at `path`, whose
 * value must be `value_bytes` long; the file must exist.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_lazy_open(const char *path, size_t value_bytes, uint32_t mode, subetha_handle *out);

/**
 * Take the right to produce the value, stamping the claim with `pid`,
 * and write whether this caller won into `out_won`.
 *
 * One caller wins. Every other is told it did not, whether because
 * another claim stands or because the value is already published, and
 * reads the value with `subetha_lazy_wait` or `subetha_lazy_try_get`.
 *
 * # Safety
 * `out_won` is a valid pointer.
 */
int32_t subetha_lazy_claim(subetha_handle handle, uint32_t pid, bool *out_won);

/**
 * Publish the value produced under the claim `pid` holds, and release
 * the claim. `SUBETHA_E_LAZY_NOT_CLAIMED` when that claim is not the one
 * standing, so a caller that never won cannot overwrite the winner's
 * value.
 *
 * # Safety
 * `value` addresses `len` readable bytes.
 */
int32_t subetha_lazy_publish(subetha_handle handle, uint32_t pid, const uint8_t *value, size_t len);

/**
 * The published value into `out`, or `SUBETHA_E_LAZY_NOT_CLAIMED` while
 * none is published. Never blocks and never claims.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_lazy_try_get(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Wait for the published value into `out`, giving up after
 * `timeout_ms`.
 *
 * `SUBETHA_E_TIMEOUT` when the deadline passes with a live claim still
 * outstanding, and `SUBETHA_E_LAZY_CLAIMANT_GONE` as soon as the claim
 * belongs to a process that has died, since no deadline is long enough
 * for a publish that will never come.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_lazy_wait(subetha_handle handle,
                          int64_t timeout_ms,
                          uint8_t *out,
                          size_t cap,
                          size_t *out_len);

/**
 * Free a claim whose process is gone, and write whether one was freed
 * into `out_freed`. A claim held by a live process is left alone.
 *
 * # Safety
 * `out_freed` is null or a valid pointer.
 */
int32_t subetha_lazy_reclaim(subetha_handle handle, bool *out_freed);

/**
 * A snapshot of the value into `out`. The state races every claim and
 * publish, so it describes a moment that has already passed.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_lazy_read_stats(subetha_handle handle, struct subetha_lazy_stats *out);

/**
 * Flush the value to its file.
 */
int32_t subetha_lazy_flush(subetha_handle handle);

/**
 * Remove the value's file. On Windows the file must not be mapped by any
 * handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_lazy_unlink(const char *path, struct subetha_unlink_report *out);

/**
 * Obtain the value at `path` and take a holder slot: a region of
 * `value_bytes` holding `value` is initialized when the file does not
 * exist, and an existing one is attached with its contents in place.
 *
 * `value` is written only by the call that creates the backing; an
 * attach leaves what is there alone, as a second `Arc::clone` leaves
 * what the first one points at. A null `value` creates the region
 * zeroed.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `value` is null or addresses
 * `value_bytes` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_shared_arc_create(const char *path,
                                  const uint8_t *value,
                                  size_t value_bytes,
                                  uint32_t max_holders,
                                  uint32_t on_last,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Attach to the value another process created at `path`, whose region
 * must be `value_bytes` long, and take a holder slot. A backing built
 * for another length or another holder count is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_shared_arc_open(const char *path,
                                size_t value_bytes,
                                uint32_t max_holders,
                                uint32_t on_last,
                                uint32_t mode,
                                subetha_handle *out);

/**
 * Copy `len` bytes from `offset` in the shared region into `out`, and
 * the count into `out_len`.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_shared_arc_read(subetha_handle handle,
                                size_t offset,
                                size_t len,
                                uint8_t *out,
                                size_t cap,
                                size_t *out_len);

/**
 * Copy `len` bytes from `src` into the shared region at `offset`.
 *
 * Not atomic and not ordered against another holder's read; see this
 * module's note on synchronizing writes.
 *
 * # Safety
 * `src` addresses `len` readable bytes.
 */
int32_t subetha_shared_arc_write(subetha_handle handle,
                                 size_t offset,
                                 const uint8_t *src,
                                 size_t len);

/**
 * Processes holding this value, into `out`, this one included.
 *
 * A holder whose process has died still counts until something reaps it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_shared_arc_strong_count(subetha_handle handle, uint64_t *out);

/**
 * Free every holder slot whose process is gone and report how many went
 * into `out`, which may be null.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_shared_arc_reap_dead_holders(subetha_handle handle, uint64_t *out);

/**
 * A snapshot of the value into `out`. The holder count races every
 * attach and release, so it describes a moment that has already passed.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_shared_arc_read_stats(subetha_handle handle, struct subetha_shared_arc_stats *out);

/**
 * Flush the shared region to its file.
 */
int32_t subetha_shared_arc_flush(subetha_handle handle);

/**
 * Remove the value's file. On Windows the file must not be mapped by any
 * handle.
 *
 * A value created with `SUBETHA_ARC_UNLINK` removes its own backing when
 * the last holder releases, so this reports nothing removed once that
 * has happened.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_shared_arc_unlink(const char *path, struct subetha_unlink_report *out);

/**
 * Mint a self-signed certificate for `sni` and copy it and its private
 * key out as DER bytes. `cert_len` and `key_len` receive the byte counts
 * the call produced, or the counts it would need when a buffer is too
 * small, in which case the answer is `SUBETHA_E_BUFFER_TOO_SMALL` and
 * nothing is copied.
 *
 * The server half needs both; a client half needs the certificate alone,
 * and trusts exactly that one. `sni` names the certificate rather than
 * the wire address, so a peer reached at any address presents it.
 *
 * # Safety
 * `sni` is a NUL-terminated UTF-8 string. `cert_out` points to
 * `cert_cap` writable bytes and `key_out` to `key_cap`; both lengths are
 * valid pointers.
 */
int32_t subetha_quic_self_signed_cert(const char *sni,
                                      uint8_t *cert_out,
                                      size_t cert_cap,
                                      size_t *cert_len,
                                      uint8_t *key_out,
                                      size_t key_cap,
                                      size_t *key_len);

/**
 * Make the client half: it pulls from the ring `ring` names and ships to
 * `server_addr`, presenting `sni` and trusting the certificate in
 * `cert`. `bind_addr` is the local address to send from; `0.0.0.0:0`
 * takes any port the system offers.
 *
 * # Safety
 * `server_addr`, `bind_addr` and `sni` are NUL-terminated UTF-8 strings;
 * `cert` points to `cert_len` readable bytes; `out` is a valid pointer.
 */
int32_t subetha_quic_bridge_client(subetha_handle ring,
                                   const char *server_addr,
                                   const char *bind_addr,
                                   const char *sni,
                                   const uint8_t *cert,
                                   size_t cert_len,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Make the server half: it binds `addr`, accepts one connection and
 * pushes what arrives into the ring `ring` names, presenting the
 * certificate in `cert` with the key in `key`. Port zero binds a port
 * the system chooses, which `subetha_quic_bridge_local_port` reports.
 *
 * # Safety
 * `addr` is a NUL-terminated UTF-8 string; `cert` points to `cert_len`
 * readable bytes and `key` to `key_len`; `out` is a valid pointer.
 */
int32_t subetha_quic_bridge_server(subetha_handle ring,
                                   const char *addr,
                                   const uint8_t *cert,
                                   size_t cert_len,
                                   const uint8_t *key,
                                   size_t key_len,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * The port the server half listens on, into `out`. For a server bound to
 * port zero this is the port the system chose.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_quic_bridge_local_port(subetha_handle handle, uint16_t *out);

/**
 * Carry `items` slots across the connection.
 *
 * In strict mode this blocks the calling thread until the transfer ends
 * or `timeout_ms` elapses, driving the work on that thread. In managed
 * mode it returns at once and the object's own thread carries the
 * transfer, which `subetha_quic_bridge_read_stats` reports on.
 *
 * A negative `timeout_ms` waits without a deadline.
 */
int32_t subetha_quic_bridge_run(subetha_handle handle, uint64_t items, int64_t timeout_ms);

/**
 * A snapshot of the bridge into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_quic_bridge_read_stats(subetha_handle handle,
                                       struct subetha_quic_bridge_stats *out);

/**
 * Make the sending half: it ships items from `local_addr` to `peer_addr`,
 * coding them in symbols of `symbol_len` bytes.
 *
 * # Safety
 * `local_addr` and `peer_addr` are NUL-terminated UTF-8 strings; `out` is
 * a valid pointer.
 */
int32_t subetha_sens_sender(const char *local_addr,
                            const char *peer_addr,
                            size_t symbol_len,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Make the receiving half: it binds `local_addr` and delivers what
 * arrives, decoding symbols of `symbol_len` bytes. Port zero binds a port
 * the system chooses, which `subetha_sens_local_port` reports.
 *
 * In managed mode the object starts a thread that drives the decoder, so
 * items are collected whether or not a caller is polling.
 *
 * # Safety
 * `local_addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_sens_receiver(const char *local_addr,
                              size_t symbol_len,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Ship `len` bytes at `bytes` to the peer.
 *
 * # Safety
 * `bytes` points to `len` readable bytes.
 */
int32_t subetha_sens_send(subetha_handle handle, const uint8_t *bytes, size_t len);

/**
 * Push out whatever this half is holding back, and answer once it has
 * gone to the socket.
 *
 * A block code fills `k` data shards before it codes and transmits, so a
 * caller with fewer items than that in hand has a stream that has not
 * left yet and no elapsed time will move it. This is the call that moves
 * it, and a caller sending in bursts smaller than a block, or ending a
 * stream, needs it.
 *
 * The sliding-window code and the unified endpoint send each item as it
 * arrives, so on those halves this succeeds having done nothing. It is
 * answered the same way on every half on purpose: a caller that flushes
 * where it should is then correct whichever code it was handed, which is
 * the point of the code being a swappable detail.
 *
 * A receiving half holds nothing back and refuses.
 */
int32_t subetha_sens_flush(subetha_handle handle);

/**
 * The next delivered item into `out`, its length into `out_len`.
 * `SUBETHA_E_RING_EMPTY` when nothing has arrived yet.
 *
 * In strict mode this drives the decoder on the calling thread first.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_sens_poll(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * The port this half's socket is bound to, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_sens_local_port(subetha_handle handle, uint16_t *out);

/**
 * A snapshot of this half into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_sens_read_stats(subetha_handle handle, struct subetha_sens_stats *out);

/**
 * Wait up to `timeout_ms` for the far end to acknowledge everything this
 * sending half has sent, retransmitting what it has not, and write
 * whether it all was into `out_acked`. This is what ends a stream: a send
 * returns once an item is on the wire, and the last items of a stream
 * have nothing after them to carry their repair, so a sender that closes
 * without this can lose its tail on a lossy path, and a stream whose
 * head was lost delivers nothing at all until the head is sent again.
 *
 * `false` in `out_acked` is the ordinary answer for a peer that stopped
 * responding; the caller decides whether that is a failure. A receiving
 * half has nothing to finish and refuses.
 *
 * # Safety
 * `out_acked` is a valid pointer.
 */
int32_t subetha_sens_finish(subetha_handle handle, int64_t timeout_ms, bool *out_acked);

/**
 * Make a sending half carrying the sliding-window code alone, with no
 * switching: the code is pinned at construction and stays.
 *
 * `window` source symbols are kept live, one repair goes out every `step`
 * symbols, and `density` is the coding density threshold. These are the
 * knobs the unified endpoint sets from measured loss; naming them is the
 * reason to reach for this constructor instead of it.
 *
 * # Safety
 * `local_addr` and `peer_addr` are NUL-terminated UTF-8 strings; `out` is
 * a valid pointer.
 */
int32_t subetha_sens_rlc_sender(const char *local_addr,
                                const char *peer_addr,
                                size_t symbol_len,
                                size_t window,
                                size_t step,
                                uint8_t density,
                                uint32_t mode,
                                subetha_handle *out);

/**
 * Make a receiving half for the sliding-window code alone. Port zero binds
 * a port the system chooses, which `subetha_sens_local_port` reports.
 *
 * No session exists until a peer is seen, and each connection id that
 * arrives opens its own, so one receiver serves several senders without
 * one of them evicting another's decode window.
 *
 * # Safety
 * `local_addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_sens_rlc_receiver(const char *local_addr,
                                  size_t symbol_len,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Make a sending half carrying the block Reed-Solomon code alone, with no
 * switching: the code is pinned at construction and stays.
 *
 * A block is `k` data shards and `r` parity shards, and `max_item` is the
 * largest item this half will be given. `k + r` is capped by the wire
 * format's per-block shard bitmap, and a request past it is refused by the
 * transport rather than trimmed.
 *
 * # Safety
 * `local_addr` and `peer_addr` are NUL-terminated UTF-8 strings; `out` is
 * a valid pointer.
 */
int32_t subetha_sens_rs_sender(const char *local_addr,
                               const char *peer_addr,
                               size_t k,
                               size_t r,
                               size_t max_item,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Make a receiving half for the block Reed-Solomon code alone. Port zero
 * binds a port the system chooses, which `subetha_sens_local_port`
 * reports.
 *
 * The block geometry rides the wire, so this half takes none: it decodes
 * whatever `k` and `r` a sender chose.
 *
 * # Safety
 * `local_addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_sens_rs_receiver(const char *local_addr, uint32_t mode, subetha_handle *out);

/**
 * Whether this library carries the sealed Sens path, into `out`. A caller
 * choosing a path at run time asks this rather than making a call and
 * reading the refusal.
 *
 * Separate from `subetha_transports_available`, which answers for the
 * bridges: a build can carry one and not the other, since the bridges
 * pull an async runtime and this path does not.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_sens_tls_available(bool *out);

/**
 * Mint a self-signed certificate for `sni` and copy it and its private
 * key out as DER bytes. `cert_len` and `key_len` receive the byte counts
 * the call produced, or the counts it would need when a buffer is too
 * small, in which case the answer is `SUBETHA_E_BUFFER_TOO_SMALL` and
 * nothing is copied.
 *
 * The receiving half needs both; a sending half needs the certificate
 * alone, and trusts exactly that one. `sni` names the certificate rather
 * than the wire address, so a peer reached at any address presents it.
 * Passing null for `sni` takes the transport's own default name, which is
 * what `subetha_sens_sender_tls` asserts when given no name of its own.
 *
 * # Safety
 * `sni` is null or a NUL-terminated UTF-8 string. `cert_out` points to
 * `cert_cap` writable bytes and `key_out` to `key_cap`; both lengths are
 * valid pointers.
 */
int32_t subetha_sens_self_signed_cert(const char *sni,
                                      uint8_t *cert_out,
                                      size_t cert_cap,
                                      size_t *cert_len,
                                      uint8_t *key_out,
                                      size_t key_cap,
                                      size_t *key_len);

/**
 * Make a sending half that handshakes with `peer_addr` before any item
 * moves, and seals every item after it.
 *
 * # Safety
 * `local_addr` and `peer_addr` are NUL-terminated UTF-8 strings;
 * `server_name` is null or one; `cert` points to `cert_len` readable
 * bytes; `out` is a valid pointer.
 */
int32_t subetha_sens_sender_tls(const char *local_addr,
                                const char *peer_addr,
                                size_t symbol_len,
                                uint32_t mode,
                                const uint8_t *cert,
                                size_t cert_len,
                                const char *server_name,
                                subetha_handle *out);

/**
 * Make a receiving half that serves sealed senders: it binds `local_addr`
 * and stands up before any peer dials, and each dialing peer handshakes
 * on its own and seals its own stream.
 *
 * `cert` and `key` are the DER certificate and private key this half
 * presents, both required. `peers` is the number of concurrent senders it
 * is provisioned for and must be at least one.
 *
 * `code` pins the erasure code to `SUBETHA_SENS_CODE_RLC` or
 * `SUBETHA_SENS_CODE_RS`, and there is no automatic choice here. That is
 * the transport's own refusal rather than a limit this layer invents: a
 * switch boundary belongs to one endpoint, so an automatic switch under
 * several peers would misdeliver silently. A single-peer caller who wants
 * the code to follow measured loss uses `subetha_sens_receiver`.
 *
 * # Safety
 * `local_addr` is a NUL-terminated UTF-8 string; `cert` points to
 * `cert_len` readable bytes and `key` to `key_len`; `out` is a valid
 * pointer.
 */
int32_t subetha_sens_receiver_tls(const char *local_addr,
                                  size_t symbol_len,
                                  uint32_t mode,
                                  const uint8_t *cert,
                                  size_t cert_len,
                                  const uint8_t *key,
                                  size_t key_len,
                                  uint32_t code,
                                  size_t peers,
                                  subetha_handle *out);

/**
 * Make the client half: it pulls from the ring `ring` names and ships to
 * `addr`, a host and port such as `127.0.0.1:9000`.
 *
 * # Safety
 * `addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_tcp_bridge_client(subetha_handle ring,
                                  const char *addr,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Make the server half: it binds `addr`, accepts one connection and
 * pushes what arrives into the ring `ring` names. Port zero binds a port
 * the system chooses, which `subetha_tcp_bridge_local_port` reports.
 *
 * # Safety
 * `addr` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_tcp_bridge_server(subetha_handle ring,
                                  const char *addr,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * The port the server half listens on, into `out`. For a server bound to
 * port zero this is the port the system chose.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_tcp_bridge_local_port(subetha_handle handle, uint16_t *out);

/**
 * Carry `items` slots across the connection.
 *
 * In strict mode this blocks the calling thread until the transfer ends
 * or `timeout_ms` elapses, driving the work on that thread. In managed
 * mode it returns at once and the object's own thread carries the
 * transfer, which `subetha_tcp_bridge_read_stats` reports on.
 *
 * A negative `timeout_ms` waits without a deadline.
 */
int32_t subetha_tcp_bridge_run(subetha_handle handle, uint64_t items, int64_t timeout_ms);

/**
 * A snapshot of the bridge into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_tcp_bridge_read_stats(subetha_handle handle, struct subetha_tcp_bridge_stats *out);

/**
 * Whether this library carries the transports, into `out`. A caller
 * choosing a path at run time asks this rather than making a call and
 * reading the refusal.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_transports_available(bool *out);

/**
 * Obtain the map at `path` covering `n_nodes` nodes, classifying at
 * `fan_out_threshold` and `fan_in_threshold`: an empty one is initialized
 * when the file does not exist, an existing one is attached with its
 * counts in place. A map built for another node count is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_topology_create(const char *path,
                                uint32_t n_nodes,
                                uint32_t fan_out_threshold,
                                uint32_t fan_in_threshold,
                                uint32_t mode,
                                subetha_handle *out);

/**
 * Truncate the map's file at `path` and initialize an empty one,
 * discarding every count live peers share.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_topology_reset(const char *path,
                               uint32_t n_nodes,
                               uint32_t fan_out_threshold,
                               uint32_t fan_in_threshold,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Attach to the map another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_topology_open(const char *path,
                              uint32_t n_nodes,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Record one message from `src` to `dst`, and report that edge's running
 * count through `out_count` when that is not null.
 *
 * # Safety
 * `out_count` is null or a valid pointer.
 */
int32_t subetha_topology_record_send(subetha_handle handle,
                                     uint32_t src,
                                     uint32_t dst,
                                     uint64_t *out_count);

/**
 * The number of distinct destinations `src` has sent to, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_topology_fan_out(subetha_handle handle, uint32_t src, uint32_t *out);

/**
 * The number of distinct sources `dst` has received from, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_topology_fan_in(subetha_handle handle, uint32_t dst, uint32_t *out);

/**
 * Push the map's dirty pages to disk, returning when they are durable.
 */
int32_t subetha_topology_flush(subetha_handle handle);

/**
 * Start pushing the map's dirty pages to disk and return at once.
 */
int32_t subetha_topology_flush_async(subetha_handle handle);

/**
 * A snapshot of the map into `out`, which walks every edge.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_topology_read_stats(subetha_handle handle, struct subetha_topology_stats *out);

/**
 * Obtain the waker at `path` holding `capacity` parks: an empty one is
 * initialized when the file does not exist, an existing one is attached
 * with its parked slots in place. A file built with another capacity is
 * a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_waker_create(const char *path,
                             uint32_t capacity,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Attach to the waker another process created at `path`; the file must
 * exist.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_waker_open(const char *path, uint32_t capacity, uint32_t mode, subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty waker there,
 * discarding whatever parks a live peer holds.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_waker_reset(const char *path,
                            uint32_t capacity,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Reserve a slot and park at `target_seq`, writing the token into `out`.
 *
 * `SUBETHA_E_RING_WAKER_FULL` when every slot is taken; the caller spins
 * on whatever it was waiting for instead.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_waker_park(subetha_handle handle, uint64_t target_seq, uint64_t *out);

/**
 * Block until a producer wakes `token` or `timeout_ms` elapses, and give
 * the park back either way, so a caller that waits does not release.
 *
 * `SUBETHA_E_TIMEOUT` when the deadline passes first. A negative
 * `timeout_ms` waits without one.
 */
int32_t subetha_waker_wait(subetha_handle handle, uint64_t token, int64_t timeout_ms);

/**
 * Give a park back without waiting on it.
 */
int32_t subetha_waker_release(subetha_handle handle, uint64_t token);

/**
 * Wake every parker whose target sequence `seq` has reached, and write
 * how many were woken into `out`.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_waker_wake_up_to(subetha_handle handle, uint64_t seq, uint64_t *out);

/**
 * Wake at most one parker whose target sequence `seq` has reached, for a
 * producer handing one item to one consumer.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_waker_wake_one_up_to(subetha_handle handle, uint64_t seq, uint64_t *out);

/**
 * Wake every parker whatever it is waiting for, for a shutdown that must
 * not leave anyone parked.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_waker_wake_all(subetha_handle handle, uint64_t *out);

/**
 * A snapshot of the waker into `out`. The parked count races every park
 * and wake, so it describes a moment that has already passed.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_waker_read_stats(subetha_handle handle, struct subetha_waker_stats *out);

/**
 * Remove the waker's file. On Windows the file must not be mapped by any
 * handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_waker_unlink(const char *path, struct subetha_unlink_report *out);

/**
 * Obtain the table at `path` with `capacity` slots: an empty one is laid
 * out when the file does not exist, an existing one is attached with its
 * claims in place, so a late joiner does not release anyone. A file
 * built with another capacity is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_holders_create(const char *path,
                               uint32_t capacity,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Attach to the table another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_holders_open(const char *path,
                             uint32_t capacity,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Truncate the file at `path` and lay out an empty table, releasing
 * every slot a live holder owns.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_holders_reset(const char *path,
                              uint32_t capacity,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Claim a free slot with `payload` and write its index into `out_slot`.
 * `SUBETHA_E_RING_FULL` when every slot is held.
 *
 * # Safety
 * `out_slot` is a valid pointer.
 */
int32_t subetha_holders_claim(subetha_handle handle, uint64_t payload, uint32_t *out_slot);

/**
 * Claim a free slot with nothing decided yet and write its index into
 * `out_slot`. The slot is visible to every process at once, so a caller
 * may then read whatever its payload depends on and call
 * `subetha_holders_publish`. `SUBETHA_E_RING_FULL` when every slot is
 * held.
 *
 * # Safety
 * `out_slot` is a valid pointer.
 */
int32_t subetha_holders_reserve(subetha_handle handle, uint32_t *out_slot);

/**
 * Fill in the payload of a slot `subetha_holders_reserve` took.
 */
int32_t subetha_holders_publish(subetha_handle handle, uint32_t slot, uint64_t payload);

/**
 * Claim the slot at `slot` in particular, and write whether it was free
 * into `out_claimed`. For a caller that has already decided which slot
 * it wants rather than taking whichever is free.
 *
 * # Safety
 * `out_claimed` is a valid pointer.
 */
int32_t subetha_holders_try_claim_slot(subetha_handle handle,
                                       uint32_t slot,
                                       uint64_t payload,
                                       bool *out_claimed);

/**
 * Give the slot at `slot` back.
 */
int32_t subetha_holders_release(subetha_handle handle, uint32_t slot);

/**
 * Read the slot at `slot`: its state into `out_state` and the process
 * holding it into `out_pid`. The state is `SUBETHA_HOLDER_FREE`,
 * `SUBETHA_HOLDER_RESERVED`, or the payload, and a caller walking the
 * table restarts its walk on a reservation rather than reading past one.
 *
 * # Safety
 * `out_state` and `out_pid` are null or valid pointers.
 */
int32_t subetha_holders_read_slot(subetha_handle handle,
                                  uint32_t slot,
                                  uint64_t *out_state,
                                  uint32_t *out_pid);

/**
 * Release every slot whose owning process is gone, and write how many
 * went into `out_freed`. This is the question a bare count cannot
 * answer: a number cannot be asked whether it is still running, and a
 * slot carrying its process id can.
 *
 * # Safety
 * `out_freed` is null or a valid pointer.
 */
int32_t subetha_holders_reap_dead(subetha_handle handle, uint32_t *out_freed);

/**
 * A snapshot of the table into `out`. The live count races a claim or a
 * release in some process, so it is a diagnostic rather than something
 * to branch on.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_holders_read_stats(subetha_handle handle, struct subetha_holders_stats *out);

/**
 * Remove the table's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_holders_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * The notifier's native object as an integer: a file descriptor on Unix,
 * an event `HANDLE` on Windows. It belongs to the notifier and is closed
 * when the notifier's handle is destroyed.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_notifier_native(subetha_handle handle, uint64_t *out);

/**
 * Consume every pending signal, so the next poll waits for the next push.
 * Call it before popping the ring to empty; a push that lands between
 * the drain and the last pop signals again and is not lost.
 */
int32_t subetha_notifier_drain(subetha_handle handle);

/**
 * Whether a signal is pending right now, without waiting.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_notifier_is_signaled(subetha_handle handle, bool *out);

/**
 * Wait up to `timeout_ms` for a signal, `SUBETHA_WAIT_FOREVER` for no
 * deadline; `SUBETHA_E_TIMEOUT` when none arrived. The event loop's own
 * wait on the native object is what a consumer normally uses; this is
 * the same wait for a caller without one.
 */
int32_t subetha_notifier_wait(subetha_handle handle, int64_t timeout_ms);

/**
 * Make an exact-delivery receiver for `consumer_id` on the stamped ring
 * `ring` names; the strategy follows the ring's stamp source and the
 * ring's merge mode is set to match. `SUBETHA_E_RING_NOT_STAMPED` on a
 * ring built without stamps.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ring_ordered_receiver(subetha_handle ring,
                                      uint32_t consumer_id,
                                      uint32_t mode,
                                      subetha_handle *out);

/**
 * The next item in exact order into `out`, at least
 * `SUBETHA_RING_SLOT_BYTES`, its length into `out_len` and its stamp into
 * `out_stamp`. `SUBETHA_E_RING_EMPTY` while nothing is ready, which under
 * the reorder strategy includes the window still filling.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` and `out_stamp` are
 * valid pointers.
 */
int32_t subetha_ordered_try_next(subetha_handle handle,
                                 uint8_t *out,
                                 size_t cap,
                                 size_t *out_len,
                                 uint64_t *out_stamp);

/**
 * `subetha_ordered_try_next`, parking on the ring's consumer waker until
 * a push arrives or `timeout_ms` elapses; the waiting contract is
 * `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` and `out_stamp` are
 * valid pointers.
 */
int32_t subetha_ordered_next_wait(subetha_handle handle,
                                  uint8_t *out,
                                  size_t cap,
                                  size_t *out_len,
                                  uint64_t *out_stamp,
                                  int64_t timeout_ms);

/**
 * One item the window still holds, in order; call in a loop at the end
 * of the stream until `SUBETHA_E_RING_EMPTY`.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` and `out_stamp` are
 * valid pointers.
 */
int32_t subetha_ordered_flush(subetha_handle handle,
                              uint8_t *out,
                              size_t cap,
                              size_t *out_len,
                              uint64_t *out_stamp);

/**
 * A snapshot of the receiver into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ordered_read_stats(subetha_handle handle, struct subetha_ordered_stats *out);

/**
 * Obtain the lease at `path` over a payload of `payload_size` bytes,
 * initialized with the `len` bytes at `initial` when the file does not
 * yet exist. An existing lease is attached with its owner and term in
 * place and `initial` goes unused. A region built for another payload
 * size is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `initial` points to `len`
 * readable bytes; `out` is a valid pointer.
 */
int32_t subetha_owner_lease_create(const char *path,
                                   const uint8_t *initial,
                                   size_t len,
                                   uint32_t payload_size,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Attach to the lease another process created at `path`; the file must
 * exist and must declare `payload_size`. `SUBETHA_E_RING_IO` names an
 * absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_owner_lease_open(const char *path,
                                 uint32_t payload_size,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Truncate the file at `path` and lay out an unheld lease there,
 * stripping whatever owner and term a live holder has.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `initial` points to `len`
 * readable bytes; `out` is a valid pointer.
 */
int32_t subetha_owner_lease_reset(const char *path,
                                  const uint8_t *initial,
                                  size_t len,
                                  uint32_t payload_size,
                                  uint32_t mode,
                                  subetha_handle *out);

/**
 * Try to take the lease for `pid`, and write whether it was taken into
 * `out_acquired`. It is taken when nobody holds it, when `pid` is below
 * the current holder's and so preempts it, or when the holder's
 * heartbeat has fallen more than `grace_epochs` behind the global epoch.
 * A caller that already holds it is told so rather than refused.
 *
 * # Safety
 * `out_acquired` is a valid pointer.
 */
int32_t subetha_owner_lease_try_acquire(subetha_handle handle,
                                        uint32_t pid,
                                        uint64_t grace_epochs,
                                        bool *out_acquired);

/**
 * Give the lease up, and write whether `pid` was holding it into
 * `out_released`.
 *
 * # Safety
 * `out_released` is null or a valid pointer.
 */
int32_t subetha_owner_lease_release(subetha_handle handle, uint32_t pid, bool *out_released);

/**
 * Copy the payload into `out`, at least `payload_size` bytes, and its
 * length into `out_len`. `SUBETHA_E_NOT_OWNER` when `pid` does not hold
 * the lease, in which case `out` is untouched.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_owner_lease_read(subetha_handle handle,
                                 uint32_t pid,
                                 uint8_t *out,
                                 size_t cap,
                                 size_t *out_len);

/**
 * Overwrite the payload with the `len` bytes at `data`, at most
 * `payload_size` of them, under the version so a reader never sees half
 * of each value. `SUBETHA_E_NOT_OWNER` when `pid` does not hold the
 * lease, in which case nothing is written.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_owner_lease_write(subetha_handle handle,
                                  uint32_t pid,
                                  const uint8_t *data,
                                  size_t len);

/**
 * Refresh the holder's heartbeat, and write whether `pid` still holds
 * the lease into `out_still_owner`. A holder calls this faster than the
 * epoch is ticked, or the grace window expires under it.
 *
 * # Safety
 * `out_still_owner` is null or a valid pointer.
 */
int32_t subetha_owner_lease_beat(subetha_handle handle, uint32_t pid, bool *out_still_owner);

/**
 * Advance the global epoch and write it into `out_epoch`. The grace
 * window is measured against this, so a stale holder is only detected
 * once something ticks it; whatever does so sets how quickly that
 * happens.
 *
 * # Safety
 * `out_epoch` is null or a valid pointer.
 */
int32_t subetha_owner_lease_tick_epoch(subetha_handle handle, uint64_t *out_epoch);

/**
 * A snapshot of the lease into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_owner_lease_read_stats(subetha_handle handle,
                                       struct subetha_owner_lease_stats *out);

/**
 * Write the lease's page to its file.
 */
int32_t subetha_owner_lease_flush(subetha_handle handle);

/**
 * Remove the lease's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_owner_lease_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * This process's id, which is what a lease is held by. A C caller can
 * reach its own platform's call for this; it is here so a program using
 * the ABI needs nothing else to work the lease.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_current_pid(uint32_t *out);

/**
 * Obtain the stack at `path`: an empty one is initialized when the file
 * does not exist, and an existing one is attached with its entries in
 * place. A file built with another capacity or layout is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`. `capacity` is at least 1;
 * `scan_interval_us` in the options is ignored, the stack has no sidecar.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout`, `options` and `out`
 * are valid pointers.
 */
int32_t subetha_stack_create(const char *path,
                             uint32_t capacity,
                             const struct subetha_element_layout *layout,
                             const struct subetha_ring_options *options,
                             subetha_handle *out);

/**
 * Attach to the stack another process created at `path`; the file must
 * exist, which is what tells this apart from `subetha_stack_create`.
 * `SUBETHA_E_RING_IO` names an absent file and
 * `SUBETHA_E_RING_LAYOUT_MISMATCH` one of another shape.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout`, `options` and `out`
 * are valid pointers.
 */
int32_t subetha_stack_open(const char *path,
                           uint32_t capacity,
                           const struct subetha_element_layout *layout,
                           const struct subetha_ring_options *options,
                           subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty stack there,
 * discarding every entry other handles share; for a caller that owns the
 * path. On Windows the file must not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout`, `options` and `out`
 * are valid pointers.
 */
int32_t subetha_stack_reset(const char *path,
                            uint32_t capacity,
                            const struct subetha_element_layout *layout,
                            const struct subetha_ring_options *options,
                            subetha_handle *out);

/**
 * Push up to `element_size` bytes without waiting; the element is zero
 * past the payload. `SUBETHA_E_RING_FULL` when the stack is at capacity,
 * `SUBETHA_E_RING_PAYLOAD_TOO_LARGE` for more bytes than an element.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_stack_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Push `count` elements of `len` bytes each, the first at `items` and
 * each next one `stride` bytes on, under one handle lookup and one panic
 * guard. The batch contract is `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_stack_try_push_many(subetha_handle handle,
                                    const uint8_t *items,
                                    size_t stride,
                                    size_t len,
                                    size_t count,
                                    size_t *out_done);

/**
 * Pop up to `count` elements, the first written at `out` and each next
 * one `stride` bytes on; `stride` is at least the element size. The batch
 * contract is `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_stack_try_pop_many(subetha_handle handle,
                                   uint8_t *out,
                                   size_t stride,
                                   size_t count,
                                   size_t *out_done);

/**
 * Push, parking until there is room or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_stack_push_wait(subetha_handle handle,
                                const uint8_t *data,
                                size_t len,
                                int64_t timeout_ms);

/**
 * Pop the top element, `element_size` bytes, into `out` without waiting;
 * `cap` must be at least that. `SUBETHA_E_RING_EMPTY` when there is
 * nothing to take.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_stack_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Pop, parking until an element arrives or `timeout_ms` elapses; the
 * waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_stack_pop_wait(subetha_handle handle,
                               uint8_t *out,
                               size_t cap,
                               size_t *out_len,
                               int64_t timeout_ms);

/**
 * Copy the top element into `out` without popping it. The copy is a
 * snapshot: a pop on another thread can retire the element while it is
 * read. `SUBETHA_E_RING_EMPTY` when the stack is empty.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_stack_peek(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * A snapshot of the stack into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_stack_read_stats(subetha_handle handle, struct subetha_stack_stats *out);

/**
 * Write the stack's dirty pages to its file.
 */
int32_t subetha_stack_flush(subetha_handle handle);

/**
 * Wake every thread parked in a wait on this stack; each re-checks the
 * stack and parks again unless it finds what it waited for.
 */
int32_t subetha_stack_wake_all(subetha_handle handle);

/**
 * Remove the stack file at `path` and its two waker files. The contract
 * is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_stack_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Create a pub/sub ring in anonymous memory. `capacity` is a power of two
 * of at least 2; `scan_interval_us` in the options is ignored, the ring
 * has no sidecar.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_pubsub_create_anon(uint32_t capacity,
                                   const struct subetha_ring_options *options,
                                   subetha_handle *out);

/**
 * Create a file-backed pub/sub ring at `path`, or attach to one that
 * exists there with the same capacity, with the waker at
 * `<path>.cwaker.bin`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_pubsub_create(const char *path,
                              uint32_t capacity,
                              const struct subetha_ring_options *options,
                              subetha_handle *out);

/**
 * Attach to a file-backed pub/sub ring another process created at `path`.
 * `SUBETHA_E_RING_LAYOUT_MISMATCH` when the file is absent or was created
 * with another capacity.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_pubsub_open(const char *path,
                            uint32_t expected_capacity,
                            const struct subetha_ring_options *options,
                            subetha_handle *out);

/**
 * Create a pub/sub ring in the named shared-memory region `name`, with
 * its waker in `{name}_cwaker`.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_pubsub_create_shm(const char *name,
                                  uint32_t capacity,
                                  uint32_t shm_namespace,
                                  const struct subetha_ring_options *options,
                                  subetha_handle *out);

/**
 * Attach to a pub/sub ring another process created in named shared
 * memory; the namespace must match the creator's.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_pubsub_open_shm(const char *name,
                                uint32_t expected_capacity,
                                uint32_t shm_namespace,
                                const struct subetha_ring_options *options,
                                subetha_handle *out);

/**
 * Publish up to `SUBETHA_PUBSUB_PAYLOAD_BYTES` bytes, never waiting: the
 * oldest slot is overwritten whether or not a subscriber has read it. The
 * position assigned lands in `out_position` when it is not null. One
 * publisher at a time is the caller's contract.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_position` is null or a
 * valid pointer.
 */
int32_t subetha_pubsub_publish(subetha_handle handle,
                               const uint8_t *data,
                               size_t len,
                               uint64_t *out_position);

/**
 * Read the slot at an absolute `position` into `out`, which holds at
 * least `SUBETHA_PUBSUB_PAYLOAD_BYTES`. `SUBETHA_E_PUBSUB_PENDING` when it
 * is not published yet, `SUBETHA_E_PUBSUB_LOST` when it was overwritten.
 *
 * # Safety
 * `out` points to `cap` writable bytes.
 */
int32_t subetha_pubsub_read_at(subetha_handle handle, uint64_t position, uint8_t *out, size_t cap);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_pubsub_read_stats(subetha_handle handle, struct subetha_pubsub_stats *out);

/**
 * Wake every subscriber parked in a wait on this ring; each re-checks and
 * parks again unless it finds what it waited for.
 */
int32_t subetha_pubsub_wake_all(subetha_handle handle);

/**
 * Subscribe to the ring `handle` names, starting at `position`, with the
 * position kept in the handle. `subetha_pubsub_read_stats` gives the
 * head, which is where a subscriber that wants only new items starts.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_pubsub_subscribe(subetha_handle handle,
                                 uint64_t position,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Subscribe with the position kept in the file `position_path`, which
 * is created at `initial` when `create` is true and opened as it is when
 * false, so a subscriber that restarts resumes where it stopped.
 *
 * # Safety
 * `position_path` is a NUL-terminated UTF-8 string; `out` is a valid
 * pointer.
 */
int32_t subetha_pubsub_subscribe_file(subetha_handle handle,
                                      const char *position_path,
                                      uint64_t initial,
                                      bool create,
                                      uint32_t mode,
                                      subetha_handle *out);

/**
 * Read the item at the subscriber's position into `out`, at least
 * `SUBETHA_PUBSUB_PAYLOAD_BYTES`, and advance past it.
 * `SUBETHA_E_PUBSUB_PENDING` when nothing newer is published;
 * `SUBETHA_E_PUBSUB_LOST` when the position was overwritten, after which
 * the subscriber stands at the ring's head.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_subscriber_try_next(subetha_handle handle,
                                    uint8_t *out,
                                    size_t cap,
                                    size_t *out_len);

/**
 * `subetha_subscriber_try_next`, parking while nothing newer is
 * published, until a publish or `timeout_ms` elapses; a lost position
 * returns at once. The waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_subscriber_next_wait(subetha_handle handle,
                                     uint8_t *out,
                                     size_t cap,
                                     size_t *out_len,
                                     int64_t timeout_ms);

/**
 * The subscriber's position, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_subscriber_position(subetha_handle handle, uint64_t *out);

/**
 * Move the subscriber's position forward by `by` without reading; the
 * new position lands in `out` when it is not null.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_subscriber_skip(subetha_handle handle, uint64_t by, uint64_t *out);

/**
 * Set the subscriber's position outright.
 */
int32_t subetha_subscriber_set_position(subetha_handle handle, uint64_t position);

/**
 * The mode a subscriber was created in, into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_subscriber_mode(subetha_handle handle, uint32_t *out);

/**
 * Remove the ring file at `path` and its waker file. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_pubsub_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Remove a subscriber's position file. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `position_path` is a NUL-terminated UTF-8 string; `report` is null or
 * a valid pointer.
 */
int32_t subetha_pubsub_unlink_position(const char *position_path,
                                       struct subetha_unlink_report *report);

/**
 * Obtain the region at `path` for `capacity` elements of `layout`: an
 * empty one is initialized when the file does not exist, an existing one
 * is attached with its allocated slots and free list in place, so indexes
 * other processes hold keep resolving. A file built with another capacity
 * or layout is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_region_create(const char *path,
                              uint32_t capacity,
                              const struct subetha_element_layout *layout,
                              uint32_t mode,
                              subetha_handle *out);

/**
 * Attach to the region another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_region_open(const char *path,
                            uint32_t capacity,
                            const struct subetha_element_layout *layout,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty region there,
 * invalidating every index other handles hold. On Windows the file must
 * not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_region_reset(const char *path,
                             uint32_t capacity,
                             const struct subetha_element_layout *layout,
                             uint32_t mode,
                             subetha_handle *out);

/**
 * Allocate a slot holding `data`, exactly `element_size` bytes, and write
 * its index into `out_index`. `SUBETHA_E_RING_FULL` when the free list
 * and the bump cursor are both exhausted.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_index` is a valid pointer.
 */
int32_t subetha_region_allocate(subetha_handle handle,
                                const uint8_t *data,
                                size_t len,
                                uint32_t *out_index);

/**
 * Free the slot at `index`, copying the element it held into `out`, at
 * least `element_size` bytes, and its length into `out_len`. The slot
 * returns to the free list and is handed out again.
 * `SUBETHA_E_OUT_OF_BOUNDS` for `SUBETHA_REGION_NIL_INDEX` or an index at
 * or past the capacity.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_region_free(subetha_handle handle,
                            uint32_t index,
                            uint8_t *out,
                            size_t cap,
                            size_t *out_len);

/**
 * Copy the element at `index` into `out`, at least `element_size` bytes,
 * and its length into `out_len`. Whether the slot is still allocated is
 * the caller's knowledge. `SUBETHA_E_OUT_OF_BOUNDS` for
 * `SUBETHA_REGION_NIL_INDEX` or an index at or past the capacity.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_region_get(subetha_handle handle,
                           uint32_t index,
                           uint8_t *out,
                           size_t cap,
                           size_t *out_len);

/**
 * Overwrite the element at `index` with `data`, exactly `element_size`
 * bytes. `SUBETHA_E_OUT_OF_BOUNDS` for `SUBETHA_REGION_NIL_INDEX` or an
 * index at or past the capacity.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_region_set(subetha_handle handle, uint32_t index, const uint8_t *data, size_t len);

/**
 * Empty the region for every handle: the bump cursor goes back to zero
 * and the free list to none, so every index handed out is stale. Not safe
 * against an allocate or a free running in any process.
 */
int32_t subetha_region_clear(subetha_handle handle);

/**
 * A snapshot of the region into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_region_read_stats(subetha_handle handle, struct subetha_region_stats *out);

/**
 * Write the region's dirty pages to its file.
 */
int32_t subetha_region_flush(subetha_handle handle);

/**
 * Remove the region file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_region_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the sampler at `path` holding `capacity` records: an empty one
 * is initialized when the file does not exist, an existing one is
 * attached with its sample in place. A sampler built with another
 * capacity is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_reservoir_create(const char *path,
                                 uint64_t capacity,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the sampler another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_reservoir_open(const char *path,
                               uint64_t capacity,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Offer the record at `item`, exactly `SUBETHA_RESERVOIR_ITEM_BYTES`
 * bytes, to the sampler. Whether it was kept, and in which slot, goes
 * into `out_slot` when that is not null: a slot index when the record
 * entered the reservoir, and `UINT64_MAX` when the sampler declined it.
 *
 * # Safety
 * `item` points to `len` readable bytes; `out_slot` is null or a valid
 * pointer.
 */
int32_t subetha_reservoir_record(subetha_handle handle,
                                 const uint8_t *item,
                                 size_t len,
                                 uint64_t *out_slot);

/**
 * The sample as it stands into `out`, which holds `cap` bytes, and the
 * records written into `out_len`. `SUBETHA_E_BUFFER_TOO_SMALL` when `cap`
 * is below what the sample needs, with the byte count in `out_len`.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_reservoir_snapshot(subetha_handle handle,
                                   uint8_t *out,
                                   size_t cap,
                                   size_t *out_len);

/**
 * Forget the stream: the sampler counts from zero again and its next
 * records fill the reservoir. Peers sharing the file see the same reset.
 */
int32_t subetha_reservoir_reset(subetha_handle handle);

/**
 * Push the sampler's dirty pages to disk, returning when they are
 * durable.
 */
int32_t subetha_reservoir_flush(subetha_handle handle);

/**
 * Start pushing the sampler's dirty pages to disk and return at once.
 */
int32_t subetha_reservoir_flush_async(subetha_handle handle);

/**
 * A snapshot of the sampler into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_reservoir_read_stats(subetha_handle handle, struct subetha_reservoir_stats *out);

/**
 * Create a ring in anonymous memory, reachable from this process only.
 * `capacity` is slots per backing and must be a power of two of at least 2.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_ring_create_anon(uint32_t max_producers,
                                 uint32_t max_consumers,
                                 uint32_t capacity,
                                 const struct subetha_ring_options *options,
                                 subetha_handle *out);

/**
 * Create a file-backed ring under `path_prefix`, or attach to one that
 * exists there with the same sizing. The backings are named
 * `<path_prefix>.spsc.bin`, `.mpsc.<i>.bin`, `.mpmc.<i>.bin`,
 * `.vyukov.bin`, `.peers.bin`, plus `.cwaker.bin` and `.pwaker.bin` for
 * the waiters; a frame region and an ordering region may appear beside
 * them later.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_ring_create(const char *path_prefix,
                            uint32_t max_producers,
                            uint32_t max_consumers,
                            uint32_t capacity,
                            const struct subetha_ring_options *options,
                            subetha_handle *out);

/**
 * Attach to a file-backed ring another process created under
 * `path_prefix`. Fails with `SUBETHA_E_RING_LAYOUT_MISMATCH` when a backing
 * is absent or was created with another capacity.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `options` and `out` are
 * valid pointers.
 */
int32_t subetha_ring_open(const char *path_prefix,
                          uint32_t max_producers,
                          uint32_t max_consumers,
                          uint32_t expected_capacity,
                          const struct subetha_ring_options *options,
                          subetha_handle *out);

/**
 * Create a ring in named shared memory under `name`, in the namespace
 * `SUBETHA_SHM_SESSION` or `SUBETHA_SHM_MACHINE`. The regions are named
 * `{name}_spsc`, `{name}_mpsc_<i>`, `{name}_mpmc_<i>`, `{name}_vyukov`,
 * plus `{name}_cwaker` and `{name}_pwaker`.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_ring_create_shm(const char *name,
                                uint32_t max_producers,
                                uint32_t max_consumers,
                                uint32_t capacity,
                                uint32_t shm_namespace,
                                const struct subetha_ring_options *options,
                                subetha_handle *out);

/**
 * Attach to a ring another process created in named shared memory. The
 * namespace must match the creator's: a mismatch resolves a different,
 * empty set of regions rather than failing.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_ring_open_shm(const char *name,
                              uint32_t max_producers,
                              uint32_t max_consumers,
                              uint32_t expected_capacity,
                              uint32_t shm_namespace,
                              const struct subetha_ring_options *options,
                              subetha_handle *out);

/**
 * Register as a producer and receive the id every push takes. Past the
 * creation hint the ring grows a backing for the new producer.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ring_register_producer(subetha_handle handle, uint32_t *out);

/**
 * Register as a consumer and receive the id every pop takes.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ring_register_consumer(subetha_handle handle, uint32_t *out);

/**
 * Give a producer id back. The ring re-morphs to the live peer counts.
 */
int32_t subetha_ring_unregister_producer(subetha_handle handle, uint32_t producer_id);

/**
 * Give a consumer id back. The ring re-morphs to the live peer counts.
 */
int32_t subetha_ring_unregister_consumer(subetha_handle handle, uint32_t consumer_id);

/**
 * Push `len` bytes from `data` without waiting. `SUBETHA_E_RING_FULL` when
 * there is no room; `SUBETHA_E_RING_PAYLOAD_TOO_LARGE` above the slot.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_ring_try_push(subetha_handle handle,
                              uint32_t producer_id,
                              const uint8_t *data,
                              size_t len);

/**
 * Pop one item into `out` without waiting. The ring carries fixed slots:
 * a push zero-fills the slot past its payload and a pop yields the whole
 * slot, so `out_len` is the slot's size and the payload's own length does
 * not survive the round trip; a caller that needs it carries it inside
 * the payload. `cap` must be at least `SUBETHA_RING_SLOT_BYTES`.
 * `SUBETHA_E_RING_EMPTY` when there is nothing to take.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_ring_try_pop(subetha_handle handle,
                             uint32_t consumer_id,
                             uint8_t *out,
                             size_t cap,
                             size_t *out_len);

/**
 * Push `count` payloads of `len` bytes each, the first at `items` and
 * each next one `stride` bytes on, under one handle lookup and one panic
 * guard. `out_done` receives how many landed: a full ring stops the run
 * early, and the caller comes back with the rest. The code is
 * `SUBETHA_OK` when at least one landed, and the refusal's own when the
 * first push was the one that failed.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_ring_try_push_many(subetha_handle handle,
                                   uint32_t producer_id,
                                   const uint8_t *items,
                                   size_t stride,
                                   size_t len,
                                   size_t count,
                                   size_t *out_done);

/**
 * Pop up to `count` slots, the first written at `out` and each next one
 * `stride` bytes on, under one handle lookup and one panic guard;
 * `stride` is at least `SUBETHA_RING_SLOT_BYTES`. `out_done` receives how
 * many were taken: an empty ring stops the run early. The code is
 * `SUBETHA_OK` when at least one was taken, and `SUBETHA_E_RING_EMPTY`
 * when none was.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_ring_try_pop_many(subetha_handle handle,
                                  uint32_t consumer_id,
                                  uint8_t *out,
                                  size_t stride,
                                  size_t count,
                                  size_t *out_done);

/**
 * Push, parking until there is room or `timeout_ms` elapses
 * (`SUBETHA_E_TIMEOUT`). `SUBETHA_WAIT_FOREVER` waits without a deadline.
 * `SUBETHA_E_RING_WAKER_FULL` means every waiter slot was in use and
 * nothing was pushed; `SUBETHA_E_DESTROYED` means the ring was destroyed
 * while this call waited.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_ring_push_wait(subetha_handle handle,
                               uint32_t producer_id,
                               const uint8_t *data,
                               size_t len,
                               int64_t timeout_ms);

/**
 * Pop, parking until an item arrives or `timeout_ms` elapses
 * (`SUBETHA_E_TIMEOUT`); the slot semantics are `subetha_ring_try_pop`'s.
 * `SUBETHA_WAIT_FOREVER` waits without a deadline.
 * `SUBETHA_E_RING_WAKER_FULL` means every waiter slot was in use and
 * nothing was taken; `SUBETHA_E_DESTROYED` means the ring was destroyed
 * while this call waited.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_ring_pop_wait(subetha_handle handle,
                              uint32_t consumer_id,
                              uint8_t *out,
                              size_t cap,
                              size_t *out_len,
                              int64_t timeout_ms);

/**
 * Pop on a stamped ring, also yielding the item's stamp into `out_stamp`,
 * so a consumer can check the order it paid for instead of trusting it.
 * Otherwise `subetha_ring_try_pop`. `SUBETHA_E_RING_NOT_STAMPED` on a ring
 * built without stamps; under a merge mode `SUBETHA_E_RING_NOT_DRAINER`
 * while another consumer holds the drainer lease.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` and `out_stamp` are
 * valid pointers.
 */
int32_t subetha_ring_try_pop_stamped(subetha_handle handle,
                                     uint32_t consumer_id,
                                     uint8_t *out,
                                     size_t cap,
                                     size_t *out_len,
                                     uint64_t *out_stamp);

/**
 * `subetha_ring_try_pop_stamped`, parking until an item arrives or
 * `timeout_ms` elapses; the waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` and `out_stamp` are
 * valid pointers.
 */
int32_t subetha_ring_pop_wait_stamped(subetha_handle handle,
                                      uint32_t consumer_id,
                                      uint8_t *out,
                                      size_t cap,
                                      size_t *out_len,
                                      uint64_t *out_stamp,
                                      int64_t timeout_ms);

/**
 * Set the ordering mode of a stamped ring, one of the `SUBETHA_ORDERING_`
 * constants. The mode lives in the ring's ordering region, so every
 * process attached reads the same value; a flip takes effect on the next
 * pop with no drain. `SUBETHA_E_RING_NOT_STAMPED` on a ring built without
 * stamps.
 */
int32_t subetha_ring_set_ordering_mode(subetha_handle handle, uint32_t mode);

/**
 * Under `SUBETHA_ORDERING_MERGE_STRICT` an idle producer refreshes its
 * watermark now and then so the merge does not wait on it; a producer
 * that pushes steadily needs no refresh. `SUBETHA_E_RING_NOT_STAMPED` on
 * a ring built without stamps.
 */
int32_t subetha_ring_refresh_watermark(subetha_handle handle, uint32_t producer_id);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the ring
 * and parks again unless it finds what it waited for. For a caller that
 * changed something a waiter would want to see.
 */
int32_t subetha_ring_wake_all(subetha_handle handle);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ring_read_stats(subetha_handle handle, struct subetha_ring_stats *out);

/**
 * Remove every file a file-backed ring under `path_prefix` names,
 * including the two waker files, so no later process attaches to it. Live
 * handles keep their mappings until destroyed; on Windows a mapped file
 * cannot be removed and that refusal is counted. `max_producers` is the
 * creation hint, the floor for how many per-producer backings are
 * looked for. The counts land in `report` when it is not null. Returns
 * `SUBETHA_E_RING_IO` when any removal was refused, with the first refusal
 * named in the detail; a missing file is not a refusal.
 *
 * # Safety
 * `path_prefix` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_ring_unlink(const char *path_prefix,
                            uint32_t max_producers,
                            struct subetha_unlink_report *report);

/**
 * Attach a pollable notifier to the ring `ring` names for this process
 * to watch: every push through the ABI in any process on the ring
 * signals it. The new handle is of kind `SUBETHA_KIND_NOTIFIER`;
 * `subetha_notifier_native` hands out its file descriptor or event
 * `HANDLE`, `subetha_notifier_drain` clears it, and destroying the handle
 * detaches it. Beside a file-backed ring the notifier is
 * `<prefix>.notify.<index>` on Unix; the record every process reads is
 * `<prefix>.notify.bin`, removed by `subetha_ring_unlink`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_ring_notifier(subetha_handle ring, subetha_handle *out);

/**
 * Send a frame of any length up to the payload region's block size.
 * Up to `SUBETHA_RING_FRAME_INLINE_BUDGET` bytes ride inside the slot; a
 * larger frame lands in the payload region, created on the first spill,
 * and the slot carries its offset. `layout_hint` is one of the
 * `SUBETHA_LAYOUT_` constants; the path taken lands in `out_class` when it
 * is not null. Frames and ordering stamps both claim the slot head, so a
 * ring built with stamps refuses this with `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 * Every shape carries frames the same way.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_class` is null or a valid
 * pointer.
 */
int32_t subetha_ring_send_frame(subetha_handle handle,
                                uint32_t producer_id,
                                const uint8_t *data,
                                size_t len,
                                uint32_t layout_hint,
                                uint32_t *out_class);

/**
 * `subetha_ring_send_frame`, parking until there is room or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_class` is null or a valid
 * pointer.
 */
int32_t subetha_ring_send_frame_wait(subetha_handle handle,
                                     uint32_t producer_id,
                                     const uint8_t *data,
                                     size_t len,
                                     uint32_t layout_hint,
                                     int64_t timeout_ms,
                                     uint32_t *out_class);

/**
 * Receive one frame into `out`, its length into `out_len`; a frame
 * carries its own length, unlike a slot. A frame larger than `cap` is not
 * lost: it is kept for this consumer id, the call returns
 * `SUBETHA_E_BUFFER_TOO_SMALL` with the size needed in `out_len`, and the
 * next call for the same consumer with room yields it. Not available on
 * a ring built with ordering stamps. `SUBETHA_E_RING_EMPTY` when there is
 * nothing to take.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_ring_recv_frame(subetha_handle handle,
                                uint32_t consumer_id,
                                uint8_t *out,
                                size_t cap,
                                size_t *out_len);

/**
 * `subetha_ring_recv_frame`, parking until a frame arrives or `timeout_ms`
 * elapses; the waiting contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_ring_recv_frame_wait(subetha_handle handle,
                                     uint32_t consumer_id,
                                     uint8_t *out,
                                     size_t cap,
                                     size_t *out_len,
                                     int64_t timeout_ms);

/**
 * A shared-memory ring's names are released by the OS with the last
 * handle that maps them: on Unix the creating handle unlinks each name
 * when it is destroyed, on Windows the section vanishes when the last
 * handle closes. There is nothing to remove by name, so this reports zero
 * removed and succeeds; it exists so the two locales have the same
 * lifecycle calls.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_ring_unlink_shm(const char *name,
                                uint32_t shm_namespace,
                                struct subetha_unlink_report *report);

/**
 * Initialize the library with the mode objects take when they ask for
 * `SUBETHA_MODE_DEFAULT`. Required before any other call. Calling it again
 * with the same default succeeds; with a different default it fails with
 * `SUBETHA_E_INIT_CONFLICT` and changes nothing. After `subetha_shutdown`,
 * calling it again re-initializes.
 */
int32_t subetha_init(uint32_t default_mode);

/**
 * Shut the library down: destroy every live handle, joining any thread an
 * object owns, and refuse every later call until `subetha_init` runs
 * again. Returns `SUBETHA_E_HANDLES_WERE_LIVE` when handles had to be
 * closed, with the count in the detail and a line on stderr, since a
 * handle still open at shutdown is a caller that forgot one.
 */
int32_t subetha_shutdown(void);

/**
 * How many thread words the borrow guard's registry holds.
 *
 * Every reclamation walks this after its barrier, so it is the second
 * term in what closing a handle costs, and it only ever grows: a word is
 * returned when its thread ends but the table keeps it for the next one.
 * Exposed so a benchmark can record the walk length it measured against
 * rather than infer it from the order its rows ran in.
 */
uint64_t subetha_borrow_registry_len(void);

/**
 * The ABI version as `(major << 16) | (minor << 8) | patch`. Safe before
 * `subetha_init`.
 */
uint32_t subetha_abi_version(void);

/**
 * The ABI version as a static string. Safe before `subetha_init`.
 */
const char *subetha_abi_version_string(void);

/**
 * The version of the crate this library was built from, as a string that
 * lives for the whole process. Safe before `subetha_init`.
 */
const char *subetha_crate_version_string(void);

/**
 * The process default mode, into `out`.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_default_mode(uint32_t *out);

/**
 * Panics caught at the boundary since the process started. Safe before
 * `subetha_init`.
 */
uint64_t subetha_panic_count(void);

/**
 * Copy the most recent caught panic's message into `buf`, NUL-terminated,
 * and return the size the whole message needs including the terminator;
 * the same contract as `subetha_last_error_detail`. Zero means no panic has
 * been caught.
 *
 * # Safety
 * `buf` is null or points to `cap` writable bytes.
 */
size_t subetha_last_panic_message(char *buf, size_t cap);

/**
 * Handles currently live, poisoned ones included.
 */
uint64_t subetha_live_handles(void);

/**
 * Destroy the object a handle names. Refuses new calls on it, wakes any
 * thread waiting inside it (those calls return `SUBETHA_E_DESTROYED`),
 * waits for every call in flight to leave, then drops the object, joining
 * any thread it owns. A poisoned handle is destroyed the same way. The
 * handle names nothing afterwards, and can never name anything again.
 */
int32_t subetha_handle_destroy(subetha_handle handle);

/**
 * The kind of object a handle names, into `out`: one of the `SUBETHA_KIND_`
 * constants. Works on a poisoned handle.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_handle_kind(subetha_handle handle, uint32_t *out);

/**
 * Whether a handle is poisoned, into `out`.
 *
 * # Safety
 * `out` is null or a valid pointer.
 */
int32_t subetha_handle_is_poisoned(subetha_handle handle, bool *out);

#if defined(SUBETHA_TEST_HOOKS)
/**
 * Panic inside a call on `handle`, so a test can watch the boundary catch
 * it and poison the handle. Present only with the `test-hooks` feature.
 */
int32_t subetha_test_panic_on(subetha_handle handle);
#endif

#if defined(SUBETHA_TEST_HOOKS)
/**
 * Panic inside a call that touches no handle. Present only with the
 * `test-hooks` feature.
 */
int32_t subetha_test_panic_free(void);
#endif

#if defined(SUBETHA_TEST_HOOKS)
/**
 * Return at once from inside the panic guard, so a bench can price the
 * guard apart from a borrow. Present only with the `test-hooks` feature.
 */
int32_t subetha_test_entry_only(void);
#endif

#if defined(SUBETHA_TEST_HOOKS)
/**
 * Borrow the handle as `kind` and return at once, so a bench can price
 * the borrow and its guard apart from any family's work. Present only
 * with the `test-hooks` feature.
 */
int32_t subetha_test_borrow_only(subetha_handle handle, uint32_t kind);
#endif

#if defined(SUBETHA_TEST_HOOKS)
/**
 * Write this process's recent ring-trace events to stderr, oldest
 * first, each prefixed with `RING-TRACE` and this process id. Returns
 * the number of lines written, or `SUBETHA_E_PANIC`.
 *
 * The trace is a per-process buffer, so a workload split across
 * processes keeps one per process and a parent that creates no rings
 * has an empty one. A child is the only place its own history exists,
 * which is why a role calls this on its own failure path instead of
 * leaving the parent to ask for something it cannot see.
 *
 * `ring_index` selects a sub-ring. Two kinds of event carry no ring of
 * their own and so appear whatever is passed:
 *
 * - a peer taking or releasing a slot,
 * - the counts a shape decision read.
 *
 * A dump without those cannot show whether a slot was held twice or
 * merely in turn.
 *
 * Zero in a release build, where the trace is not compiled. Present
 * only with the `test-hooks` feature.
 */
int32_t subetha_test_ring_trace_dump(size_t ring_index, size_t limit);
#endif

/**
 * Obtain the lock at `path`: an empty one is initialized when the file
 * does not exist, an existing one is attached with whatever holds are
 * live, so a caller arriving while another process holds the write lock
 * does not clear it.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_rwlock_create(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Attach to the lock another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_rwlock_open(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an unheld lock there,
 * discarding whatever a live holder owns.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_rwlock_reset(const char *path, uint32_t mode, subetha_handle *out);

/**
 * Take a hold for reading without waiting, and write a handle naming it
 * into `out`. `SUBETHA_E_WOULD_BLOCK` while a writer holds the lock or
 * is waiting for it, which is what stops a stream of readers starving a
 * writer.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rwlock_try_read(subetha_handle handle, uint64_t *out);

/**
 * Take a hold for writing without waiting, and write a handle naming it
 * into `out`. `SUBETHA_E_WOULD_BLOCK` while anyone holds the lock at
 * all.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rwlock_try_write(subetha_handle handle, uint64_t *out);

/**
 * `subetha_rwlock_try_read`, waiting up to `timeout_ms` for the lock;
 * `SUBETHA_WAIT_FOREVER` waits without a deadline.
 * `SUBETHA_E_TIMEOUT` when the wait elapses and `SUBETHA_E_DESTROYED`
 * when the lock's handle is destroyed under it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rwlock_read(subetha_handle handle, int64_t timeout_ms, uint64_t *out);

/**
 * `subetha_rwlock_try_write`, waiting up to `timeout_ms` for the lock.
 * The contract is `subetha_rwlock_read`'s, and for as long as this waits
 * it counts in `waiting_writers`, so readers arriving after it are
 * refused and it takes the lock ahead of them.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rwlock_write(subetha_handle handle, int64_t timeout_ms, uint64_t *out);

/**
 * Whether `token` names a hold taken for reading or for writing:
 * `SUBETHA_LOCK_READ` or `SUBETHA_LOCK_WRITE`. A token that names no
 * live hold is `SUBETHA_E_INVALID_ARGUMENT`, so this also answers
 * whether a token is still good.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rwlock_hold_kind(subetha_handle handle, uint64_t token, uint32_t *out);

/**
 * A snapshot of the lock into `out`. Every count races an acquire or a
 * release in some process, so it is a diagnostic rather than something
 * to branch on.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_rwlock_read_stats(subetha_handle handle, struct subetha_rwlock_stats *out);

/**
 * Give the lock back and close this hold's handle, which names nothing
 * afterwards. Another process waiting on the lock is free to take it as
 * soon as this returns.
 *
 * This is the call to use to unlock. `subetha_handle_destroy` on a hold
 * does the same thing to the caller's eye, but pays a process-wide
 * barrier for this one handle where this call pays one for a batch of
 * them - about 331 ns against about 5 ns on an idle process, and
 * 5.90 us against the same 5 ns once other threads are running. A lock
 * taken and released in a loop should not be paying that.
 */
int32_t subetha_rwlock_unlock(subetha_handle handle, uint64_t token);

/**
 * Write the lock's page to its file.
 */
int32_t subetha_rwlock_flush(subetha_handle handle);

/**
 * Remove the lock's file at `path`. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_rwlock_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Create the semaphore at `path` with `initial` permits available and
 * `max_permits` as the ceiling a release may not push the count past.
 * `initial` above `max_permits` is refused. The three backing files are
 * initialized, so an existing semaphore at the same path is set back to
 * `initial`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_semaphore_create(const char *path,
                                 uint32_t initial,
                                 uint32_t max_permits,
                                 uint32_t mode,
                                 subetha_handle *out);

/**
 * Attach to the semaphore another process created at `path`, with the
 * same `max_permits` the creator used; the backing files must exist.
 * `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_semaphore_open(const char *path,
                               uint32_t max_permits,
                               uint32_t mode,
                               subetha_handle *out);

/**
 * Take a permit without waiting, and write the token naming it into
 * `out`. `SUBETHA_E_WOULD_BLOCK` when no permit is available.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_semaphore_try_acquire(subetha_handle handle, uint64_t *out);

/**
 * `subetha_semaphore_try_acquire`, waiting up to `timeout_ms` for a
 * permit; `SUBETHA_WAIT_FOREVER` waits without a deadline.
 * `SUBETHA_E_TIMEOUT` when the wait elapses and `SUBETHA_E_DESTROYED`
 * when the semaphore's handle is destroyed under it. The caller counts
 * among the waiters while it waits, so a release from any process
 * advances the wakeup generation.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_semaphore_acquire(subetha_handle handle, int64_t timeout_ms, uint64_t *out);

/**
 * Give the permit `token` names back. Another process waiting on the
 * semaphore is free to take it as soon as this returns, and the token
 * names nothing afterwards.
 *
 * A token given back twice is refused rather than returning a permit the
 * semaphore never lent, and so is a token from a permit that ended whose
 * slot has since been taken by another.
 */
int32_t subetha_semaphore_release(subetha_handle handle, uint64_t token);

/**
 * Whether `token` still names a permit this semaphore has lent.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_semaphore_holds(subetha_handle handle, uint64_t token, bool *out);

/**
 * A snapshot of the semaphore into `out`. Every count races an acquire
 * or a release in some process, so it is a diagnostic rather than
 * something to branch on.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_semaphore_read_stats(subetha_handle handle, struct subetha_semaphore_stats *out);

/**
 * Permits this handle has lent and not had back, which a caller reads
 * to find a leak: it should return to zero once every token taken has
 * been released.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_semaphore_held(subetha_handle handle, uint64_t *out);

/**
 * Remove the semaphore's three files at `path`: `<path>.count.bin`,
 * `<path>.wakeup.bin` and `<path>.waiters.bin`. The contract is
 * `subetha_ring_unlink`'s, and the report counts all three.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_semaphore_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the slab at `path` for `capacity` records of `layout`: an empty
 * one is initialized when the file does not exist, an existing one is
 * attached with its records in place. A file built with another capacity
 * or layout is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_slab_create(const char *path,
                            uint32_t capacity,
                            const struct subetha_element_layout *layout,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to the slab another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_slab_open(const char *path,
                          uint32_t capacity,
                          const struct subetha_element_layout *layout,
                          uint32_t mode,
                          subetha_handle *out);

/**
 * Attach to the slab at `path` with read access alone, for a process that
 * may not write the file. `get`, `slot_version` and the stats behave as
 * on any handle; `set` returns `SUBETHA_E_READ_ONLY`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_slab_open_read_only(const char *path,
                                    uint32_t capacity,
                                    const struct subetha_element_layout *layout,
                                    uint32_t mode,
                                    subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty slab there,
 * discarding every record other handles share. On Windows the file must
 * not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_slab_reset(const char *path,
                           uint32_t capacity,
                           const struct subetha_element_layout *layout,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Copy the record at `index` into `out`, at least `element_size` bytes,
 * its length into `out_len`; a slot nothing has written reads as zeros.
 * The copy is taken under the slot's SeqLock, so it is never torn.
 * `SUBETHA_E_OUT_OF_BOUNDS` when `index` is at or past the capacity.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_slab_get(subetha_handle handle,
                         uint64_t index,
                         uint8_t *out,
                         size_t cap,
                         size_t *out_len);

/**
 * Write `data`, exactly `element_size` bytes, at `index` under the slot's
 * SeqLock. One writer per slot: two writers on the same slot race, and
 * serializing them is the caller's contract. `SUBETHA_E_OUT_OF_BOUNDS`
 * when `index` is at or past the capacity.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_slab_set(subetha_handle handle, uint64_t index, const uint8_t *data, size_t len);

/**
 * The version word of slot `index` into `out`: twice the writes the slot
 * has taken, odd while a writer holds it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_slab_slot_version(subetha_handle handle, uint64_t index, uint32_t *out);

/**
 * A snapshot of the slab into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_slab_read_stats(subetha_handle handle, struct subetha_slab_stats *out);

/**
 * Write the slab's dirty pages to its file.
 */
int32_t subetha_slab_flush(subetha_handle handle);

/**
 * Remove the slab file at `path`. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_slab_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Create a blocking SPSC ring in anonymous memory, reachable from this
 * process only. `capacity` is a power of two of at least 2; `mode` is one
 * of the `SUBETHA_MODE_` constants.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_spsc_create_anon(uint32_t capacity, uint32_t mode, subetha_handle *out);

/**
 * Create a file-backed blocking SPSC ring under `base_path`: the ring at
 * `<base_path>.ring.bin` and its wakers at `.cw.bin` and `.pw.bin`.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_spsc_create(const char *base_path,
                            uint32_t capacity,
                            uint32_t mode,
                            subetha_handle *out);

/**
 * Attach to a file-backed blocking SPSC ring another process created
 * under `base_path`. `SUBETHA_E_RING_LAYOUT_MISMATCH` when the ring is
 * absent or was created with another capacity.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_spsc_open(const char *base_path,
                          uint32_t expected_capacity,
                          uint32_t mode,
                          subetha_handle *out);

/**
 * Push `len` bytes without waiting; the slot semantics are the adaptive
 * ring's. `SUBETHA_E_RING_FULL` when there is no room.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_spsc_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Pop one slot into `out` without waiting; `cap` must be at least
 * `SUBETHA_RING_SLOT_BYTES` and `out_len` receives the slot's size.
 * `SUBETHA_E_RING_EMPTY` when there is nothing to take.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_spsc_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Push `count` payloads of `len` bytes each, the first at `items` and
 * each next one `stride` bytes on, under one handle lookup and one panic
 * guard. The batch contract is `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_spsc_try_push_many(subetha_handle handle,
                                   const uint8_t *items,
                                   size_t stride,
                                   size_t len,
                                   size_t count,
                                   size_t *out_done);

/**
 * Pop up to `count` slots, the first written at `out` and each next one
 * `stride` bytes on; `stride` is at least `SUBETHA_RING_SLOT_BYTES`. The
 * batch contract is `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_spsc_try_pop_many(subetha_handle handle,
                                  uint8_t *out,
                                  size_t stride,
                                  size_t count,
                                  size_t *out_done);

/**
 * Push, parking until there is room or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_spsc_push_wait(subetha_handle handle,
                               const uint8_t *data,
                               size_t len,
                               int64_t timeout_ms);

/**
 * Pop, parking until an item arrives or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_spsc_pop_wait(subetha_handle handle,
                              uint8_t *out,
                              size_t cap,
                              size_t *out_len,
                              int64_t timeout_ms);

/**
 * Enable or disable predictive waiting on the consumer: when a producer
 * arrives on a regular cadence, the consumer parks until just before the
 * predicted arrival and spins a short guard band instead of paying the
 * wake. Off by default; it wins only for an in-process consumer whose
 * producer contends for cores, and loses across a process boundary.
 */
int32_t subetha_spsc_set_phase_locking(subetha_handle handle, bool enabled);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_spsc_read_stats(subetha_handle handle, struct subetha_spsc_stats *out);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the
 * ring and parks again unless it finds what it waited for.
 */
int32_t subetha_spsc_wake_all(subetha_handle handle);

/**
 * Remove the three files a file-backed blocking SPSC ring under
 * `base_path` names. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `base_path` is a NUL-terminated UTF-8 string; `report` is null or a
 * valid pointer.
 */
int32_t subetha_spsc_unlink(const char *base_path, struct subetha_unlink_report *report);

/**
 * Attach a pollable notifier to the SPSC ring `ring` names, for this
 * process to watch: every push through the ABI in any process on the
 * ring signals it. The contract is `subetha_ring_notifier`'s; beside a
 * file-backed ring the record is `<base>.notify.bin` and the Unix
 * notifiers `<base>.notify.<index>`, removed by `subetha_spsc_unlink`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_spsc_notifier(subetha_handle ring, subetha_handle *out);

/**
 * Obtain the vec at `path` for `capacity` elements of `layout`: an empty
 * one is initialized when the file does not exist, an existing one is
 * attached with its elements in place. A file built with another
 * capacity or layout is a `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_vec_create(const char *path,
                           uint32_t capacity,
                           const struct subetha_element_layout *layout,
                           uint32_t mode,
                           subetha_handle *out);

/**
 * Attach to the vec another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent file.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_vec_open(const char *path,
                         uint32_t capacity,
                         const struct subetha_element_layout *layout,
                         uint32_t mode,
                         subetha_handle *out);

/**
 * Attach to the vec at `path` with read access alone, for a process that
 * may not write the file. `get` and the stats behave as on any handle;
 * `push_back`, `pop_back`, `set` and `clear` return `SUBETHA_E_READ_ONLY`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_vec_open_read_only(const char *path,
                                   uint32_t capacity,
                                   const struct subetha_element_layout *layout,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Truncate the file at `path` and initialize an empty vec there,
 * discarding every element other handles share. On Windows the file must
 * not be mapped by any handle.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `layout` and `out` are valid
 * pointers.
 */
int32_t subetha_vec_reset(const char *path,
                          uint32_t capacity,
                          const struct subetha_element_layout *layout,
                          uint32_t mode,
                          subetha_handle *out);

/**
 * Append one element, exactly `element_size` bytes, and write the index
 * it landed at into `out_index` when that is not null.
 * `SUBETHA_E_RING_FULL` when the vec is at capacity.
 *
 * # Safety
 * `data` points to `len` readable bytes; `out_index` is null or a valid
 * pointer.
 */
int32_t subetha_vec_push_back(subetha_handle handle,
                              const uint8_t *data,
                              size_t len,
                              uint64_t *out_index);

/**
 * Append `count` elements from the caller's own array, each exactly
 * `element_size` bytes at `items + i * stride`, and write the index each
 * landed at into `out_indices`. One handle lookup and one panic guard
 * for the run.
 *
 * The indices are reported rather than left to be worked out: another
 * process may append between two of these calls, so the caller cannot
 * assume its elements are consecutive from the length it read before.
 * The batch stops at the first refusal and reports how many landed, so
 * `out_indices` holds `out_done` of them.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_indices`
 * addresses `count` writable `uint64_t`s; `out_done` is a valid pointer.
 */
int32_t subetha_vec_push_back_many(subetha_handle handle,
                                   const uint8_t *items,
                                   size_t stride,
                                   size_t len,
                                   size_t count,
                                   uint64_t *out_indices,
                                   size_t *out_done);

/**
 * Read `count` elements into the caller's own array, the element at
 * `indices[i]` into `out + i * stride`. One handle lookup and one panic
 * guard for the run; the batch stops at the first index the vec refuses
 * and reports how many it read.
 *
 * # Safety
 * `indices` addresses `count` readable `uint64_t`s; `out` addresses
 * `count * stride` writable bytes; `out_done` is a valid pointer.
 */
int32_t subetha_vec_get_many(subetha_handle handle,
                             const uint64_t *indices,
                             uint8_t *out,
                             size_t stride,
                             size_t count,
                             size_t *out_done);

/**
 * Remove the last element into `out`, at least `element_size` bytes, its
 * length into `out_len`. `SUBETHA_E_RING_EMPTY` when the vec is empty.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_vec_pop_back(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Copy the element at `index` into `out`, at least `element_size` bytes,
 * its length into `out_len`. `SUBETHA_E_OUT_OF_BOUNDS` when `index` is at
 * or past the length.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_vec_get(subetha_handle handle,
                        uint64_t index,
                        uint8_t *out,
                        size_t cap,
                        size_t *out_len);

/**
 * Overwrite the element at `index`, below the length, with `data`,
 * exactly `element_size` bytes. `SUBETHA_E_OUT_OF_BOUNDS` when `index` is
 * at or past the length.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_vec_set(subetha_handle handle, uint64_t index, const uint8_t *data, size_t len);

/**
 * Empty the vec for every handle: the length goes to zero and the next
 * push lands at index zero. Slot bytes stay in place, unreachable by
 * index. Not safe against a push or a pop running in any process.
 * `SUBETHA_E_READ_ONLY` on a handle opened read-only.
 */
int32_t subetha_vec_clear(subetha_handle handle);

/**
 * A snapshot of the vec into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_vec_read_stats(subetha_handle handle, struct subetha_vec_stats *out);

/**
 * Write the vec's dirty pages to its file.
 */
int32_t subetha_vec_flush(subetha_handle handle);

/**
 * Remove the vec file at `path`. The contract is `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_vec_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * Obtain the chain at `path` holding `capacity` versions: an empty one is
 * initialized when the file does not exist, an existing one is attached
 * with its history in place. A chain built with another capacity is a
 * `SUBETHA_E_RING_LAYOUT_MISMATCH`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_versioned_chain_create(const char *path,
                                       uint64_t capacity,
                                       uint32_t mode,
                                       subetha_handle *out);

/**
 * Truncate the chain's file at `path` and initialize an empty one,
 * discarding every version live peers share.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_versioned_chain_reset(const char *path,
                                      uint64_t capacity,
                                      uint32_t mode,
                                      subetha_handle *out);

/**
 * Attach to the chain another process created at `path`; the file must
 * exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `out` is a valid pointer.
 */
int32_t subetha_versioned_chain_open(const char *path,
                                     uint64_t capacity,
                                     uint32_t mode,
                                     subetha_handle *out);

/**
 * Make the `len` bytes at `value` the version current from `version`.
 * `version` must be strictly above the head's, and `SUBETHA_E_RING_FULL`
 * says every node is taken.
 *
 * # Safety
 * `value` points to `len` readable bytes.
 */
int32_t subetha_versioned_chain_push(subetha_handle handle,
                                     uint64_t version,
                                     const uint8_t *value,
                                     size_t len);

/**
 * The value current as of `snapshot_version` into `out`, and its length
 * into `out_len`. `SUBETHA_E_MAP_KEY_ABSENT` when no version at or below
 * that one is in the chain.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_versioned_chain_read_at(subetha_handle handle,
                                        uint64_t snapshot_version,
                                        uint8_t *out,
                                        size_t cap,
                                        size_t *out_len);

/**
 * The head version into `out_version` and its value into `out`, the way
 * `subetha_versioned_chain_read_at` reports one.
 * `SUBETHA_E_MAP_KEY_ABSENT` on an empty chain.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` and `out_version` are
 * valid pointers.
 */
int32_t subetha_versioned_chain_current(subetha_handle handle,
                                        uint64_t *out_version,
                                        uint8_t *out,
                                        size_t cap,
                                        size_t *out_len);

/**
 * Empty the chain and rebuild its free list. Every version goes, and a
 * snapshot version a reader still holds stops naming anything.
 */
int32_t subetha_versioned_chain_clear(subetha_handle handle);

/**
 * Push the chain's dirty pages to disk, returning when they are durable.
 */
int32_t subetha_versioned_chain_flush(subetha_handle handle);

/**
 * Start pushing the chain's dirty pages to disk and return at once.
 */
int32_t subetha_versioned_chain_flush_async(subetha_handle handle);

/**
 * A snapshot of the chain into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_versioned_chain_read_stats(subetha_handle handle,
                                           struct subetha_versioned_chain_stats *out);

/**
 * Obtain the map at `tree_path` with `capacity` nodes for `key_size`-byte
 * keys and `value_size`-byte values, and its epoch table at `epochs_path`
 * holding `max_pins` pins. `epochs_path` may be the table the store's
 * other structures share.
 *
 * # Safety
 * `tree_path` and `epochs_path` are NUL-terminated UTF-8 strings; `out`
 * is a valid pointer.
 */
int32_t subetha_versioned_map_create(const char *tree_path,
                                     uint64_t capacity,
                                     size_t key_size,
                                     size_t value_size,
                                     const char *epochs_path,
                                     uint64_t max_pins,
                                     uint32_t mode,
                                     subetha_handle *out);

/**
 * `subetha_versioned_map_create` at the default class.
 *
 * # Safety
 * `tree_path` and `epochs_path` are NUL-terminated UTF-8 strings; `out`
 * is a valid pointer.
 */
int32_t subetha_versioned_map_create_default(const char *tree_path,
                                             uint64_t capacity,
                                             const char *epochs_path,
                                             uint64_t max_pins,
                                             uint32_t mode,
                                             subetha_handle *out);

/**
 * Attach to the map and epoch table another process created; both files
 * must exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `tree_path` and `epochs_path` are NUL-terminated UTF-8 strings; `out`
 * is a valid pointer.
 */
int32_t subetha_versioned_map_open(const char *tree_path,
                                   uint64_t capacity,
                                   size_t key_size,
                                   size_t value_size,
                                   const char *epochs_path,
                                   uint64_t max_pins,
                                   uint32_t mode,
                                   subetha_handle *out);

/**
 * Make `value` current at `key` at a fresh epoch.
 * `SUBETHA_E_WOULD_BLOCK` when the key's tombstone is still reachable by
 * a pin.
 *
 * # Safety
 * `key` points to `key_len` readable bytes and `value` to `value_len`.
 */
int32_t subetha_versioned_map_insert(subetha_handle handle,
                                     const uint8_t *key,
                                     size_t key_len,
                                     const uint8_t *value,
                                     size_t value_len);

/**
 * The value current at `key` into `out`, and its length into `out_len`.
 * `SUBETHA_E_MAP_KEY_ABSENT` when the key is absent or a tombstone.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_versioned_map_get(subetha_handle handle,
                                  const uint8_t *key,
                                  size_t key_len,
                                  uint8_t *out,
                                  size_t cap,
                                  size_t *out_len);

/**
 * Stamp `key` as superseded at a fresh epoch, writing what was current
 * into `out`. The entry stays until no pin can reach it.
 * `SUBETHA_E_MAP_KEY_ABSENT` when the key was absent or already a
 * tombstone.
 *
 * # Safety
 * `key` points to `key_len` readable bytes; `out` points to `cap`
 * writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_versioned_map_remove(subetha_handle handle,
                                     const uint8_t *key,
                                     size_t key_len,
                                     uint8_t *out,
                                     size_t cap,
                                     size_t *out_len);

/**
 * The entries current at one moment, in key order, at most `limit` of
 * them, packed into `out` as key bytes then value bytes per entry.
 *
 * `low` and `high` are inclusive bounds, each null for unbounded, and
 * each of the map's key size when given. `out_count` receives the entries
 * written and `out_len` the bytes they take.
 * `SUBETHA_E_BUFFER_TOO_SMALL` leaves `out` untouched with the bytes
 * needed already reported.
 *
 * The pin is taken and released inside the call, so the result is one
 * consistent moment. The limit counts entries examined, not entries
 * returned, so a range dense in tombstones can return fewer than `limit`
 * while more remain.
 *
 * # Safety
 * `low` and `high` are null or point to `key_size` readable bytes; `out`
 * points to `cap` writable bytes; `out_len` and `out_count` are valid
 * pointers.
 */
int32_t subetha_versioned_map_range(subetha_handle handle,
                                    const uint8_t *low,
                                    const uint8_t *high,
                                    size_t limit,
                                    uint8_t *out,
                                    size_t cap,
                                    size_t *out_len,
                                    size_t *out_count);

/**
 * Drop every tombstone no pin can reach, reporting how many went through
 * `out_freed` when that is not null. `SUBETHA_E_RING_IO` when none could
 * go, which is what a full tree reports.
 *
 * # Safety
 * `out_freed` is null or a valid pointer.
 */
int32_t subetha_versioned_map_sweep(subetha_handle handle, uint64_t *out_freed);

/**
 * Undo every stamp this map holds at `epoch`: an entry born there goes,
 * and one superseded there is current again. Reports the entries touched
 * through `out_touched` when that is not null.
 *
 * # Safety
 * `out_touched` is null or a valid pointer.
 */
int32_t subetha_versioned_map_void_epoch(subetha_handle handle,
                                         uint64_t epoch,
                                         uint64_t *out_touched);

/**
 * The epoch a reader pinning now would see, into `out`. The pin is taken
 * and released inside the call; it names the moment rather than holding
 * it.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_versioned_map_pin_epoch(subetha_handle handle, uint64_t *out);

/**
 * Push the map's dirty pages to disk, returning when they are durable.
 */
int32_t subetha_versioned_map_flush(subetha_handle handle);

/**
 * A snapshot of the map into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_versioned_map_read_stats(subetha_handle handle,
                                         struct subetha_versioned_map_stats *out);

/**
 * Obtain the slab at `slab_path` with `capacity` slots of `value_size`
 * bytes keeping `depth` versions each, and its epoch table at
 * `epochs_path` holding `max_pins` pins. `epochs_path` may be the table
 * the store's other structures share.
 *
 * # Safety
 * `slab_path` and `epochs_path` are NUL-terminated UTF-8 strings; `out`
 * is a valid pointer.
 */
int32_t subetha_versioned_slab_create(const char *slab_path,
                                      uint64_t capacity,
                                      size_t value_size,
                                      size_t depth,
                                      const char *epochs_path,
                                      uint64_t max_pins,
                                      uint32_t mode,
                                      subetha_handle *out);

/**
 * `subetha_versioned_slab_create` at the default class:
 * `SUBETHA_VERSIONED_VALUE_BYTES_DEFAULT` bytes and
 * `SUBETHA_VERSIONED_DEPTH_DEFAULT` versions.
 *
 * # Safety
 * `slab_path` and `epochs_path` are NUL-terminated UTF-8 strings; `out`
 * is a valid pointer.
 */
int32_t subetha_versioned_slab_create_default(const char *slab_path,
                                              uint64_t capacity,
                                              const char *epochs_path,
                                              uint64_t max_pins,
                                              uint32_t mode,
                                              subetha_handle *out);

/**
 * Attach to the slab and epoch table another process created; both files
 * must exist. `SUBETHA_E_RING_IO` names an absent one.
 *
 * # Safety
 * `slab_path` and `epochs_path` are NUL-terminated UTF-8 strings; `out`
 * is a valid pointer.
 */
int32_t subetha_versioned_slab_open(const char *slab_path,
                                    uint64_t capacity,
                                    size_t value_size,
                                    size_t depth,
                                    const char *epochs_path,
                                    uint64_t max_pins,
                                    uint32_t mode,
                                    subetha_handle *out);

/**
 * Make the `len` bytes at `value` the version current at slot `i`, from a
 * fresh epoch. `len` must be the slab's value size.
 * `SUBETHA_E_WOULD_BLOCK` says the chain is full and every version in it
 * is still reachable by a pin.
 *
 * # Safety
 * `value` points to `len` readable bytes.
 */
int32_t subetha_versioned_slab_set(subetha_handle handle,
                                   uint64_t i,
                                   const uint8_t *value,
                                   size_t len);

/**
 * The current value at slot `i` into `out`, and its length into
 * `out_len`. `SUBETHA_E_MAP_KEY_ABSENT` when the slot has no live version.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_versioned_slab_get(subetha_handle handle,
                                   uint64_t i,
                                   uint8_t *out,
                                   size_t cap,
                                   size_t *out_len);

/**
 * Supersede the current version at slot `i` at a fresh epoch, writing
 * what was current into `out` the way `subetha_versioned_slab_get` does.
 * `SUBETHA_E_MAP_KEY_ABSENT` when the slot had no live version.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_versioned_slab_retire(subetha_handle handle,
                                      uint64_t i,
                                      uint8_t *out,
                                      size_t cap,
                                      size_t *out_len);

/**
 * Drop every version at slot `i` that no pin can reach, reporting how
 * many went through `out_dropped` when that is not null. A push does this
 * itself on a full chain; this is for a caller reclaiming on its own
 * schedule.
 *
 * # Safety
 * `out_dropped` is null or a valid pointer.
 */
int32_t subetha_versioned_slab_sweep_slot(subetha_handle handle, uint64_t i, uint64_t *out_dropped);

/**
 * Undo every stamp this slab holds at `epoch`: a version born there goes,
 * and a version superseded there is current again. For an epoch whose
 * holder died part way through a compound write. Reports the versions
 * touched through `out_touched` when that is not null.
 *
 * # Safety
 * `out_touched` is null or a valid pointer.
 */
int32_t subetha_versioned_slab_void_epoch(subetha_handle handle,
                                          uint64_t epoch,
                                          uint64_t *out_touched);

/**
 * Push the slab's dirty pages to disk, returning when they are durable.
 */
int32_t subetha_versioned_slab_flush(subetha_handle handle);

/**
 * A snapshot of the slab into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_versioned_slab_read_stats(subetha_handle handle,
                                          struct subetha_versioned_slab_stats *out);

/**
 * Create a Vyukov ring in anonymous memory, reachable from this process
 * only. `capacity` is a power of two of at least 2; `scan_interval_us`
 * in the options is ignored, the ring has no sidecar.
 *
 * # Safety
 * `options` and `out` are valid pointers.
 */
int32_t subetha_vyukov_create_anon(uint32_t capacity,
                                   const struct subetha_ring_options *options,
                                   subetha_handle *out);

/**
 * Create a file-backed Vyukov ring at `path`, truncating any file there,
 * with its wakers at `<path>.cwaker.bin` and `<path>.pwaker.bin`.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_vyukov_create(const char *path,
                              uint32_t capacity,
                              const struct subetha_ring_options *options,
                              subetha_handle *out);

/**
 * Attach to a file-backed Vyukov ring another process created at `path`.
 * `SUBETHA_E_RING_LAYOUT_MISMATCH` when the file is absent or was created
 * with another capacity.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_vyukov_open(const char *path,
                            uint32_t expected_capacity,
                            const struct subetha_ring_options *options,
                            subetha_handle *out);

/**
 * Create a Vyukov ring in the named shared-memory region `name`, in the
 * namespace `SUBETHA_SHM_SESSION` or `SUBETHA_SHM_MACHINE`, with its
 * wakers in `{name}_cwaker` and `{name}_pwaker`.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_vyukov_create_shm(const char *name,
                                  uint32_t capacity,
                                  uint32_t shm_namespace,
                                  const struct subetha_ring_options *options,
                                  subetha_handle *out);

/**
 * Attach to a Vyukov ring another process created in named shared
 * memory; the namespace must match the creator's.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `options` and `out` are valid
 * pointers.
 */
int32_t subetha_vyukov_open_shm(const char *name,
                                uint32_t expected_capacity,
                                uint32_t shm_namespace,
                                const struct subetha_ring_options *options,
                                subetha_handle *out);

/**
 * Push up to `SUBETHA_RING_PAYLOAD_MAX` bytes without waiting; the slot
 * is zero past the payload. `SUBETHA_E_RING_FULL` when there is no room.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_vyukov_try_push(subetha_handle handle, const uint8_t *data, size_t len);

/**
 * Push `count` payloads of `len` bytes each, the first at `items` and
 * each next one `stride` bytes on, under one handle lookup and one panic
 * guard. The batch contract is `subetha_ring_try_push_many`'s.
 *
 * # Safety
 * `items` addresses `count * stride` readable bytes; `out_done` is a
 * valid pointer.
 */
int32_t subetha_vyukov_try_push_many(subetha_handle handle,
                                     const uint8_t *items,
                                     size_t stride,
                                     size_t len,
                                     size_t count,
                                     size_t *out_done);

/**
 * Pop up to `count` payloads, the first written at `out` and each next
 * one `stride` bytes on; `stride` is at least
 * `SUBETHA_RING_PAYLOAD_MAX`. The batch contract is
 * `subetha_ring_try_pop_many`'s.
 *
 * # Safety
 * `out` addresses `count * stride` writable bytes; `out_done` is a valid
 * pointer.
 */
int32_t subetha_vyukov_try_pop_many(subetha_handle handle,
                                    uint8_t *out,
                                    size_t stride,
                                    size_t count,
                                    size_t *out_done);

/**
 * Pop one slot's payload, `SUBETHA_RING_PAYLOAD_MAX` bytes, into `out`
 * without waiting; `cap` must be at least that.
 * `SUBETHA_E_RING_EMPTY` when there is nothing to take.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_vyukov_try_pop(subetha_handle handle, uint8_t *out, size_t cap, size_t *out_len);

/**
 * Push, parking until there is room or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_push_wait`'s.
 *
 * # Safety
 * `data` points to `len` readable bytes.
 */
int32_t subetha_vyukov_push_wait(subetha_handle handle,
                                 const uint8_t *data,
                                 size_t len,
                                 int64_t timeout_ms);

/**
 * Pop, parking until an item arrives or `timeout_ms` elapses; the waiting
 * contract is `subetha_ring_pop_wait`'s.
 *
 * # Safety
 * `out` points to `cap` writable bytes; `out_len` is a valid pointer.
 */
int32_t subetha_vyukov_pop_wait(subetha_handle handle,
                                uint8_t *out,
                                size_t cap,
                                size_t *out_len,
                                int64_t timeout_ms);

/**
 * A snapshot of the ring's state into `out`.
 *
 * # Safety
 * `out` is a valid pointer.
 */
int32_t subetha_vyukov_read_stats(subetha_handle handle, struct subetha_vyukov_stats *out);

/**
 * Write the ring's dirty pages to its file. Nothing to do in the
 * anonymous and shared-memory locales.
 */
int32_t subetha_vyukov_flush(subetha_handle handle);

/**
 * Find the first slot at or after `from`, within the claimed but
 * undrained window, that a producer claimed and never published, which
 * is what a producer that died mid-push leaves behind. `out_found` says
 * whether there is one and `out_pos` names it. Never called by the data
 * path; a watchdog calls it once it has established that a producer is
 * dead.
 *
 * # Safety
 * `out_found` and `out_pos` are valid pointers.
 */
int32_t subetha_vyukov_next_stuck_slot(subetha_handle handle,
                                       uint64_t from,
                                       bool *out_found,
                                       uint64_t *out_pos);

/**
 * Publish the slot at `pos` on behalf of a producer that died after
 * claiming it, so the next consumer drains it; its bytes are whatever
 * that producer wrote. `out_healed` is false when the slot was not stuck.
 * The caller must have established that the producer is dead: a live
 * one racing this call publishes the same value, and the consumer then
 * drains a slot that producer never finished writing.
 *
 * # Safety
 * `out_healed` is a valid pointer.
 */
int32_t subetha_vyukov_heal_stuck_slot(subetha_handle handle, uint64_t pos, bool *out_healed);

/**
 * Wake every thread parked in a wait on this ring; each re-checks the
 * ring and parks again unless it finds what it waited for.
 */
int32_t subetha_vyukov_wake_all(subetha_handle handle);

/**
 * Remove the ring file at `path` and its two waker files. The contract is
 * `subetha_ring_unlink`'s.
 *
 * # Safety
 * `path` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_vyukov_unlink(const char *path, struct subetha_unlink_report *report);

/**
 * A shared-memory ring's regions are released with the last handle that
 * maps them, as for `subetha_ring_unlink_shm`; this reports zero removed
 * and succeeds.
 *
 * # Safety
 * `name` is a NUL-terminated UTF-8 string; `report` is null or a valid
 * pointer.
 */
int32_t subetha_vyukov_unlink_shm(const char *name,
                                  uint32_t shm_namespace,
                                  struct subetha_unlink_report *report);

#ifdef __cplusplus
}  // extern "C"
#endif  // __cplusplus

#endif  /* SUBETHA_H */
